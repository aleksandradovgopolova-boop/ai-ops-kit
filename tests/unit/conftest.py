"""Общие фикстуры разрезанных мега-тест-файлов — здесь, а не импортом в сами тест-файлы.

Почему conftest, а не `from _installer_helpers import child`: импорт фикстуры, которую тест
использует как ПАРАМЕТР, ruff видит как F811 (параметр «переопределяет» импортированное имя). В
conftest фикстуры не используются как параметры — они лишь ре-экспортируются, поэтому F811 не
возникает, а pytest подхватывает их автоматически по имени (нативный способ делить фикстуры между
файлами без импорта). Это тот же приём, что и корневой `tests/conftest.py`.

Плоские helper-ФУНКЦИИ (не фикстуры) по-прежнему импортируются в каждый файл из соответствующего
`_*_helpers.py` — их параметрами не зовут, F811 на них нет.

Коллизий имён нет: любой другой тест-файл, объявляющий свою `child`/`installed`/…, перекрывает эту
(pytest берёт ближайшую фикстуру), поэтому ре-экспорт достаёт ровно разрезанные файлы, которые своей
копии не держат.
"""
# Фикстуры делаются доступными импортом их в пространство имён conftest (F401 в наборе кита
# намеренно не включён — это не «поломка»).
from _installer_helpers import child, installed, installed_copy  # noqa: F401
from _workpackage_helpers import three_area_sig, two_area_sig  # noqa: F401
from _pipeline_char_helpers import child_repo  # noqa: F401
