---
read_tier: 1
stability: volatile
reviewed_at: '2026-09-14'
owner: product-team
status: actual
---

# ProductStatus.md — что реально готово и живёт сейчас

**Одна точка правды о фактическом состоянии продукта.** Это НЕ «как должно быть»
(для этого — нормативные JTBD/USE_CASES/MVP_SCOPE/ADR), а «что уже построено, где
работает и что отложено». Каждая новая сессия читает этот файл **первым** (см.
`ai-session-start`), и он обновляется на **каждом PR, меняющем готовность** (см.
`rules/core/ProductStatusPolicy.md`).

Это снимок: он верен на `reviewed_at` из frontmatter. Устаревшую строку исправляет тот
PR, который меняет реальность, — а не «когда-нибудь потом».

> **Этот файл был шаблоном до 2026-08-12**: вместо фактов в нём стояла инструкция-заглушка о том,
> что заполнить его должен child. Кит требует ProductStatus от каждой подключённой дочки, а у себя
> держал заготовку — тот же класс, что R-21 («требует план от дочек, не имея своего»).
> Заполнен по замеру: числа получены командами, названными в правой колонке.

## Что реально готово / живёт

Продукт — **переиспользуемый пакет**, а не сервис: «прод» для него — это child-репозитории,
куда он установлен. Поэтому строки ниже — про подсистемы пакета, а не про backend/frontend.

| Область | Статус | Чем подтверждено |
|---|---|---|
| Движок исполнения (WorkItem → pipeline → evidence → gates → delivery) | **готово** | 368 модулей в 19 пакетах (замер 14.09.2026; рост от разреза монолитов на фасад+сателлит — кампании ≥1000 и среднего яруса, 0 монолитов ≥700) |
| Quality gates | **готово** | 36 <!-- claim:gates-total --> гейтов, из них 23 блокирующих (`quality/gates.yaml`); 13 advisory — обкатка приостановлена (приоритет ушёл в Product OS, вернётся с работой) |
| Валидаторы (машинная проверка инвариантов) | **готово** | 88 <!-- claim:validators-total --> валидаторов в `ai_ops_kit/validation/`, все запускаются извне (замер 17.09.2026: +валидатор из недавних кампаний — паспорт сведён к реальному числу файлов validate_*.py). |
| Установщик и обновление child-репозиториев | **готово** | `init`/`update`/`doctor`/`diff`/`migrate` + 10 команд; транзакционный откат по backup, миграции исполняются по-настоящему |
| Политика обновлений `update_policy: pr` | **готово с 3.36.9+** | F-022: локальный `update` при `policy: pr` уходит в ветку `ai-ops/update-v<версия>`, рабочее дерево владельца не трогается. В теге 3.36.9 ещё НЕ исполнялась — исправление влито после релиза (PR #84) |
| Провайдеры | **частично** | реализованы 3 адаптера: `anthropic`, `openai`, `openai-compatible` (+ `claude-cli`, `mock`). В `registry/providers.yaml` объявлено 10 — остальные помечены `DECLARED_NOT_IMPLEMENTED` с причиной, не молчат |
| Собственный CI | **готово** | 11 проверок на PR: lint, coverage (ратчет, пол 70% при замере 71%), python39-compat, clean-install, pipeline-e2e 3.9/3.12, 4 группы quality, smoke |
| Координация параллельных сессий | **частично** | реестр активных работ с межпроцессным локом работает; контракт коммита исполняется хуком `commit-contract`; правило «worktree на сессию» держится на исполнителе — проверки нет (`docs/parallel-sessions.md`) |
| Квалификация на живом продукте | **готово** | первый brownfield (niti) и второй (ии-среда: owner-led verified PR #701, merge 2026-09-02) пройдены; повтор на ДВУХ внешних репозиториях до verified PR (#668); полевые доказательства v4.3.0 на двух дочках (`registry/release-claims.yaml`). Остаются доказываемые ЖИВЫМ прогоном (не кодом) исходы zero-touch: `owner-speaks` и `owner-does-not-design-parallelism` — ждут owner-led замера |

## Что отложено и почему

- **Принятие `ruff format`** — отложено осознанно. Замер при реальной конвенции (line-length 110,
  p99 по символам): 528 файлов из 901, ~45 000 строк. Один такой коммит топит `git blame` по всему
  репозиторию; горизонт «Later» в `ROADMAP.md`.
- **`RUF100`** — не включать, пока набор ruff узкий: под текущим набором 391 директива помечается
  «неиспользуемой» лишь потому, что правило не включено; `--fix` снёс бы легитимные подавления.
- **Остаток ратчета гашения исключений** — `tests/` (7 мест). Продакшн-пакет разобран целиком;
  в тестах гашение не может дать ложный green продукту, поэтому срез последний.
- **Строгий DAG пакетов — ДОСТИГНУТ.** Честный замер (F-03): 0 <!-- claim:layering-pairs --> взаимных
  пар и 0 <!-- claim:layering-cycles --> циклов длиннее двух внутри capabilities, заратчено как потолок
  (`packages/layering.yaml`). Прежний «0» был слепотой анализатора к `__import__`: пары не сняли, а
  спрятали за динамическим импортом; гейт теперь их считает — и все пять сняты по существу (K1
  engine↔gates, K4 engops↔gates, K2 context↔engine, K3 delivery↔engine, K5 engine↔lifecycle —
  переносом проводки в правильный слой, не сокрытием). Кольцо стало DAG по-честному. Остаётся отдельным
  срезом только переход внутренних импортов на пакетные имена (`internal_imports_by_flat_name`).
- **Провайдеры `google`/`gigachat`/`local`/`custom`** — объявлены в реестре и не реализованы;
  честная ошибка с причиной вместо тихого фолбэка на mock.

## Границы, названные прямо

- **`stable` заработан честно (v4.3.1, 2026-09-14).** Прежняя граница «stable опережает реальность»
  снята: критерий канала теперь требует ПОЛЕВЫХ доказательств на ≥2 дочках
  (`field_evidence_min_repos: 2`) и продуктового слоя, и это проверяет `validate_release_claims` —
  слово `stable` больше не декларируется, а зарабатывается. Полевые доказательства v4.3.0 собраны на
  двух дочках. Урок 2026-08-12 (пять дефектов P1/P2, четыре — из поля) и стал причиной ужесточения
  критерия.
- **Изоляция прогона — не полный jail.** Брокер даёт enforceable-подмножество; полная изоляция
  файловой системы, сети и ресурсов — задача контейнерного runtime (объявлено в манифесте).
- **Числа в этом файле — снимок на `reviewed_at`.** Те, что кит умеет считать сам
  (`gates_count`, `agents_count`, `validators_count`), живут в `registry/release-claims.yaml`
  и проверяются `validate_release_claims`; здесь они продублированы для чтения человеком.

## Источники истины

- Версия и релизные заявления: `VERSION`, `registry/release-claims.yaml`
- Что делаем сейчас и что взять следующим: `planning/plan.yaml`, `ROADMAP.md` (`ai-ops next`)
- Реестр находок с доказательствами: `docs/audit-report.md`
- Квалификация на живых продуктах: `qualification/REAL-PRODUCT-*.md`
- Параллельная работа нескольких сессий: `docs/parallel-sessions.md`
- Гейты и их политики: `quality/gates.yaml`

> Если дата выше старше 14 дней — `validate_freshness.py` пометит документ протухшим.
> Для статуса готовности это сигнал: снимок мог разойтись с реальностью, перепроверьте
> до того, как анкорить на нём задачу.
