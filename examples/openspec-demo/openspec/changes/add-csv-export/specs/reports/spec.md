## ADDED Requirements

### Requirement: CSV export
The system SHALL allow an authenticated user to export a report as a CSV file.

#### Scenario: Successful CSV export
- WHEN the user clicks "Export CSV"
- THEN the system downloads a CSV file with the report data
