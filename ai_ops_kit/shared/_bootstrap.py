"""Алиас: shared._bootstrap -> плоский модуль _bootstrap (один объект, не копия).

ЕДИНСТВЕННЫЙ модуль, чей код остаётся плоским. Он настраивает sys.path, в том числе кладёт туда
корень репозитория — а без корня пакет ai_ops_kit не импортируется. Переезд его кода в пакет дал
бы цикл: чтобы импортировать пакет, нужен путь, который добавляет модуль внутри этого пакета.
"""
import sys

import _bootstrap as _target

sys.modules[__name__] = _target
