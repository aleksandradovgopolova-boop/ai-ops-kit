"""shared — общий фундамент: bootstrap sys.path, контракты, детект стека, генераторы (5 модулей).

Пакетные имена (`ai_ops_kit.<пакет>.<модуль>`) — единственные точки входа: снятый
в 4.0 слой `tools/` больше не существует.
"""
