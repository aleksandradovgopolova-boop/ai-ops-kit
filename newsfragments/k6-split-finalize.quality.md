K6-глубина: финализация прогона (событие run_completed для спутников, запись run_end в журнал, вывод статуса работы done/blocked и снятие с учёта active-work) вынесена из god-функции `run()` контроллера в отдельную `_finalize_run`. `run()` уменьшилась с 866 до 812 строк и стала меньше `run_pipeline` — глобальный максимум размера функций теперь держит `run_pipeline` (864). Потолок опущен 866 → 864. Поведение сохранено (тесты контроллера зелёные до и после без правок).
