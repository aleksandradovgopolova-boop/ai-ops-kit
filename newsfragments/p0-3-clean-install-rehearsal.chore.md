Репетиция P0 №3: кит поставлен из текущего main в одноразовый репозиторий командой `setup` (init→doctor→onboard→bootstrap→model, 640 файлов, авто-коммит) и продуктовые входные поверхности пройдены на РЕАЛЬНОЙ установке (не через PYTHONPATH): `specify` (эхо «Поняла», без id/YAML/CLI), `propose` (продуктом, без «фундамента»), `status`, `next` — все чисто. Отчёт `qualification/ONE-FEATURE-ZERO-PIPELINE-REHEARSAL-2026-09-16.md`. Границы run→Verified→merge→Outcome и внешний замер человеком остаются за P0 №3 (полный)/№7. Находка (не блокер): плейсхолдер-id `goal-id-1` из bootstrap-черновика течёт в текст `propose`.
