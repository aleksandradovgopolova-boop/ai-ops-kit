Закрыт P0-класс R-42: симлинк внутри рабочего корня (`src/out -> migrations/destructive`) обходил и
`protected_paths`, и `write_scope` — политика судила НАПИСАННЫЙ путь, а запись разыменовывала симлинк,
и физический страж `_within_root` ловил только побег ЗА корень. Брокер теперь в `execute()` судит
РАЗЫМЕНОВАННУЮ цель записи по deny-стороне (protected + побег через симлинк), оставляя `write_scope`
на написанном пути (иначе он стал бы fail-open). Это восстанавливает контроль над `production/`,
`migrations/destructive/`, `security/`, `.ai-ops.yaml` — включая writer≠judge (модель больше не может
переписать файл своих же ограничений через псевдоним). Замер — пробой по содержимому на диске.
