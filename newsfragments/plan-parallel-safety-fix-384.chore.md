Находка поля #384 заведена в план (`team-works-in-parallel`): гейт parallel-safety `--strict` валит собственные install/update-PR кита — координационные `planning/plan.yaml` и `history/` в них неизбежны при первом заезде на репозиторий без кита. Готово, когда гейт различает update/install-PR кита либо `update` раскладывает миграцию плана в отдельный координаторский PR.
