Добавлена третья конституция кита — **Продуктовая** (`standards/product/PRODUCT_CONSTITUTION.md`, статьи `PROD-001…010`): у фичи объявлена метрика успеха и адресат/JTBD, ставки обратимы, заявки честны, фича заведена в реестре, есть discovery до кода, объявлены non-goals и альтернативы, а после выпуска измеряется исход. По образцу арх/UI-UX-конституций: источник + генератор (`scripts/build-rules.py`) + машинный реестр `rules.yaml`, зеркало-тест держит их в синхроне. Проведена в тот же механизм конформанса (`constitution_conformance.CONSTITUTIONS`) наравне с арх и UI/UX — статьи `PROD-*` резолвятся тем же кодом. Три статьи ссылаются на реальные существующие гейты (`discovery_completeness`, `feature_coverage`), остальные — честный advisory-долг (`Гейт: none`); новых гейтов работа не заводит.
