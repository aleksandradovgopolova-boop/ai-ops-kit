Машинный реестр Архитектурной конституции (`standards/architecture/rules.yaml`) теперь **едет в дочку** (`managed_set` + пакет `ai-ops-quality`, как uiux) — конституция перестала быть прозой в репозитории кита: её ID резолвятся в дочке. Тяжёлый источник и оперативный слой остаются родительскими (узкий footprint-бюджет доставки). Волна #825 направления #820.
