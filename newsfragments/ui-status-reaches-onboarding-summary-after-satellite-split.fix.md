Команда `ai-ops ui-status` снова доходит до онбординг-сводки, а не падает на полпути. При недавнем
разрезе установщика (#815) функция сводки переехала в сателлит, а вызов остался обращённым к старому
месту — команда роняла человека ошибкой вместо статуса. Вызов возвращён к тому файлу, где функция
теперь живёт, и путь закрыт тестом, который запускает саму команду: разъедется снова — тест покраснеет.
