Описание задачи (`specify`) больше не показывает человеку внутренний id фичи (`wi-…`) и флаги CLI (`--feature`, синтаксис `--answers`). Вход — одна фраза; в ответе — вопросы обычными словами и продуктовое «дальше — план», а точные команды остаются в технических деталях по запросу (#958).
