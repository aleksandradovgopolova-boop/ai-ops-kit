Превентивно разрезан монолит `tests/unit/test_ai_ops_cli.py` (837/850 у потолка анти-монолит-гейта) по когезивным темам: машинерия превью и флагов осталась в исходном файле, main()/прямые действия команд вынесены в `test_ai_ops_cli_main.py`, а владельческие read-only команды `explain` и `inbox` — в `test_ai_ops_cli_owner_queue.py`. Общий помощник `_explain_run` не задублирован — explain и inbox делят его в одном файле; поведение тестов не менялось (#819).
