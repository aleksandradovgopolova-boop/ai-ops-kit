Тир-2 WIRE (#678, часть): генератор контекста `context/context_compiler.compile_bundle` теперь сам проверяет форму собранного ContextBundle (validate-and-warn) — вердикт формы проведён в генератор, а не только в контракт-тест. Нарушение (напр. included не объект) даёт громкое `RuntimeWarning` «контекст модели может быть неполным/нечестным», но bundle всё равно возвращается (warn, не блок — компиляция контекста не срывается). Чистый `check()` вынесен вниз в `checks/context_bundle.py`, рантайм зовёт его context→checks без восходящего ребра context→validation (v3.38-приём, тем же способом, что run_handoff в #687).
