Стена CI на PR срезана по замеру длительностей (#462): она держалась не на дисбалансе слоёв, а на двух тяжёлых тестах — мутационном гейте `test_every_declared_probe_of_the_kit_kills_its_mutant` (~6 мин) и агрегатном `TestRunBenchAggregate` (~2.5 мин, module-scoped прогон корпуса). Оба помечены маркером `nightly` и сняты с пути каждого PR (селфтест-слои гоняют `-m "slow and not nightly"`); гоняются в новом `nightly-quality.yml` (ночь + push в main + вручную). Стена `package-quality` на PR: ~10 → ~4.5 мин. Размен осознанный, как с покрытием: выживший мутант / ложно-зелёное ловится ночью и на main, не блокируя каждый PR (вернуть гейт на PR быстрым — через распараллеливание, #462 вариант B).
