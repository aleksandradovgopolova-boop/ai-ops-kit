Пульт-карта продукта переставлен под взгляд владельца: из шапки убраны логотип и служебная
подпись про поток блоков, а роадмап теперь показывает **три горизонта в ряд — «Сейчас · Дальше ·
Позже»** — с ёмкими названиями направлений на понятном языке (взяты из `ROADMAP.md`, не выдуманы;
если направлений больше — «+ ещё N»). Сама карта продукта (видение → … → паспорт) переехала вниз
дашборда широкой полосой. Сборщик `dashboard/build_data.py` вместо одного горизонта «Сейчас»
отдаёт три (поле `roadmap`).
