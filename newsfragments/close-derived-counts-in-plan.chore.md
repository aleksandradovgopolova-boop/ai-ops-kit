Работа `derived-counts-are-computed-not-declared` перенесена из активного плана в историю как `done` (PR #269, счёт валидаторов выведен из факта). Половина названия про потолки закрыта осознанно, а не сделана: авто-вывод потолка убил бы запас; их дрейф ведёт отдельная работа `gate-measures-merge-result` — записано в результате.
