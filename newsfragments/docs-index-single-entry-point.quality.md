`docs/index.md` стал единой дверью в документацию: три ранее недостижимых документа вписаны в индекс, README ссылается на него как на вход. Находимость держит проверка `test_docs_index_coverage` — документ в `docs/`, до которого не дойти по ссылкам от индекса, краснеет (вместо отдельной вики, которая разъезжается с деревом молча).
