mypy включён и исполняется на фундаменте (F-05 внешнего аудита закрыт). Инструмент стоял в `[dev]` и не запускался нигде — тот же класс «объявлено, но не исполняется», что кит вычистил у ruff; новый инвентарь `enforcement-inventory` его подсветил. Замер решил спор «включить vs убрать»: обычный проход mypy нашёл 8 РЕАЛЬНЫХ проблем на `ai_ops_kit/shared`+`ai_ops_kit/kernel` — implicit-Optional в `usage_ledger.normalize`, рассогласование возврата с TypedDict `UsageRecord`, необъявленные переменные и widening `_CI_PATTERNS` до `object`. Все восемь починены; `[tool.mypy]` (область — фундамент, `follow_imports=silent`), шаг в джобе `lint` (`mypy==2.3.1`), запись в инвентаре → `enforced`. Полная аннотация всего пакета (`disallow_untyped_defs`, ~107 подписей) и `py.typed` отложены как ратчет-вверх — маркер «весь пакет типизирован» при 29% покрытия был бы над-заявлением.
