K6-глубина: фаза подготовки окружения (установка зависимостей стека + baseline-evidence + откат мутаций подготовки до правок модели) вынесена из god-функции `run_pipeline` в отдельную `_prepare_environment` — та же логика, но теперь читается и тестируется как самостоятельная фаза. Поведение сохранено (характеристические тесты зелёные до и после без правок).
