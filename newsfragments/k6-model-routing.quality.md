Из `run()` вынесен блок model-routing → `_resolve_models(ctx)` (K6-глубина): router→runtime резолв модели по роли, JIT provider-trust, complexity-aware strong-executor и provider-fallback теперь живут в отдельной фазе, мутирующей `RunContext`. Поведение сохранено под новой характеристикой openai-compatible пути (`test_ai_ops_run_model_routing.py`, ранее не покрытого ни одним тестом): writer≠judge по модели, self-model fallback, strong-executor и fail-closed blocked-preflight. func-size ратчет `run()` опущен 750 → 610 (глобальный максимум теперь `run_pipeline`, потолок 730).
