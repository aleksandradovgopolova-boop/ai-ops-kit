Продолжен разрез монолита `installer/ai_ops.py`: двенадцать тонких обработчиков вспомогательных подкоманд `ai-ops` (subsession, delivery-proof, usage, session, method, engops, audit, drift, ui-status, migrate, verify-capabilities, resolve-ref) вынесены в модуль-сателлит `installer/aux_commands.py`. Все они делегируют в подсистемы `ai_ops_kit` и вызываются только из диспетчера команд, поэтому переносятся замкнуто. Чистый рефакторинг: поведение байт-в-байт прежнее; в точке входа остался ленивый sibling-загрузчик, общие с ядром функции читаются через живое состояние установщика. Монолит ужат с 3258 до 2937 строк, потолок module-size снят до нового факта. Сателлит (379 строк) ниже порога 700.
