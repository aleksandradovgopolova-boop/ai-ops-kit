Из `run_pipeline` (`execution_pipeline.py`) вынесен блок изоляции → `_setup_isolation(...)` (K6-глубина): base-binding (форк рабочей ветки от разрешённого `--base`, не от HEAD), создание git-worktree, resume поверх подтверждённой работы и все fail-closed отказы (base-preflight, несохранённые коммиты, нарушение base-binding, worktree не создан) теперь отдельная фаза. Поведение сохранено под существующей характеристикой (worktree/discard/resume/base-preflight) и новым тестом ветви «несохранённые коммиты» (`TestUnsavedCommitsRefusal`, ранее не покрытой). func-size ратчет `run_pipeline` опущен 730 → 616 (глобальный максимум, потолок 616).
