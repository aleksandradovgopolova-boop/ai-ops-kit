K6-глубина: подсчёт стоимости прогона (агрегат токенов/задержек/денег из вызовов модели, запись в usage-ledger задачи и продукта, очистка call-context) вынесен из god-функции `run()` контроллера в отдельную `_finalize_run_cost`. `run()` уменьшилась с 910 до 866 строк; потолок размера функций опущен 910 → 866 (почти вровень с `run_pipeline`, 864). Поведение сохранено (характеристические тесты агрегата стоимости зелёные до и после без правок).
