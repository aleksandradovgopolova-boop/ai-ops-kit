Документация больше не учит снятому в 4.0 плоскому слою: примеры команд в WALKTHROUGH/QUICKSTART/dogfooding/qualification-runbook, карты `FILE_INDEX.md` и `docs/architecture/overview.md` и живые policy-доки переведены на пакетные точки входа (`python3 -m ai_ops_kit.<pkg>.<mod>`), заодно поправлены мёртвые ссылки на несуществующих агентов, счётчики и недостающие пакеты. Новый сторож `test_docs_no_removed_flat_entrypoints` ловит `tools/X.py` и корневой `validation/X.py` в живых доках — включая команды в fenced-блоках и корневые карты, которые прежние проверки не охватывали, — так что дочка, скопировав пример, больше не получит «No such file».
