Подготовка к снятию плоского слоя `tools/` (деприкация перед v4.0). Вход дочки и все внутренние ссылки кита переведены с `tools/<module>.py` на пакетные пути `ai_ops_kit/<pkg>/<module>.py`: обёртка дочки зовёт `ai_ops_kit/cli/ai_ops_cli.py` напрямую, `verification_tiers`/gate-runnables/mutation-probes/доки/скрипты — тоже. `tools/` пока на месте и работает (не ломающая правка); физический снос — в v4.0. Побочно исправлен реальный дефект: список критической инфраструктуры (`ALWAYS_FULL_PATTERNS`) указывал на shim-пути, из-за чего правка настоящего `execution_pipeline`/`orchestrator` могла не поднять полную проверку.
