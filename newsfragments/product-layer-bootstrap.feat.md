`ai-ops init`/`update` создаёт Product Operating Layer `.ai-ops/` в дочке (PR-3): `_seed_product_layer` в установщике читает СОСТАВ из реестра артефактов (PR-4), а не из хардкода, и кладёт `.ai-ops/{ROADMAP,DELIVERY}.md` + `POLICY.yaml` из официальных версионных шаблонов, ГЕНЕРИРУЕТ `PRODUCT_PASSPORT.md` из фактического состояния репозитория (PR-6) и синхронизирует `.ai-ops/templates/` копией версий кита для определения Outdated и миграций. Существующие файлы владельца не трогаются. Продуктовый слой теперь едет в дочку: реестр, загрузчик, генератор паспорта и шаблоны сняты с dev-only, потолок файлов поставки поднят 490→495 записью в `quality/delivery-budget.yaml` (7 названных файлов, они исполняются В дочке). Прогнано живьём: `ai-ops init` на чистом git-репо создаёт все 5 артефактов, каждый Valid, паспорт собран из фактов.
