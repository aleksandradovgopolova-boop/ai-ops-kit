В план кита заведена цель `kit-release-strategy` (todo, строить ПОСЛЕ stable — решение владельца
2026-08-31): кит собирает релизы осознанно (по вехе/расписанию), а не когда гейт вынудил, и владелец
продукта выбирает стратегию обновления из НАЗВАННОГО меню (канал × политика, сверх pr/manual —
scheduled-батч и auto-stable). Повод — наблюдение владельца во время владелец-ведомого прогона:
релиз 3.38.0 вышел реактивно (staleness-гейт cockpit вынудил смести накопленное одним куском, потому
что cockpit обновили с main вместо qualification-тега). Каналы (edge/qualification/stable) и ручки
дочки (update_channel × allowed_version_range × update_policy) уже есть — это дисциплина и меню поверх
них. Направление добавлено в ROADMAP.md → «Дальше». Попутно записан рассинхрон: тип коммита `release`
не пускают cz/PR-title гейты (обошли `chore(release):`) — сводится этой же работой.
