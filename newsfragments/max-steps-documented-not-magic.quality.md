`--max-steps` (потолок шагов tool-loop, дефолт 40) перестал быть немотивированной магической
цифрой: help CLI и комментарий у поля `max_steps` в `run_context.py` объясняют, что 40 — это
предохранитель от убежавшего писателя, а не рабочая точка (типовой расход единицы-десятки шагов;
«читающего по кругу» ловит анти-флейл в run_loop). Измеренного по task_type корпуса пока нет —
число названо задокументированным предохранителем, а не оптимумом (#602).
