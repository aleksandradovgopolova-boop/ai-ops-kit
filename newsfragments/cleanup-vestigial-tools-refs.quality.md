Кит больше не «говорит» о снятом в 4.0 плоском слое `tools/` там, где его нет. Вычищены мёртвые вставки `tools/` в `sys.path` (движок, orchestrator, оба `_bootstrap`, 48 тест-файлов — путь давал ещё до 4.0 `tests/conftest.py`, а импорты рядом пакетные), докстринг-примеры импортов приведены к пакетной форме (`from ai_ops_kit.shared.budget import …`), а комментарии и докстринг, утверждавшие, что «плоские алиасы остаются точками входа», переписаны под актуальную точку входа `python3 -m ai_ops_kit.<pkg>.<mod>`. Поведение не меняется — это чистка честности деклараций, не механизм.
