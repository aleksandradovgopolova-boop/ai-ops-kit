Две работы, чей механизм давно слит и которые ждут одного действия владельца (включить «Require merge queue» для main), помечены новым статусом `waiting_on_owner` с явной причиной в поле `waiting_on` вместо `in_progress` — план перестал показывать их как идущие. Статус завела схема #497.
