Ночной дельта-обзор теперь реально запускается по расписанию: его самогенерируемый CI-workflow чинит script-mode импорт (`PYTHONPATH` на каталог пакета) и ставит зависимости (`pyyaml`), права понижены до `contents: read`. Раньше расписания не было вовсе, а сгенерированный триггер упал бы красным на `import ai_ops_kit`. Добавлен ручной запуск (`workflow_dispatch`) — обзор можно форснуть, не дожидаясь ночи (#421).
