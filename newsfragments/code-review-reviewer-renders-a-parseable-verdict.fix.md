Гейт `code_review` больше не встаёт молча, когда независимый ревьюер вынес корректный вердикт, но
ответ был «грязным». Раньше живой путь ревьюера разбирал ответ жадно — от первой фигурной скобки до
последней; на ответе, где ревью цитирует код со скобкой или кладёт вердикт в fenced ```json,
разбор ломался, ВАЛИДНЫЙ вердикт молча терялся, и гейт не закрывался ни на какой правке (автономный
green был недостижим — полевой замер cockpit 8293fe54). Теперь терминальный вердикт достаётся тем же
надёжным разбором, что и офлайн-путь (кандидаты по одному, берётся последний reviewer-result),
промпт ревьюера ОБЯЗЫВАЕТ завершить машиночитаемым вердиктом («последний блок = вердикт»), фолбэк по
прозе понимает `needs_work` и берёт итоговую строку вердикта, а пустой ответ провайдера отличается
от непарсибельного — каждая причина названа, и ни одна не выдаётся за pass.
