Две находки внешнего аудита закрыты решениями «не делать» с обоснованием, а не оставлены висеть. F-07 (принять `ruff format`) ОТКЛОНЁН замером: `ruff format .` раздувает код (`execution_pipeline.run_pipeline` 184→319 строк, всего 823 файла), давая 24 нарушения module-size + 6 func-size — принять форматтер значило бы поднять 30 потолков размера и выпотрошить ратчеты против роста монолитов; аудит этого взаимодействия не учёл (`ep-2026-09-10-ruff-format-breaks-size-ratchets`). F-14 (перенести хронологию конфига в decisions) ОТКЛОНЁН: комментарии pyproject/workflows объясняют «почему правило такое» и служат ясности команды — аудит сам велел их сохранить, а миграция фиддли, на горячих файлах и с риском потерять контекст (`ep-2026-09-10-config-narrative-stays`). Обе работы — в history как `dropped`.
