Прополка завершена: сняты последние 8 монолитных `*_selftest`, чьё покрытие уже было перенесено в
гранулярные тесты, но которых держали внешние ссылки. Smoke-слой CI (`pr-smoke.yml`) переведён с
имён селфтестов на сами гранулярные файлы критичных модулей — то же покрытие, диагностируемое
пофункционально (заодно чинится уже осиротевший после снятия tool_broker `-k`-селектор). Селфтест
детектора убран из `DETECTOR_OWN_MATERIAL`, приёмочные ссылки research переведены на гранулярный
`test_research_tools.py` (он гоняет `--selftest` всех трёх инструментов параметризованно). Все 26
пар «монолит рядом с гранулярным» теперь единообразны: одно поведение — один именованный тест.
