K6-глубина: гейт продолжения прогона (resume-preflight — можно ли продолжить WorkItem поверх подтверждённой работы, или честный ранний выход при переписанной/сдвинутой базе и необходимости ревалидации) вынесен из god-функции `run()` контроллера в отдельную `_resume_gate`. `run()` уменьшилась с 957 до 910 строк; потолок размера функций опущен 957 → 910. Поведение сохранено (тесты resume-пути зелёные до и после без правок).
