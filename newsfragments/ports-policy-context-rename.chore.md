Разведена коллизия имени `RunContext` на стыке ядра: TypedDict контекста решения политики в `kernel/ports.py` переименован в `PolicyContext`, чтобы не путаться с каноническим dataclass `engine.run_context.RunContext` (источник истины прогона). Только имя типа — поля и структура не тронуты.
