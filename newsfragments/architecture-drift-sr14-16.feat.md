Инженерный стандарт SR-14..16 (срез 4): расхождение (drift) архитектуры дочки как отдельный РОД
находки. `ai_ops_kit/planning/drift.py` + блок `drift` в `schemas/governance-report.schema.json`:
расхождение (код и его описание разошлись, чинить нельзя — только показать) отличается от нарушения
СЛОВОМ состояния `drift`, а не severity (SR-14); меряется на ДИФФЕ, а не на дереве — переиспользует
`source_of_truth_behind` и молчит на унаследованном долге, которого PR не касался (SR-15). Снимок
архитектуры теперь СОХРАНЯЕТСЯ в область дочки (`.ai-ops/architecture-baseline.json`) видимым шагом
(`python -m ai_ops_kit.planning.drift snapshot .`), и `diff_baselines` есть с чем сравнивать (SR-16).
Проведён в governance-отчёт дочки (#605) отдельным advisory-блоком, без нового блокирующего гейта.
