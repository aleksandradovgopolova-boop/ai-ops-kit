Stable-канал не объявить, пока пять целей Product OS не достигнуты: `product_layer_ready`
добавлен в `channels.stable.requires` (ep-2026-08-20). Валидатор читает `planning/plan.yaml`
и краснеет, если хотя бы одна цель не `achieved`. «Канал заработан» и «продукт-как-ОС готов» —
один рубеж, проверяемый машиной, а не словами.
