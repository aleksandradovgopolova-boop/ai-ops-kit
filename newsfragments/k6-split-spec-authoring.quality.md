K6-глубина: сборка prompt-контекста реализации (prelude/resume/провалы базы) и pre-authoring по Spec-First вынесены из god-функции `run_pipeline` в `_assemble_context_and_author`. Порядок Spec-First (автор → валидация формы → реализация только при валидной спеке) сохранён; поведение неизменно (характеристические тесты зелёные до и после без правок).
