CI видит платформу владельца, а инструменты не выбирают что попало (F-10 внешнего аудита), три части. (1) **macOS**: разработка идёт на macOS, а CI её не проверял — заведена advisory-джоба `smoke-macos` (macos-latest, `critical_path`, `continue-on-error`, не в required-контекстах): наблюдает платформу владельца, но красный не держит PR — реальные отличия macOS/Linux (NFC/NFD, case-sensitivity) станут видны, а не спрячутся. (2) **Пол интерпретатора**: обёртка `./ai-ops` перебирала `python3.9` и выбрала бы его при `requires-python=">=3.12"`, падая позже и невнятно — перебор сведён к 3.13/3.12, и отбор теперь проверяет ВЕРСИЮ (`sys.version_info>=(3,12)`), а не только `import yaml`. (3) **NFC/NFD**: 13 кириллических имён файлов в `findings/from-children/` переименованы в ASCII (`obs-<дата>-<хэш>.yaml`), ссылки в plan/history обновлены.
