Доставка стала автономнее — сняты три разрыва, пойманные полем (owner-led прогон ii-sreda, 01.09.2026): (1) брошенная `session:*`-заявка теперь гасится по ВОЗРАСТУ (`holder_is_gone`: старше 12ч без finish = держать некому) — раньше вчерашняя session-заявка держала доставку сутки, снять её можно было только руками; (2) у `run` появился `--takeover` — перенять заявку на работу/ветку, не спускаясь в `active_work.py` (прежний держатель остаётся записан); (3) `print_human` больше не падает `KeyError('base_workflow')` на минимальном отчёте отказа — печатает id/статус/причину коротко.
