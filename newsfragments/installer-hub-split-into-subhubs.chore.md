Завершён разрез монолитов кита: последний многотысячник `installer/ai_ops.py` (2168 строк — критичный код, копируемый в каждую дочку) ужат до 349 строк. Общий код доставки и сверки вынесен в четыре когезивных под-хаба, каждый ниже порога 700: `installer/version_ops.py` (версии и каналы обновления), `installer/delivery_ops.py` (состав и бюджет поставки), `installer/managed_state.py` (целостность managed-слоя, провенанс, гейт отставания) и `installer/asset_ops.py` (доставка ассетов, отчёт, `validate`). Тонкий фасад-роутер `installer/core.py` маршрутизирует общие функции к нужному под-хабу — так монолит вынесен, но НЕ возрождён одним большим файлом. Чистый рефакторинг: поведение байт-в-байт прежнее, контракт CI дочки (`ai_ops.py check-update` из одинокой копии) сохранён. После разреза в ките не осталось ни одного файла ≥1000 строк.
