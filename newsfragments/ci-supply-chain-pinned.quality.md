Цепочка поставки CI закреплена (F-09 внешнего аудита). Тест-зависимости запинены по мажору во всех workflow'ах (`pytest==9.*`, `pytest-xdist==3.*`, `hypothesis==6.*`, `pytest-cov==7.*`, `pyyaml==6.*`) — мажорный релиз больше не покрасит CI на невиновном коммите (тот же довод, что уже был у ruff/towncrier/commitizen). Действия `actions/checkout@v4` и `actions/setup-python@v5` закреплены на commit SHA (OpenSSF Pinned-Dependencies) с человекочитаемым `# v4`/`# v5`. Заведён `.github/dependabot.yml` (github-actions + pip): двигает SHA и версии отдельными PR'ами на ревью, ничего не мержит само. Инвариант «рантайм кита = stdlib + pyyaml» не затронут — это фордж-конфигурация над dev-инструментами.
