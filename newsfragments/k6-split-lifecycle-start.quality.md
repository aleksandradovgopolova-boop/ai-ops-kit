K6-глубина: durable-старт жизненного цикла прогона (создание WorkItem, надёжная запись RunPlan и run-settings как барьеров, отметка run_start в журнале, снимок истории) вынесен из god-функции `run()` контроллера в отдельную `_start_lifecycle`. `run()` уменьшилась с 1003 до 957 строк; потолок размера функций опущен 1003 → 957. Заодно убрана мёртвая переменная `_sdump` (присваивалась, нигде не читалась). Поведение сохранено (тесты planned-пути и resume зелёные до и после без правок).
