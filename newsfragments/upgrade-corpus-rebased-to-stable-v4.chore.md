Корпус upgrade-тестов переведён на новую базовую линию после намеренной чистки истории/тегов на
stable (2026-09-08): `test_upgrade_from_released_versions` и `scripts/check-upgrade-path.sh` больше не
ссылаются на снесённые pre-4.0 теги, а берут ФАКТИЧЕСКИЕ релизные теги из git (сейчас — v4.0.0).
На новой линии новейший тег равен текущему номеру, поэтому обновление ПЕРЕ-ДОСТАВЛЯЕТ весь
managed-слой (равная версия — не понижение). Зубы сохранены: отдельная проверка стережёт, что корпус
релизных тегов НЕ ПУСТ (иначе параметризация молча ничего не гоняет). Специфичная проверка миграции
3.33->3.34 честно снята — её исходные теги снесены вместе с историей, имитировать её нельзя.
