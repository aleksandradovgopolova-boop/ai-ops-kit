Печать результата прогона вынесена из самого крупного модуля движка (`ai_ops_run.py`) в отдельный `ai_ops_run_print.py` — поведение не изменилось, но god-модуль стал на ~200 строк короче и легче в сопровождении.
