Монолит `ai_ops_kit/engine/ai_ops_run_lifecycle.py` разрезан на фасад + сателлит `ai_ops_run_lifecycle_stages.py` (пробо-свободные стадии прогона: commit-барьер, старт lifecycle, resume-гейт, агрегат стоимости, финализация статуса работы и транзакционная доставка за барьером). Чистый рефакторинг: публичная поверхность и поведение сохранены (фасад ре-экспортирует все перенесённые имена, `ai_ops_run_lifecycle.X` работает как раньше), мутационные пробы остались в фасаде вместе с пробируемыми функциями, модуль снят с потолка module-size (952 → 491 строка).
