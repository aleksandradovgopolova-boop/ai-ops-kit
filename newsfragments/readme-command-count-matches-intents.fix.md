README больше не расходится с реальным числом команд владельца: писал «32 команды», а фактически (реестр INTENTS) и в capability-map — 34 (P1 №11 продуктового ревью). Число исправлено и помечено машинно-проверяемым маркером `claim:commands-total`; тест-замок сверяет README ↔ INTENTS ↔ capability-map, чтобы расхождение больше не появилось незаметно.
