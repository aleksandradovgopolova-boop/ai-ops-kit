Самопроверка кита `ai_ops_kit/validation/validate_own_medicine.py` (795 строк) разрезана на фасад
и сателлит-фундамент `ai_ops_kit/validation/own_medicine_checks.py` без изменения поведения
(эталонный `--json` байт-в-байт совпал до и после). В сателлит уехали константы исходов, `PKG`,
`LIMITATIONS`, разбор кода доставки (`delivery_steps`/`init_only_calls`/`_cmd_init_fn`/
`_installer_module`), git-помощники и 12 проверок по шагам `deliver_assets` с реестром
`DELIVERY_CHECKS`; фасад оставил проверки установочных/строчных/реестровых шагов, `evaluate` и
печать. Направление импортов только фасад -> сателлит; двухконтекстный `_bootstrap`-блок сохраняет
запуск валидатора как модулем и как отдельным файлом под `-S`. Чистый рефактор ради потолка размера.
