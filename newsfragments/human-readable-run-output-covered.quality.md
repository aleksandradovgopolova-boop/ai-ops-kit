Человеко-читаемый вывод прогона теперь проверяется по фактическому печатаемому тексту, а не по факту запуска (`tests/unit/test_run_output_human_readable.py`, #437): `_print_pipeline` / `print_human` / `_print_contour_consistency` и `presenter.from_*`. Отдельная регресс-проба на минимальный отказной отчёт БЕЗ `base_workflow` — та форма, что исторически роняла `KeyError('base_workflow')`, — теперь краснеет при удалении стража. Покрыты формы: READY_FOR_PR, NOT_READY, error, «критерии приёмки НЕ сверялись», «сверены: НЕ ВЫПОЛНЕНО», «критериев не было объявлено», непроверенные области контуров.
