Результат прогона (`ai-ops do`/`run --execute`) на аудитории product теперь читается человеком: вместо технической стены (WorkItem/base_workflow/tool-loop/SHA/evidence/гейты/context-токены/spec-level) кит печатает СВОДКУ — готово/не готово, сколько файлов изменено, честные оговорки («критериев приёмки не было — «готово» ≠ «сверено с ожиданием»; тестов в стеке нет — проверка пропущена; прогон без песочницы»), и следующий шаг (открыть PR). Технический разбор — «по запросу», как велит communication-policy (technical_details: on_request). Найдено вторым живым прогоном задачи. Оговорки сохранены целиком: «готово» не выдаётся за «проверено».
