Живой прогон 06.09.2026 (cockpit на ките 4.x, фича free-tile-counter, provider claude-cli) завёл три работы в `planning/plan.yaml`: `code-review-reviewer-renders-a-parseable-verdict` (ревьюер не рождает вердикт → гейт не закрыть; P0 живой на 4.x), `forced-major-update-reconciles-allowed-version-range` (после `update --force` через мажор версия вне своего диапазона, doctor молчит), `level-signals-carry-across-specify-plan-run` (task_type не переносится между шагами → тихий QUICK). Каждая с полевым evidence и критерием готовности.
