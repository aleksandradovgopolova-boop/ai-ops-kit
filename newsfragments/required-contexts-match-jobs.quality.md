Обязательные статусы защиты ветки сверяются с реальными CI-джобами: добавлен валидатор `validate_required_contexts.py`, декларация `quality/required-contexts.yaml` и 6 контрактных тестов. Рассинхрон «объявленный контекст ↔ существующая джоба» ловится в CI до того, как повесит очередь.
