Доставка надёжнее в двух местах. (1) Свою delivery-ветку при расхождении с remote
(non-fast-forward — старый PR на старой базе) кит теперь пере-пушит `--force-with-lease` вместо
падения сырым rc=1 (#401). (2) Токен для доставки при отсутствии GITHUB_TOKEN/GH_TOKEN берётся из
`gh auth token` — раньше доставка молча деградировала до «нет токена», хотя gh на машине
авторизован (#402).
