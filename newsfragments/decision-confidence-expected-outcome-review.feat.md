Решения в журнале теперь могут нести уверенность (`confidence`), ожидаемый исход
(`expected_outcome` — метрика, где сейчас и куда идём) и дату сверки (`review_at`).
Это позволяет позже проверить, сбылось ли ожидание. Поля необязательны: старые
решения без них остаются валидными.
