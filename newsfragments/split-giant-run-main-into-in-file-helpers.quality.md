Гиганты-функции прогона и CLI расщеплены на внутренние помощники без смены поведения: `run()`
(движок) и `main()` в `ai_ops_run.py`/`ai_ops_cli.py` теперь оркестраторы, а базовое разрешение,
подбор предложителей, preflight, controller-путь, сборка argparse и путь `run --execute/do`
вынесены в приватные функции тех же файлов. Читать проще, поведение и мутационные пробы прежние;
потолок func-size для `cli/` опущен 348 → 156 (ратчет ходит вниз).
