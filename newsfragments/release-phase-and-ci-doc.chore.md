Честность метаданных релиза + канонический CI-док. (1) Из `registry/release-claims.yaml` убрано поле `phase`: свободная проза, дублировавшая заголовок раздела CHANGELOG, которую `release_bump` не трогал (он бампает число версии и пишет заголовок из `--title`), а `validate_release_claims` не проверял — поэтому `phase` молча застряло на «v3.39.2» при `version: 4.0.0`. Ни код, ни валидатор, ни схема поле не читали; заявление о содержимом релиза остаётся в одном месте (заголовок CHANGELOG + `patch_note` в release-notes.yaml). (2) `docs/guides/ci.md` переписан в канонический аудируемый документ: таблица обязательных статус-проверок защиты main (источник — `quality/required-contexts.yaml`, сверяется `validate_required_contexts`), с явной пометкой, что `clean-install`/`upgrade-path`/`pipeline-e2e`/`security-scan` защищают дочку; advisory-гейты `parallel-safety`/`pr-size`; почему `quality` не гонится на draft-PR; где живёт nightly. Прежний текст был устаревшим (описывал матрицу из 7 групп вместо реальных fast/selftests-a/selftests-m/contracts) и не содержал списка required-checks.
