Из `run_pipeline` (`execution_pipeline.py`) вынесены ready-критерии уровня спеки → `_assess_readiness(...)` (K6-глубина): spec-depth enforcement (незакрытые разделы уровня, мапящиеся на unmet-гейты), Real Spec-First (реальный неполный spec.yaml блокирует), сверка критериев приёмки независимым судьёй (B2-14) и context-budget overflow теперь отдельная фаза. Поведение сохранено (296/296 тестов `test_execution_pipeline.py`, тесты не правились; проба `acceptance-not-gated-by-review-flag` переехала в фазу — мутант по-прежнему убивается). Этим расщепление `run_pipeline` завершено: за сессию 730 → 401 (глобальный func-size максимум, потолок 401).
