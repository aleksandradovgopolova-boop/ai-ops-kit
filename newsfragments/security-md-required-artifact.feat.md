`SECURITY.md` теперь **обязательный артефакт** каждого репозитория под управлением кита (статья `SEC-001` Архитектурной конституции): кит сидирует черновик при `init`/`update`, проверяет наличие в `doctor` и own-medicine, а версия стандарта поднята до 6. Сам кит догфудит правило — получил корневой `SECURITY.md`.
