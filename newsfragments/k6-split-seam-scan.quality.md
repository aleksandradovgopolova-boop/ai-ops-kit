K6-глубина: advisory-детектор «дефекта шва» по диффу base..committed вынесен из god-функции `run_pipeline` в `_seam_scan_advisory`. Проверка остаётся non-blocking advisory; поведение сохранено (характеристические тесты зелёные до и после без правок).
