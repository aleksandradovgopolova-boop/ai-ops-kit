Промпты ревьюера и автора обёрнуты в XML-якори (`<role>`, `<task>`, `<criteria>`, `<diff>`,
`<rules>`, `<output_format>`) вместо маркеров `=== ЗАДАЧА ===`. Направление подтверждено
рекомендацией Anthropic (Claude обучен на XML-теги); конкретные цифры статьи не воспроизводимы,
но структурная разметка входа — стандарт для длинного контекста. Границы: JSON-схемы на диске
и формат вывода не тронуты.
