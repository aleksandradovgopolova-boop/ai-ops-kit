Из `run_pipeline` (`execution_pipeline.py`) вынесена сборка evidence → `_assemble_evidence(...)` (K6-глубина): реальный прогон проверок профиля на зафиксированном SHA, intake/regression/authored/reevaluate-seed, освобождения по неприменимым проверкам, UI-evidence на точном SHA, seam-scan advisory, независимые ревью и доменный security-вердикт теперь отдельная фаза, возвращающая всё нужное для гейтов и отчёта. Поведение сохранено (296/296 тестов `test_execution_pipeline.py`, тесты не правились). func-size ратчет `run_pipeline` опущен 616 → 500 (глобальный максимум, потолок 500).
