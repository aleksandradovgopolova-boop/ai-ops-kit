K6-глубина: компиляция артефактов контекста (bundle/payload/hybrid/spec/work-package) вынесена из god-функции `run()` контроллера в отдельную `_compile_context_artifacts` — та же логика, но теперь это самостоятельная, отдельно тестируемая фаза. `run()` уменьшилась с 1275 до 1182 строк. Это первый срез K6-глубины, реально опустивший потолок размера функций: 1275 → 1182. Поведение сохранено (характеристические тесты фазы контекста, включая context-hybrid, зелёные до и после без правок).
