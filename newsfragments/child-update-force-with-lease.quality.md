Шаблон автообновления дочки (`templates/ci/ai-ops-update.yml`) использует `git push --force-with-lease` вместо `git push -f` — безопасный push не перетирает чужие коммиты на ветке. Permissions задекларированы явно с пояснением, зачем каждый нужен.
