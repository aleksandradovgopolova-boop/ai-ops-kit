Из `run()` вынесен блок исполнения движком и fix-loop → `_execute_with_fix_loop(ctx, …)` (K6-глубина): построение `_pipe`, итерация по блокерам ревью/проверок поверх той же ветки, WRITER QUALITY-ESCALATION по escalation_ladder, типизированный error-отчёт при сбое провайдера и re-raise на прерывании теперь живут в отдельной фазе, читающей/мутирующей `RunContext`. Поведение сохранено под характеристикой happy-path (`fixloop_run`), провала провайдера (`test_provider_exception_returns_error_report`) и новой характеристикой quality-эскалации (`test_ai_ops_run_fix_loop.py`, ранее не покрытой). func-size ратчет `run()` опущен 610 → 458 (глобальный максимум остаётся `run_pipeline`, потолок 730).
