Крупнейший модуль движка `ai_ops_run.py` разрежён с 1073 до 411 строк: проб-свободные хелперы прогона (fix-loop с quality-эскалацией, парсер команд run/resume, резолв провайдера и базы, сбор proposers, preflight, код возврата) вынесены в новый спутник `ai_ops_run_exec.py`. Поведение не изменилось — чистый перенос с ре-экспортом, пробируемые точки (`main`, `_run_controller_path`) и вход `run` остались на месте.
