Доводка вычистки снятого слоя: после удаления `tools/` (4.0) и переезда валидаторов в `ai_ops_kit/validation/` (v3.34) в движке оставались МЁРТВЫЕ вставки в `sys.path`, ведущие в несуществующие каталоги верхнего уровня `tools/` и `validation/`. Убраны целиком — вместе с осиротевшими `import sys`/`from pathlib import Path`, устаревшими `# noqa: E402` (E402 в наборе кита не включён) и комментарием, обещавшим «плоские импорты ниже», которых нет. Затронуты `engine/pipeline_failure|readiness|helpers|evidence|acceptance_verify.py` и `engine/run_plan.py`. Поведение не меняется (несуществующий путь Python и так игнорировал) — код перестал ссылаться на снятые каталоги.
