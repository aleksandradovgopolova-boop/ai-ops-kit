Команда `./ai-ops session` в подключённом репозитории делает работу, а не показывает заглушку.
Интент был объявлен и обработчик написан, но имя не внесли в список исполняемых интентов — команда
печатала «Выполню намерение» и возвращала 0. Точка входа дочки вдобавок слала `session` в
установщик, которого в поставке нет. Заодно снят вызов чужого валидатора: каждый запуск печатал
«kind должен быть CompletionRitual» про артефакт, которого не производил.
