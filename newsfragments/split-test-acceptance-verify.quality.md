Поведение снова адресуемо: четвёртый мега-тест-файл (`test_acceptance_verify.py`, 847 строк) разрезан по темам тем же behavior-preserving приёмом, что и #438/#464. Вердикт `verify()` остался в `test_acceptance_verify.py`, разбор критериев (`parse_criteria` / `criteria_from_spec`) выделен в `test_acceptance_criteria_parsing.py`, заземление цитат (`_ground_quote` / `_post_state` / `_diff_by_file`) — в `test_acceptance_verify_grounding.py`. Общие хелперы понадобились только ядру и остались в нём инлайном (conftest не тронут). Чистая реорганизация: ни один тест не потерян и не изменён, число собранных тестов ДО == ПОСЛЕ (53), доля поведенческих тестов выросла 85.44% -> 85.51% (структурных 61 без изменений).
