Закрыта вторая половина R-43 — покрытие. Пост-фактум сторож защищённых путей теперь видит и
ИГНОРИРУЕМЫЕ файлы внутри protected-каталогов (`production/`, `security/`, пути под `.ai/`): запись
в них больше не проходит молча. Скан сужен до защищённых префиксов намеренно — снимать игнор всего
дерева на каждой shell-операции значило бы откатывать законную суету (`__pycache__`, `node_modules`,
артефакты сборки). Новый игнор-файл в защищённом пути удаляется; правка существующего игнор-секрета
на месте ОБНАРУЖИВАЕТСЯ и честно докладывается как «откатить не смогли» (прежнего содержимого нет —
файл не под git), а не выдаётся за успешный откат. Вместе с честным отчётом об откате (#797) R-43
закрыт с обеих сторон.
