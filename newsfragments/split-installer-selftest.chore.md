Продолжен разрез монолита `installer/ai_ops.py`: offline self-test установщика (`ai-ops selftest` — сценарий транзакционного отката установки) вынесен в модуль-сателлит `installer/selftest_ops.py`. Чистый рефакторинг: поведение байт-в-байт прежнее; временную подмену глобалов установщика self-test пишет через живое состояние установщика. Монолит ужат с 2601 до 2454 строк, потолок module-size снят до нового факта; сателлит (199 строк) ниже порога 700.
