K6-глубина: фаза коммита (фиксация работы на ветке `ai-ops/<wid>` до сбора evidence, чтобы evidence бился о точный SHA; переиспользование HEAD при reevaluate) вынесена из god-функции `run_pipeline` в `_commit_work`. Поведение сохранено (характеристические тесты, включая committed-путь, зелёные до и после без правок).
