Синхронизация плана с реальностью (сверка issue ↔ план, 02.09.2026):

- work `parallel-safety-allows-kit-own-update-prs`: `todo → in_progress` — код фикса #384 влит (PR #387),
  issue #384 закрыт; до проверки полем (заезд на голый main без ручного git rm) не `achieved`.
- Заведены пять новых work-элементов по открытым заявкам поля: #404 (прогон не валит свой
  status-freshness гейт), #405 (детерминированный вердикт writer'а), #406 (resume ругается на флаги
  старых версий + гашение заявки мёртвой сессии по liveness), #160 (внятный отказ при структурно
  недоступном claude-cli вместо RuntimeError), #148 (правки plan.yaml не конфликтуют параллельно —
  report-history уже закрыт merge=union, остаётся структурный plan.yaml).

Проверкой по коду подтверждено: #150 (трассируемость) и #151 (когда сливать/выкатывать) во многом
покрыты отгруженными механизмами (claim видим другим машинам, next исключает чужую работу,
green-means-checked) — рекомендованы к закрытию отдельно.
