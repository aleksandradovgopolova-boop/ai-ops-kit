handoff-вердикт закрывает code_review через штатный resume/reevaluate, а не только в юните.
Записанный оркестратором вердикт-артефакт теперь ПОТРЕБЛЯЕТСЯ реальным потоком без `--review`
(artifact-first жил только внутри провайдерного `_run_reviews`); пустой/структурно-немой ответ
провайдера (env-unavailable И empty_answer) на блокирующем гейте ведёт в awaiting_reviewer с
записанным запросом на ревью; `--reevaluate-only` поднят в CLI-обёртку `./ai-ops run` (уровень —
из сохранённых на specify сигналов, ENGINEERING не становится молча QUICK). Инвариант 0 false-green
и writer≠judge сохранены: устаревший-SHA / писательский / незаземлённый вердикт гейт не закрывает.
