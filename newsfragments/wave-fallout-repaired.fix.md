Осадок волн Phase B/W3 убран: 13 красных тестов main зелёные. Три пропущенных вызова
`evidence_collector.collect` получили брокера (K1 доводка — workpackage_executor ×2, parallel_live ×2);
`kernel/ports.py` честно объявлен неподключённым (UNWIRED: реализации портам ещё не соответствуют);
подписчик `session_events` регистрируется явно на пути `run` (видим и рантайму, и статическому
обходчику); пакет `kernel` объявлен в public-surface; ратчет func-size опущен 1285→1275 под факт K6;
тест ратчета слоёв научился миру с нулевым долгом (впрыск обратного ребра вместо снятия несуществующего);
подъёмы поставки записаны в delivery-budget (526 файлов, 4.34 МБ — шина событий W3); стале-пути
в REFACTORING_MAP/kernel-extraction поправлены. Плюс сверка плана: 21 закрытая работа Phase 0+B
переехала в history с названными результатами, `ai-ops next` снова отвечает.
