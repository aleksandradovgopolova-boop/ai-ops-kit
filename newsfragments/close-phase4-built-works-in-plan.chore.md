Четыре работы Phase 4 (`drift-detected-between-artifacts`, `ai-decisions-logged-with-rationale`,
`policy-engine-suggest-to-approval`, `human-override-as-signal`) перенесены из активного плана в
историю: их код давно построен и влит (PR #229/#285) с проходящими тестами, но работы висели как
открытые. У каждой исход соответствующей цели остаётся `false` — он требует живого прогона (реальный
дрейф/решение на дочке), а не кода. Бухгалтерский шаг: активный план 15 → 11 работ; устаревшая ветка
`lane-5-work` (её код уже в main) удалена.
