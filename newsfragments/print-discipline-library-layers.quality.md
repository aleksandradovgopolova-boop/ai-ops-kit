Заведён ратчет дисциплины вывода: `print` в библиотечных пакетах `ai_ops_kit` заморожен вниз (F-08 внешнего аудита). `print()` в точке входа (слой `entrypoints` в `layering.yaml`: cli/devtools/validation) — её работа; но библиотечный пакет, печатающий в stdout, нельзя переиспользовать из MCP/web без перехвата, и у дочки нет ручки «покажи подробнее». `validate_print_discipline` (AST-счёт вызовов print на пакет) заморозил 690 библиотечных print в 14 пакетах на `packages/print-discipline-baseline.yaml`; ратчет ходит только вниз — новый print в библиотечном слое краснеет, перевод `print` → возврат данных/`logging` опускает потолок. Аллоу-лист берётся из слоя entrypoints, а не хардкодится. Новый baseline потребовал сам инвентарь «объявлено→исполняется» — механизм сработал.
