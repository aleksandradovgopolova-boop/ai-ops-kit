Путь доставки приведён к одному каноническому по умолчанию: `engine=pipeline` теперь дефолт и в
`run()`, и в argparse (прежде «сырой» дефолт был `controller`, а пользовательский CLI уже
форсировал `pipeline` — расхождение убрано). `controller` (оркестрация именованных агентов)
остаётся доступен явной альтернативой `--engine controller`.

Закрыта ложная зрелость из аудита: README утверждал «движок вызывает по имени 10» агентов — неверно
для обоих путей (пайплайн зовёт 0 по имени, резолвя роли в модели; оркестратор — переменное число по
workflow). Заявление приведено к факту, и согласованность теперь ПРОВЕРЯЕТСЯ новым контракт-тестом
`test_execution_path_reconciled`: канонический дефолт = pipeline; пайплайн не загружает персон по
имени; именованных агентов зовёт оркестратор; README не утверждает фиксированного «по имени N».
