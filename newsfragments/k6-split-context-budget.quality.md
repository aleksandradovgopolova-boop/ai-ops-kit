K6-глубина: проверка переполнения context-budget (fail-closed: ошибка сборки bundle = блокируем ready) вынесена из god-функции `run_pipeline` в `_context_budget_overflow`. Поведение сохранено (характеристические тесты зелёные до и после без правок).
