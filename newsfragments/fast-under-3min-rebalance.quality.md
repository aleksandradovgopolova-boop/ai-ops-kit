Стена CI `package-quality` снижена: тяжёлые интеграционные/мета-тесты (`test_local_check_mirrors_ci`, `test_evidence_scope`, `test_workpackage_executor`, `test_workpackage_resume`) перенесены из `fast` в `slow` и маршрутизированы в простаивавший `selftests-a` — тесты сохранены (гоняются на каждом PR, просто в другой джобе), имена джоб не менялись (branch protection не сиротеет). Цель — fast <3 мин без капкана статусов (#465).
