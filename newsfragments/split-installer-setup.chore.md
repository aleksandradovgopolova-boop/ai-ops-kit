Продолжен разрез монолита `installer/ai_ops.py`: команды установки `init` и `setup` (единая установка init→onboard→bootstrap→model) вынесены в модуль-сателлит `installer/setup_ops.py`. Чистый рефакторинг: поведение байт-в-байт прежнее; общие с ядром функции читаются через живое состояние установщика, самопроверка own-medicine научена находить `cmd_init` в сателлите. Монолит ужат до нового факта, потолок module-size снят; сателлит ниже порога 700.
