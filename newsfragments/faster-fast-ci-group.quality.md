Быстрее обратная связь на PR: самый долгий тест-файл группы `fast` больше не держит всю проверку в одном потоке. Два тяжёлых файла (сценарии resume и execute-sequence для workpackage-исполнителя) механически разрезаны по темам, и параллельный прогонщик теперь раскладывает их по разным ядрам — стена ожидания на PR ниже, поведение тестов не изменилось.
