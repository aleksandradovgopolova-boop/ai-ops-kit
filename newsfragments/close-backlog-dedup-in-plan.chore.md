Работа `backlog-deduplication` перенесена из активного плана в историю с результатом и местом
перепроверки (тест-доказательство — в PR #350: детерминированный тест на репо с реальными дублями).
Исход цели `duplicates_detected_and_merged` осознанно остаётся `false` — «слияние» требует живого
решения человека, кодом не флипается. Бухгалтерский шаг.
