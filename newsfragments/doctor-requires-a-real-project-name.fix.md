`doctor` больше не зовёт ставить задачу, пока в `.ai-ops.yaml` стоят заготовки установки. Кит просил
заменить `project.name: <project-name>` и не проверял, сделано ли это: в живом продукте заготовка
простояла пять дней при зелёном «Всё в порядке».

Проверяется ровно одно — значение больше не равно тому, что положил установщик. Осмысленность имени
не проверяется, описания с угловыми скобками внутри текста не краснеют, а код возврата `doctor` не
изменился: это замечание, а не «работать нельзя».
