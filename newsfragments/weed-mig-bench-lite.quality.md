Прополка: 25 агрегатных инвариантов прогона `run_bench()` (bench_lite) перенесены из монолитного
`test_bench_lite_selftest.py` в гранулярные тесты `test_bench_lite.py` — одно поведение = один тест,
через общую module-scoped фикстуру с единственным прогоном корпуса. Монолит снят.
