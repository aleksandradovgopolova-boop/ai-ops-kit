`ai-ops resume --deliver-only` доставляет уже готовый READY-коммит без перезапуска писателя.
Раньше resume всегда переавторил и при сбое доставки после READY плодил новые evidence-коммиты
(ветка обрастала дублями, evidence и remote дрейфовали). Флаг переиспользует существующий режим
reevaluate-only (перепроверка HEAD без нового коммита), затем обычную доставку (#403).
