Свод плана под закрытый исход 3 работы `the-plan-tells-the-truth-about-the-run`. Работа выполнена
полностью (исходы 1+2 — PR #838, исход 3 — PR #851: `specify` раскрывает уровень и разделы эскалации
формы ДО заполнения) и перенесена в `history/plan-history.yaml`. Направление
`owner-speaks-product-not-pipeline` остаётся активным: механизм доведён, а единственный оставшийся
исход `owner_never_edits_yaml_or_knows_feature_id` доказывается не кодом, а живым замером — заведена
работа `owner-speaks-proven-by-zero-touch-cockpit-run` (`waiting_on_owner`: измеряемый zero-touch
прогон на ai-ops-cockpit), родственная остатку `parallel-safety-allows-kit-own-update-prs`.
