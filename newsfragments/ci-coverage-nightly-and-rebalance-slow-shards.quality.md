CI на пути PR облегчён по замеру: покрытие (держало стену ~10.8 мин и за окно прогонов не ловило падений) снято с каждого PR в отдельный ночной прогон (`nightly-coverage.yml`: ночь + push в main + вручную), порог-ратчет 70/71% сохранён; слои слоу-тестов перебалансированы (граница a‑l|m‑z → a‑h|i‑z, партиция проверена collect-only). `coverage` убран из `quality/required-contexts.yaml` синхронно со снятием его из обязательных проверок branch protection — рассинхрон «объявлен, но джобы нет» исключён.
