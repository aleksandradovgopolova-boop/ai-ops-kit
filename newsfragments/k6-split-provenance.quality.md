K6-глубина: заполнение отчёта прогона provenance-полями (runtime / провайдер / модель / base-binding / lifecycle-артефакты) вынесено из god-функции `run()` контроллера в отдельную `_enrich_run_report`. `run()` уменьшилась с 1129 до 1084 строк; потолок размера функций опущен 1129 → 1084. Поведение сохранено (характеристические тесты provenance-полей зелёные до и после без правок).
