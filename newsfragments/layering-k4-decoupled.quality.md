Слои (K4): пара engops↔gates развязана по существу. Модуль оценки окружения переехал из
engops в checks (примитивы, ниже gates), а проверка готовности к деплою импортирует его
статически вместо динамического `__import__`. Ребро engops→gates снято, список известных
исключений опустел, взаимных пар слоёв стало 3 из 5 (циклов длиннее двух — 5). Это перенос,
а не сокрытие: модуль живёт там, где ему место.
