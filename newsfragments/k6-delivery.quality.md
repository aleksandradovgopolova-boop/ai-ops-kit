Из `run()` вынесен блок доставки → `_deliver(ctx, rep, …)` (K6-глубина): governance-gate и транзакционная доставка за commit-барьером (DeliveryIntent → идемпотентное внешнее действие → DeliveryReceipt, ветви outcome_unknown и fail-closed) теперь отдельная фаза, мутирующая `rep`. Поведение сохранено под новой характеристикой (`test_ai_ops_run_delivery.py`, ранее не покрытой: путь `open_pr`+ready с мокнутым `_deliver_pr`) — delivered, outcome_unknown, governance-observe. Этим разобраны все четыре оставшихся крупных блока `run()`: за сессию `run()` опущен 812 → 315 и больше не держит func-size потолок (глобальный максимум — `run_pipeline`, 730).
