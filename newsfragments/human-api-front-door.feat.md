Человеческая «передняя дверь» CLI (#675): `ai-ops` без аргументов и `ai-ops help` теперь показывают короткий понятный набор команд владельца простыми словами (onboard/discuss/do/status/next/explain/inbox/review/products/readout), а не argparse-стену из 36 интентов и 30 флагов. Человек, не знающий внутренней механики кита, понимает, что попросить, с первого экрана; полный список рабочей механики — `ai-ops help --all`. Это витрина, а не ограничение: любой интент вызывается напрямую как раньше. Набор двери мал намеренно и проверяется тестом, чтобы не разрастись обратно в стену.
