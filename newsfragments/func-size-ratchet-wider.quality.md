Ратчет размера функций (`validate_func_size`) расширен с одного каталога `engine/` на пять: теперь у каждого из `engine/`, `cli/`, `planning/`, `providers/`, `validation/` свой потолок в `packages/func-size-baseline.yaml` (секция `scopes`). Раньше god-функции ВНЕ `engine/` (`cli/main` ~348 строк, `validation/run_scenarios` ~207, `planning/validate` ~188, `providers/run_workflow` ~170) росли свободно — ратчет давал ложное чувство защиты. Потолки заморожены на ТЕКУЩЕМ максимуме каждого каталога: существующий код не ломается, но НОВАЯ god-функция теперь краснеет везде, а не только в движке. Поведение `engine/` сохранено ровно (потолок 399). Регресс-тесты (`test_validate_func_size.py::TestWiderScopeCatchesGodFunctions`) доказывают, что превышение в любом из новых каталогов ловится.
