Детектор дедупа backlog доказан на репо с реальными дублями: в одном отчёте он находит пару
дубликатов, помечает устаревшую задачу и предлагает слить — никогда не сливает молча
(`action == "suggest_merge"`, объединение требует одобрения человека). Проба детерминирована:
фиксированная опорная дата, без обращения к часам и живому GitHub.
