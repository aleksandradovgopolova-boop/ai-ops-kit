Регрессия к монолитным `*_selftest` теперь ловится контрактом `tests/contracts/test_no_monolith_funnel.py`:
он падает, если тест-файл носит ретированный суффикс `_selftest.py` ИЛИ тест-функция сводит ≥5
проверок через soft-помощник-накопитель (`nonlocal`/`list.append`/`print`, вызванный с булевым
условием) в один финальный assert. Помощник, который САМ ассёртит, разрешён — он гранулярен по сути.
Конвенция в AGENTS.md обновлена: тест модуля — гранулярный `tests/unit/test_<module>.py`, одно
поведение = один именованный тест. Сама прополка 128 монолитов завершена ранее (unit-weeding,
0 `*_selftest.py` в наборе); эта работа страхует, что форма не вернётся, — с fail-closed
доказательствами, что у детектора есть зубы.
