ROADMAP и план сведены к нулю предупреждений `roadmap.check(plan=)` — кит перестал нарушать своё же правило «цель под «Сейчас» без работы и работа без направления — расхождения». Факт-правки: `plan-as-control-plane` → achieved (все четыре исхода true), `kit-release-strategy` → «Следующий результат» (работы уже идут); `qualification-closeout` получил работу на единственный открытый исход (повтор owner-led verified PR на второй дочке). Взяты в работу под «Сейчас»: `uiux-standard-as-product` волны 2–4 цепочкой зависимостей строго по порядку (#418 → #419 → #420) и `storybook-as-visual-contract` (#415, #416). Сдвинуты в «Дальше» и приостановлены (`status: paused`, бэклог заведён): `product-decision-loop` (#417), `nightly-product-review` (#421–#423), `outcome-and-analytics-loop` (#424).
