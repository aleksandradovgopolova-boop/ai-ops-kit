Storybook проведён в живой контур (#613): `storybook_query` перестал быть built-not-wired —
у него появился живой потребитель. При UI-изменении (`ui_changed`) `context_compiler` вкладывает
в контекст пишущего агента навигацию по дизайн-системе дочки: каталог компонентов и stories,
СВЯЗАННЫЕ с затронутыми файлами, — чтобы агент переиспользовал существующие компоненты/варианты/a11y,
а не изобретал заново. Связывание починено под реальную структуру (живой прогон на ии-среде: 47
компонентов, 114 stories): матч по имени компонента в title story (сгруппированные
`foundations.stories.tsx`) и по стему в соседнем файле, а не только строгий суффикс пути. Строгий
evidence-матчер гейта не тронут; MCP-сервер как транспорт по-прежнему сознательно отложен.
