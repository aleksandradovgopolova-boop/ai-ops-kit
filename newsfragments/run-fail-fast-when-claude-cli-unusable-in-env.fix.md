`run --execute` теперь сразу объясняет, что делать, если исполнитель `claude-cli` в этой среде запуститься не может (типичный случай — запуск изнутри уже открытой сессии Claude: вложенный вызов обрывается, не дойдя до модели). Прежде кит делал пять бессмысленных повторов и падал общим трейсбеком; теперь этот структурный отказ распознаётся по сигнатуре (`api_error` + ноль токенов + `duration_api_ms:0`), повторов ноль, а человек получает последствие и оба выхода: запустить из обычного терминала вне сессии Claude либо указать другого исполнителя с ключом через `--provider`. Повтор настоящих транзиентных сбоев (529/429/5xx/таймауты) не затронут (#160).
