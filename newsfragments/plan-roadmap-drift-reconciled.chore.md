Дрейф план↔roadmap сведён правдой (`roadmap check`: 5 предупреждений → 0). `nightly-product-review`
→ paused (механизм доставлен и отработал живьём, активной работы под остатком #423 нет).
`product-decision-loop` получила работу на проводку гейта в обязательный контур. `green-means-checked`
(инфра-исходы met, но остаются поле-дефекты #405/#404), `checks-that-run` (первоначальные исходы
верны, но внешний аудит #721 завёл бэклог из 9 находок) и `layering-ring-is-a-dag` (гейт слоёв #716)
выведены в ROADMAP как активные цели с открытой работой — где им и место.
