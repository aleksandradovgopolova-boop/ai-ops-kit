Команды установщика проверяются машиной. Девять из них — включая `doctor`, `validate`, `migrate`,
`usage`, `audit` — не упоминались в тестах ни разу, а сам файл покрыт был на 46%: это самый важный
файл продукта, через него кит попадает в каждый подключённый репозиторий, и в нём уже жили пять
точек входа, падавших при любом запуске. Крупнейшим непроверенным местом оказался сценарий
транзакционного отката неудачного обновления — то самое последнее, что стоит между сбоем и
сломанным репозиторием.
