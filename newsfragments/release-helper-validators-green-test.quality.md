Релиз-помощник `release_bump`: тест закрывает вторую половину критерия работы — после бампа зелены не только `check` помощника, но и версионные правила ОБОИХ релизных валидаторов (`validate_release_claims.authoritative_version_errors` для README/ROADMAP и инвариант `manifest.package_version == VERSION` из `validate_ai_first_registry`). Работа `release-helper-bumps-all-version-surfaces` доведена до `done`.
