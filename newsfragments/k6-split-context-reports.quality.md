K6-глубина: формирование контекст-отчётов прогона (context-bundle / shadow / hybrid / payload / spec-coverage / work-package в отчёте) вынесено из god-функции `run()` контроллера в отдельную `_add_context_reports`. `run()` уменьшилась с 1182 до 1129 строк; потолок размера функций опущен 1182 → 1129. Поведение сохранено (характеристические тесты контекста, включая context-shadow и context-hybrid, зелёные до и после без правок).
