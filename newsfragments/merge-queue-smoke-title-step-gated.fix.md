Шаг проверки заголовка PR в pr-smoke запускается только на pull_request: в merge_group заголовка
нет, а PR_TITLE_CHECK=1 захардкожен — проба сделала бы pytest.fail, обязательный контекст smoke
упал бы и очередь слияния повисла навсегда. Заголовок к моменту очереди уже проверен на PR.
