Кит теперь проверяет продукт дочки ПО РАСПИСАНИЮ, а не только по требованию: новый поставляемый
workflow `ai-ops-audit` раз в неделю запускает `ai-ops audit product` (артефакты/tech/delivery/
backlog/риски) и выводит отчёт в summary прогона. Read-only — ничего не меняет; опт-аут — удалить файл.
