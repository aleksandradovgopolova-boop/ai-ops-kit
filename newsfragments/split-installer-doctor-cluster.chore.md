Продолжен разрез крупнейшего файла кита `installer/ai_ops.py`: кластер диагностики (команда `doctor`) вынесен в модуль-сателлит `installer/doctor.py`. Переехали `cmd_doctor` (самая большая функция файла) и её doctor-only хелперы — сбор строк вывода и вердикт (`_dprint`, `_blocker`, `_doctor_state`, `_doctor_verdict`), гигиена путей окружения (`_path_hygiene`), отчёт о стратегии обновления. Чистый рефакторинг: поведение байт-в-байт прежнее. В точке входа остался ленивый sibling-загрузчик, который передаёт сателлиту живое состояние работающего установщика; общие с другими командами функции остаются в `ai_ops.py`. Монолит ужат с 3605 до 3258 строк, потолок module-size снят до нового факта. Сателлит (408 строк) ниже порога 700 — отдельным потолком не заводится.
