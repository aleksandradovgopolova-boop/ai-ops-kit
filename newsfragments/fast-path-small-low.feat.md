Быстрый путь для мелкой безопасной работы: задача с size:small и risk:low, которую классификатор
не поднял к L2+, идёт мимо плана и приёмки — остаётся обычный PR (ревью человека + CI). Час работы
снова стоит час, а не сутки церемонии. Опасное быстрый путь не открывает по построению:
необратимое/деструктивное/секретное/высокий риск классификатор эскалирует к L3, и пометить
рискованное как small не поможет — уровень считает сигналы риска, а не size. PRODUCT/CRITICAL и
автономный `do` полный путь сохраняют.
