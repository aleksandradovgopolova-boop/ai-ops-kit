Строгая типизация фундамента пошла ратчетом-вверх: на `ai_ops_kit.kernel.*` включён `disallow_untyped_defs` (per-module override в `[tool.mypy]`). Ядро уже было полностью типизировано, поэтому замок бесплатен — но теперь новая функция без аннотаций в ядре краснит, а не проскальзывает. `shared` пока на уровне «реальные типовые ошибки = 0»: полный disallow там — 99 подписей, отдельный шаг ратчета. Граница «где типы обязательны» ходит только вверх.
