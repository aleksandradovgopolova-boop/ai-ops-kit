Быстрый слой проверок на PR (шард `fast`, `-m "not slow"`) больше не держат тяжёлые интеграционные тесты: прогоны execute_sequence с реальными git-worktree и подпроцессами, а также мета-тесты скриптов `check-fast.sh`/`check-full.sh` помечены `slow`. Они по-прежнему выполняются на каждом PR — в slow-шардах selftests, — но перестали задавать пол времени быстрого слоя. Стена `fast` опускается ниже 3 минут (issue #465).
