Два исхода роадмапа помечены достигнутыми после слияния кода (координация отделена от кода по
`parallel-safety`): `tokens_contract_and_capability_registries_available` под `uiux-standard-as-product`
(#418 — реестры токенов/capability как проверяемые данные) и `no_direct_writes_to_main_ever` под
`nightly-product-review` (#422 — валидатор `validate_nightly_no_direct_main_write`). Обе цели остаются
`active` (у каждой открыты другие исходы); закрытые работы внесены в историю плана с evidence.
