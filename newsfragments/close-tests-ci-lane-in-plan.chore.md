План актуализирован: лента «ревью + тесты/CI» после 4.0 закрыта. Зафиксированы четыре слитых PR — #474 (разрез `test_acceptance_verify.py` по темам), #475 и #476 (вычистка мёртвых ссылок на снятый слой `tools/` и уехавший в v3.34 `validation/`), #477 (ускорение группы `fast` на −12% разрезом двух тяжёлых под `--dist loadfile`) — и две находки: event-тиринг CI отвергнут замером (у него ~0 выигрыша при риске для защиты ветки), а самодостаточный запуск точек входа без `PYTHONPATH` уже работает и правок не требует.
