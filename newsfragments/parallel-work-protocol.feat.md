Кит получил механизм против беговой дорожки пересдач при параллельной работе: координационные файлы
(план, история, решения, релизные заявления) объявлены единым источником, который ведёт одна рука,
а `validate_parallel_safety.py` ловит PR, смешивающий код с их правкой. Протокол — в
`docs/parallel-work.md`, список файлов — в `registry/coordination-files.yaml`; дочка расширяет его
своим. Замерено на самом ките: без этого каждый мёрж делал соседние PR DIRTY.
