`installer/` включён в архитектурные ратчеты размера (F-06 внешнего аудита). `validate_module_size` обходил только `SOURCE_ROOT="ai_ops_kit"`, и `installer/ai_ops.py` (3926 строк) — крупнейший файл репозитория и единственный код, запускаемый пользователем ДО появления кита, — рос вне всякого гейта. Теперь корней два (`ai_ops_kit` + `installer`): файл заморожен потолком 3926 в `module-size-baseline`, и заведён func-size scope `installer/` (макс. функция `cmd_doctor`, 264 строки). Оба ратчета ходят только вниз — чинить сейчас не нужно, но рост монолита установщика больше не пройдёт незамеченным.
