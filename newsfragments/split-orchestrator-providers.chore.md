Монолит `ai_ops_kit/providers/orchestrator_providers.py` разрезан на фасад + три сателлита: `provider_errors.py` (типы отказов и распознаватели их сигнатур), `provider_writer_lock.py` (машинный замок локального писателя) и `provider_calls.py` (как собрать и вызвать провайдера — mock/anthropic/openai/claude-cli). Чистый рефакторинг: публичная поверхность и поведение сохранены (фасад ре-экспортирует все перенесённые имена), модуль снят с потолка module-size (978 → 332 строки).
