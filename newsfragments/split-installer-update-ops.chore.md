Продолжен разрез монолита `installer/ai_ops.py`: команды `update`, `status`, `diff` и отложенное обновление дочки через worktree вынесены в модуль-сателлит `installer/update_ops.py`. Гейт отставания `check-update` намеренно оставлен в `ai_ops.py` — он обязан работать из одинокой копии установщика без сателлитов рядом. Чистый рефакторинг: поведение байт-в-байт прежнее; общие с ядром функции читаются через живое состояние установщика. Монолит ужат с 2937 до 2601 строк, потолок module-size снят до нового факта; сателлит (400 строк) ниже порога 700.
