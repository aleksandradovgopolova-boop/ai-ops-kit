K6-глубина: транзакционный commit-barrier перед доставкой (надёжная запись RunHandoff и финального отчёта + отметка готовности в журнале) вынесен из god-функции `run()` контроллера в отдельную `_commit_barrier`. `run()` уменьшилась с 1084 до 1054 строк; потолок размера функций опущен 1084 → 1054. Поведение сохранено (тест порядка commit-barrier зелёный до и после без правок).
