Прямые git-вызовы движка сведены на единый хелпер с таймаутом (`shared.gitio`): зависший git-субпроцесс (сеть, lock, hook) больше не вешает прогон навсегда, а структурный тест-ратчет держит инвариант «ни одного неограниченного git-субпроцесса».
