Тесты гоняются параллельно по ядрам (`pytest-xdist`, `-n auto`). Набор параллель-безопасен —
проверено полным прогоном: 3343 passed под `-n auto`, 0 падений. Замер на 10 ядрах: полный контур
`check-full.sh` 11мин → ~5мин; быстрый профиль `check-fast.sh` (`-m "not slow"`) — ~52 сек. В CI
`-n auto` добавлен во все группы (`fast`/`contracts`/`selftests-a`/`selftests-m`) и в джобу покрытия
(она была самой долгой и держала слияние). `pytest-xdist` добавлен в `requirements-dev.txt` и в
install-строки CI. `check-fast.sh` gracefully работает и без xdist (последовательно) — быстрый
профиль не обязан падать из-за отсутствия пакета.

Число тестов не резалось: медленность была не в количестве (3343 быстрых теста идут ~минуту), а в
том, что сотни тестов поднимают подпроцессы и git-репозитории. Параллель — правильный рычаг: держит
страховку и возвращает минуты.
