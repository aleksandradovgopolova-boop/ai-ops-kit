`ai-ops explain` для незапущенной задачи читается по-человечески: вместо сломанной фразы «работа модель ещё не тратила» теперь «Стоимость пока не измерена — обращений к модели по этой задаче ещё не было». (Проверка команд двери показала, что их ответы в остальном уже человечны — политика общения работает; это была единственная явная грамматическая шероховатость.)
