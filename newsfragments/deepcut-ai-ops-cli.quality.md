Крупнейший файл пакета `ai_ops_kit/cli/ai_ops_cli.py` разрежён (1855 → 1251 строк): проб-свободные
обработчики намерений (`products`/`delivery`/`model`/`contract`/`inspect`/`plan`/`session`) вместе с
`build_preview`/`_print_preview`/`_run_backlog` и хелперами здоровья/рисков вынесены в новый спутник
`ai_ops_kit/cli/ai_ops_cli_intents.py`. Поведение не менялось — чистый перенос с ре-экспортом именами
`ai_ops_cli` (вызывающие, тесты и диспетч резолвятся как прежде); все 10 несущих мутационные пробы
строк остались в `ai_ops_cli.py`, реестр проб не тронут. Потолок ратчета размера модуля опущен под
факт, потолок числа файлов поставки поднят под новый спутник записью в ленте.
