Автономный PR больше не немёржабелен по собственному status-freshness (#404). На шаге доставки прогон обновляет living-status дочки в ТОМ ЖЕ коммите: дописывает факт «доставлена работа <wid>» в журнал и бампает `reviewed_at`, чтобы volatile-док не протух и required-проверка дочки не блокировала PR. Честно — доставленная работа реально меняет «что готово», а не косметика даты. Путь дока — из `.ai-ops.yaml` (`living_status.doc`), иначе `context/product/ProductStatus.md` / `PROJECT_STATUS.md`. Управляемого дока нет, нет frontmatter или `template: true` → no-op (чужой статус кит наугад не сочиняет).
