Слои (K2): пара context↔engine развязана по существу. Сборщик контекста больше не строит
план прогона сам через динамический импорт engine — готовый план приходит извне или строитель
инъецируется параметром, а CLI-обёртка, которой он нужен, переехала в точку входа (devtools).
Это ребро замыкало все длинные циклы: взаимных пар слоёв стало 2 из 5, а циклов длиннее двух —
0 (и ноль теперь настоящий, а не слепота анализатора к `__import__`).
