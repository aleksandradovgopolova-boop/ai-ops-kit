Прополка тестов: покрытие двух монолитных селфтестов перенесено в гранулярные файлы —
одно поведение = один тест с настоящей проверкой значения, а не одна общая обёртка `expect()`.

`tool_loop`: в `tests/unit/test_tool_loop.py` добавлены проверки, которых не было гранулярно —
запись в scope реально создаёт файл (и в `executed` есть `write ok=True`), запись вне scope
физически не создаётся, shell исполняется как evidence, модель видит содержимое прочитанного
файла в контексте, восстановление после битого JSON с корректирующей подсказкой, read-cap
(отклонение лишних чтений и нетронутый нормальный поток) и весь контракт `run_review`
(pass/fail с blockers, read-only запрет записи, видимость содержимого, no-verdict, форс-ход
вердикта после исчерпания чтений). Монолит `tests/unit/test_tool_loop_selftest.py` снят.

`ai_ops_cli`: в `tests/unit/test_ai_ops_cli.py` добавлены проверки понятого workflow, измеренных
данных превью (агенты/токены), названного результата, согласия с роутером без `task_type`,
адресации CRITICAL-одобрения человеку, настоящих действий команд `new`/`plan`/`discuss`/`status`,
честного отказа `health` без метрик, невыполнения `preview onboard` и честного no-branch у `review`,
а также проводки `--model` через `run --execute` в отчёт. Монолит
`tests/unit/test_ai_ops_cli_selftest.py` ОСТАВЛЕН: его тест-функция названа поимённо в
`-k`-выражении смоук-прогона `.github/workflows/pr-smoke.yml` — снятие сломало бы CI.
