Тир-2 WIRE (#677 + #678, часть): вердикт двенадцати ранее-неподключённых валидаторов теперь потребляется parent-CI, а не только контракт-тестом формы. Новый `tests/contracts/test_safety_validators_green_on_live_kit.py` гоняет на ЖИВЫХ артефактах кита safety-валидаторы (`validate_model_qualification`, `validate_model_roles` — допуск моделей и роли-классы; `validate_security_domains`, `validate_security_posture` — домены и постура; `validate_sandbox_boundary` — честность слова «песочница») и валидаторы живых реестров (`feature_learning`, `learning_loop`, `regression_corpus`, `research_artifacts`, `presets`, `workflow_gates`, `stale_gates`). Дрейф любого из этих реестров теперь красит сборку. Остаток #678 (валидаторы рантайм-эмитируемых объектов run_handoff/context_bundle/spec_coverage и «удержанные» policy-валидаторы access_filter/key_lifecycle/context_architecture/budget_contract) требует проводки в генератор/энфорсер — отдельным заходом.
