Тир-2 WIRE (#677): вердикт пяти safety-валидаторов теперь потребляется parent-CI, а не только контракт-тестом формы. Новый `tests/contracts/test_safety_validators_green_on_live_kit.py` гоняет на живых артефактах кита `validate_model_qualification`, `validate_model_roles` (допуск моделей и роли-классы: «никакого неквалифицированного судьи», cheapest-first), `validate_security_domains`, `validate_security_posture` (домены и постура безопасности, резолвинг evidence) и `validate_sandbox_boundary` (честность слова «песочница»). Дрейф любого из этих реестров теперь красит сборку.
