K6-глубина: фаза исполнения (снимок HEAD до правок, прогон реализации через модель, распознавание факта правок — broker/shell/model-commit) вынесена из god-функции `run_pipeline` в `_run_tool_loop`. Та же логика определения «что произвёл именно этот прогон»; поведение сохранено (характеристические тесты зелёные до и после без правок).
