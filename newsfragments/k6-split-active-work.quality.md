K6-глубина: регистрация active-work и concurrency-preflight (координация параллельных сессий — не начинать работу, которую держит другая сессия) вынесена из god-функции `run()` контроллера в отдельную `_register_active_work`. `run()` уменьшилась с 1051 до 1003 строк; потолок размера функций опущен 1051 → 1003. Поведение сохранено (тест регистрации active-work зелёный до и после без правок).
