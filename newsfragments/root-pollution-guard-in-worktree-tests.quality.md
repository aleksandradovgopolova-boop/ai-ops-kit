Регресс, при котором доставляющий вызов кита с корнем по умолчанию писал managed-слой, `.ai-ops` или CI-воркфлоу в корень рабочего репозитория и порядок-зависимо ронял from-copy проверку, теперь ловится сразу и адресно: сторож в `tests/conftest.py` валит именно тест-виновника и точечно чинит дерево (не трогая отслеживаемый контекст). Заодно `_backfill_required_context` перестал писать мимо переданного корня.
