# Инсайты первого боевого прогона (child: ii-sreda, фича catalog-api-migration)

- **Источник:** retrospective и run_report первого полного цикла в child-репозитории
  (ii-sreda PR #8, #11, #12); ретро — features/catalog-api-migration/retrospective/.
- **Owner:** repository-memory-curator.
- **Дата проверки:** 2026-07-09.
- **Условие устаревания:** данные 3+ прогонов на разных классах проектов заменят
  выводы одного прогона.

## Данные

Итог прогона: вердикт OK; из 28 артефактов дефолтного blueprint заполнено 7,
**declined — 21 (75%)**. Причины отказов легитимны (mock-прототип без телеметрии,
без пользовательской документации, без прод-мониторинга), но объём ручного
declined-оформления — заметная стоимость процесса.

## Уроки для кита

1. **Дефолтный blueprint слишком тяжёл для класса «прототип/MVP».**
   75% отказов на честном прогоне — сигнал вводить профили blueprint
   (lean: discovery/definition/delivery/analytics/retrospective; full: все 11 стадий).
   Кандидат v2.3 (см. ROADMAP).
2. **Правило «blueprint закрывается в том же PR, что и релиз»** — родилось в child
   и уже автоматизировано у него (run_report в CI). Перенесено в комментарий
   шаблона FeatureBlueprint.yaml.
3. Подтверждена ценность связки run_report + CI: обе находки прогона
   (невалидный статус, отставание blueprint от релиза) теперь воспроизводимо
   краснят PR, а не ждут ручного аудита.
