---
read_tier: 1
stability: evolving
reviewed_at: 2026-08-11
owner: product-team
---

# ROADMAP — куда идёт AI Ops Kit

Видение — в `VISION.md`. Список релизов — в `CHANGELOG.md`. Пройденный путь (фазы v1.3 → v3.36,
856 строк) — в [`docs/changelog/roadmap-history.md`](docs/changelog/roadmap-history.md).

Совместимость: текущий мажор — **4.x** (текущий канал — **v4.0.0 qualification**; точная версия — в
`VERSION`). Переход 3.x → 4.0 был breaking: физически снят плоский слой `tools/`, точки входа только
пакетные (`python3 -m ai_ops_kit.<pkg>.<mod>`) — руководство в `MIGRATION_GUIDE_4.0.md`, обновление
дочки через границу мажора осознанное (`ai-ops update --force`). Внутри 4.x — обратная совместимость.
Физический разнос дерева по `packages/<name>/` (3.0-срез 3, тоже breaking) в 4.0 НЕ входил — это
отдельная будущая веха.

**Это уровень направления, а не таск-трекер.** Здесь нет «сделать API», «починить экспорт» — это
работа, и её место в плане (`planning/plan.yaml` у подключённого репозитория). Здесь — что
изменится для того, кто китом пользуется.

**Сверху — продуктовый исход, снизу — инженерное доказательство.** Каждая цель называет результат
для пользователя ПЕРВЫМ; чем именно он доказан (номера PR, снятые взаимные пары в
`packages/layering.yaml`, проведён ли модуль в контур) — деталь под ним, а не сам ориентир.
Что кит умеет **прямо сейчас** — не в этом файле: актуальная карта возможностей с честным
разделением **built** и **built≠wired** — [docs/capability-map.md](docs/capability-map.md),
и она ВЫВЕДЕНА из реестров и кода, а не написана руками. ROADMAP говорит, куда идём; витрина —
где мы уже стоим.

Формат цели: `` `goal-id` `` в обратных кавычках, дальше описание результата. Порядок под «Сейчас»
= приоритет. Контракт файла проверяется кодом — `ai_ops_kit/planning/roadmap.py`; он же требует
четыре горизонта от каждого подключённого репозитория, и кит обязан выполнять его сам.

## Сейчас

Что главное сейчас. Порядок = приоритет.

- `qualification-closeout` — реальная продуктовая задача доходит до verified PR **без ручной правки
  самого кита**, и это повторяется на двух разных живых репозиториях. Решение владельца 14.08.2026
  (`decisions/registry.yaml → ep-2026-08-14-product-os`): до расширения продуктового и
  дизайнерского слоя закрывается текущая единица надёжности. Проверяемо семью числами прогона —
  технические вмешательства владельца, необоснованные вопросы, ложные green, ложные red, придуманные
  факты, стоимость, время до результата — плюс перечень внутренних сущностей, которые человеку
  пришлось понимать. Отдельный результат closeout, добавленный по замеру: **контур ловит класс
  «объявлено — не исполняется»**. Замер: 36 находок за пять кругов свежего ревью на PR #118, ни
  одной не поймали 11 джоб CI; пока это так, каждый новый цикл поедет с тем же дефектом формы.
  Первое живое топливо (09.09, #587): внешний репозиторий `ai-ops-live-587` доведён до verified PR,
  но планка **«без ручной правки кита»** там НЕ взята — пришлось починить два дефекта, которые прогон
  вскрыл (#667: ревьюер не мог заземлить `pass` на claude-cli-пути; `specification` терял
  `requirements_covered` на reevaluate). Оба влиты в main → closeout разблокирован.
  Второй прогон (09.09, #668) взял планку **«без ручной правки кита» на ДВУХ новых сторонних
  репозиториях**, оба на одном неизменённом ките (`origin/main` 45eb5b3f), провайдер `claude-cli`
  в сессии: `durationkit` (`parse_duration`, PR
  aleksandradovgopolova-boop/durationkit-live-668#1, 20/20 настоящих тестов) и `slugkit` (`slugify`,
  PR aleksandradovgopolova-boop/slugkit-live-668#1, 16/16). Оба — `READY_FOR_PR`, 11/11 гейтов
  (8 машиной, 3 судьёй, ноль писателем-штампом, ноль человеком), evidence на точном SHA, ноль ложных
  green (сверено настоящим pytest независимо от вердикта). Семь чисел прогона: технические
  вмешательства в КИТ 0 · необоснованные вопросы 0 · ложные green 0 · ложные red 0 · придуманные
  факты 0 (кит отказался сочинять спеку) · стоимость не тарифицируется (claude-cli в сессии) · время
  ~5 мин/репо от `run --execute` до открытого PR. Внутренние сущности, которые пришлось понимать
  человеку: `--feature`, сигналы task_type/size/risk, L1-спека (15 разделов), флаги
  `--execute`/`--spend-ok`/`--open-pr`, вердикт `READY_FOR_PR`/гейты, worktree-изоляция, провайдер.
  Основная планка closeout взята. Оговорки: (1) прогон вскрыл в самом ките класс «объявлено — не
  исполняется» — `resume --deliver-only` падал `unrecognized arguments` (объявлен CLI, не исполнялся
  движком); починено этой сессией (тест + newsfragment), основного пути `run --execute --open-pr`
  дефект не касается; (2) под-исход «контур ловит объявлено-не-исполняется В ДОЧКЕ» этими прогонами
  не проверен — реализации в дочках были честными, ловить было нечего. Прежняя оговорка «кит хорошо
  СУДИТ» подтверждена; в этих прогонах писатель с первого захода писал корректно.
- `team-works-in-parallel` — над продуктом работает КОМАНДА одновременно, и это не оплачивается
  конфликтами и дублями. Решение владельца 18.08.2026 (`decisions/registry.yaml →
  ep-2026-08-18-team-works-in-parallel`): вариант «считать конфликты нормальной ценой» отклонён.
  Проверяемо ЧЕТЫРЬМЯ исходами, и первый из них решающий: заявка на работу видна ДРУГИМ МАШИНАМ
  (сегодня реестр «кто что держит» лежит в игнорируемых файлах — замер `git check-ignore` в ките и в
  дочке, — то есть для команды он не координирует ничего); `next` не предлагает то, что уже держит
  другой; правки самого кита не создают конфликтов в файлах, которые трогает каждая работа; владелец
  НЕ проектирует эту работу, а участник не сомневается, нужна ли его задача и не делает ли её кто-то
  ещё. Замер повода: девять PR из четырёх сессий за три часа, выброшенный дубль #526, слияние
  из-под чужих рук, один конфликт разрешён пять раз за час (заявка #150).
  Надёжность ПАРАЛЛЕЛЬНОЙ авто-постройки укреплена 03.09 тройкой durable-фиксов (заезд 6 работ
  вскрыл повод): машинный замок писателя — параллельные `run --execute` не деадлочат на одном
  claude-cli (#453); fix-цикл не зацикливает писателя на нехватке инструментов в среде (#454);
  вердикт называет дефицит среды средой, а не дефектом кода (#456). Вывод заезда: **кит хорошо себя
  СУДИТ (ворота+CI поймали каждый дефект, ни один кривой PR не влит зелёным), но плохо себя ПИШЕТ**
  в этой среде. Оставшийся корень — песочница прогона без инструментов CI (ruff/pytest/pyyaml):
  агент проходит ослабленный набор, дефекты всплывают на CI, а не внутри прогона (#468).

- `storybook-as-visual-contract` — UI-задача проектируется, согласуется и проверяется через
  Storybook. Сторона evidence построена и проведена в контур (`ui/storybook_adapter.py` собирает
  UIEvidenceBundle, `validate_storybook_evidence.py` не даёт выдать «нет данных» за «чисто»); сторона
  СОЗДАНИЯ построена И теперь ВПЛЕТЕНА в гейт: обязательные stories выводятся из Experience Contract
  кодом (`required_story_specs`/`required_stories_coverage`, #452), и с 09.09 `required_stories`
  входит в UIEvidenceBundle и в `evidence_for_gate` — объявленного контрактом опыта нет в собранном
  Storybook → `ux_review` краснеет ДЕТЕРМИНИРОВАННО (built≠wired закрыт). Варианты опыта с trade-offs
  (`generate_design_options`, #416) построены и проведены в engineering_advisor. ЖИВОЙ e2e ПОЛУЧЕН
  09.09 на реальной UI-дочке `ai-ops-cockpit` (экран Active Work: настоящий Experience Contract +
  собранный Storybook-индекс + evidence, кит из origin/main): полный контракт → `ux_review: pass`
  (заземлён на точный SHA, `validate_storybook_evidence` чист); убрать обязательную story «error» →
  `required_stories.missing=[active-work-error]` → `ux_review: fail` детерминированно. Волна 3 на том
  же репо: `constitution_coverage` доступен (65 правил, 18 автоматизируемых, 7 покрыто, 11 названы).
  Оговорка «синтетика, не реальный контракт» СНЯТА. Контракт-проверка ДЕТЕРМИНИРОВАННА (не субъективна)
  и потому блокирует по существу; «advisory» касается СУБЪЕКТИВНОЙ части (flow/copy/tone) — оставлено
  advisory решением владельца 09.09 (`ep-2026-09-09-direction-wave-owner-calls`, «сначала полевые
  доказательства»; теперь они есть). Дальше — уже интеграция в постоянный UI-репозиторий команды.

- `uiux-standard-as-product` — дочерний репозиторий получает единый ПРОВЕРЯЕМЫЙ UI/UX-стандарт, а не
  тонкий набор чек-листов: правила со стабильными ID (`UI-`/`AI-`/`UI-FORBIDDEN-`), токены-контракт,
  каталог компонентов/паттернов/шаблонов, из которого агент собирает экран, и Storybook-контракт
  вместо ручной вёрстки. Существующие блокирующие гейты (`ux_review`, `accessibility_review`,
  `design_system_usage`) обретают глубокое содержание, ссылаясь на ID стандарта. Внедряется СЛИЯНИЕМ
  с существующим слоем дизайна, волнами: (1) Конституция как источник + реконсиляция ID гейтов
  (наблюдение), (2) токены-контракт + реестры возможностей, (3) углубление ролей-ревьюеров и
  UI-health, (4) MCP-доступ к UI-системе. Сторона создания Storybook — через
  `storybook-as-visual-contract`. Источник: пакет владельца `ai-ops-uiux` (решение A, 2026-08-27).
  Волна 1 достигнута 28.08; волна 2 (токены-контракт + реестры) слита 03.09 (#451): в дочку едут
  СХЕМЫ контракта (design-tokens/uiux-registry), против которых дочка валидирует свои токены/реестры.
  Исход `tokens_contract_and_capability_registries_available` ЗАКРЫТ 04.09 (#418): реестры
  `standards/uiux/registries/tokens.yaml` (контракт дизайн-токенов) и `capabilities.yaml` (сквозные
  возможности UI-системы) добавлены как ПРОВЕРЯЕМЫЕ данные — их читает и валидирует
  `standards/uiux/scripts/validate-registries.py` (ссылочная целостность). Про «пробел волны 2»
  (доставка эталонных ДАННЫХ в дочку) — РЕШЕНО владельцем 09.09 (`ep-2026-09-09-direction-wave-owner-calls`):
  эталонные данные ОСТАЮТСЯ В КИТЕ, в дочку НЕ везём. Функционально дочке для валидации своих токенов
  достаточно СХЕМ (`schemas/*.json` уже едут), а доставка кита по умолчанию грузит весь managed_set в
  КАЖДУЮ дочку — возить эталон всем значило бы раздувать проекты без дизайна (и пробивать гард
  footprint). Валидатор `validate-registries.py` дочке не служит (self-relative — проверяет эталон
  кита). Волна 3 (углубление ролей-ревьюеров и UI-health)
  ДОСТИГНУТА 09.09 (#530/#419, статус сведён к правде — код был на main): `constitution_coverage`
  вшит в `ui_readiness` → pipeline/deploy_readiness, UI-health считает покрытие Конституции; гейты
  цитируют ID (волна 1). Волна 4 (MCP-доступ) — на main минимальный адаптер (`storybook-query` как
  MCP-инструмент, #529/#420), но исход `mcp_access_to_ui_system` держится честно `false`: полный
  MCP-сервер, отдающий токены/реестры и вердикты (`get_*/validate_*`), СОЗНАТЕЛЬНО отложен ревью
  владельца (`registry/tools.yaml`: «минимальный адаптер, а не центр»). Гейты advisory.
- `owner-speaks-product-not-pipeline` — владелец описывает задачу обычным языком, а кит отделяет
  факты от предположений, называет варианты с trade-offs, берёт ОДНО содержательное решение,
  проектирует опыт, показывает состояния в Storybook, реализует, проверяет критерии и создаёт
  verified PR. Проверяемо тем, чего владелец НЕ делает: не выбирает агентов и workflow, не правит
  YAML, не знает feature id, не запускает внутренние команды, не разбирается в provider transport,
  не толкует сырые гейты. Первый полигон — `ai-ops-cockpit`, и первая работа там одна вертикальная
  функция (экран активной работы), а не «улучшить дизайн»: у полигона есть слепое пятно — владелец
  там сам себе пользователь, поэтому «ноль технических вмешательств» мерит либо сторонний человек,
  либо заранее записанный сценарий владельца. Активная работа под целью есть — поэтому в «Сейчас».
- `kit-release-strategy` — кит собирает релизы осознанно (по вехе/расписанию), а не когда гейт
  вынудил, и владелец продукта выбирает стратегию обновления из НАЗВАННОГО меню (канал × политика,
  сверх pr/manual — scheduled-батч и auto-stable). Каналы и ручки дочки уже есть — это дисциплина и
  меню поверх них. Заведена по наблюдению владельца 2026-08-31 (реактивный выпуск 3.38.0). Активная
  работа в плане есть (релиз-помощник слит #445, меню обновления) — поэтому в «Сейчас».

## Следующий результат

Что изменится для того, кто китом пользуется, после ближайшей работы. Проверяется его глазами, а
не глазами разработчика кита.

- `product-decision-loop` — обоснованное продуктовое решение: у фичи проверяется baseline/target/
  guardrails кодом (`check_feature_target`/`gate_feature_decisions`, #450 слит). Механизм построен,
  но исход держится `false` до проводки в обязательный контур (модуль `decision_loop` пока unwired):
  следующий содержательный шаг — подключить механизм к гейту, чтобы решение фичи проверялось на
  каждой работе, а не только существовало как функция.

## Дальше

Следующие крупные возможности. Без сроков — направление, а не план.

- Обкатка advisory-гейтов на child-репозиториях: 14 из 35 <!-- claim:gates-total --> гейтов
  объявлены и не блокируют. Цель ПРИОСТАНОВЛЕНА 2026-08-14 (`plan-as-control-plane`): приоритет ушёл
  в Product OS, работы под ней сейчас нет, и держать её под «Сейчас» значило бы объявлять занятость,
  которой нет. Вернётся вместе с работой.
- Два продуктовых направления БЫЛИ приостановлены 2026-09-02, затем по ним взят и слит первый шаг
  03.09.2026 (заезд 6 работ). МЕХАНИЗМ каждого доставлен, но исход держится честно неполным до
  доказательства — не путать «построено» с «достигнуто» (третье, `product-decision-loop`, поднято в
  «Следующий результат» — по нему следующий шаг ближе всего):
  - `nightly-product-review` — механизм есть: дельта-обзор умеет идти ночью реальным CI-триггером и
    доставляет бриф владельцу (durable-инбокс + receipt, #447 слит). Исход держится `false` до
    ЖИВОГО прогона (реальная ночная джоба, отработавшая и приславшая бриф, ещё не наблюдалась). Исход
    «никаких прямых записей в `main`» ЗАКРЫТ 04.09 (#422): валидатор
    `validate_nightly_no_direct_main_write` AST-обходом ночного пути (оркестратор + worktree-шов +
    pr_open-шов) краснеет на прямой записи в main — принцип стал проверкой. Остаётся `false-positive
    rate назван` (#423, работа не взята); плюс живой ночной прогон для первого исхода.
  - `outcome-and-analytics-loop` — ДОСТИГНУТ 2026-09-04 (#424): проводка producer → гейт замкнута.
    Производитель `events_verified_live` теперь ПИТАЕТ гейт `analytics_runtime_verification` как
    источник evidence (`verify_analytics_runtime` → `gate_executor`), и вердикт машины авторитетен
    над словом судьи — «verified in runtime» даёт машина, а не декларация. Стоит фактом, не
    направлением; разбор — в `history/plan-history.yaml` (`analytics-runtime-evidence-wired`).
    Оговорка честности: «достигнуто» = проводка авторитетна ПРИ ВЫЗОВЕ; рантайм-интеграция
    post-release-гейта в конвейере (PRR зовёт `verify_analytics_runtime`) остаётся follow-up —
    упирается в доступ к аналитическому бэкенду дочки, тот же живой прогон, что отложен у соседней цели.
- Достигнутое убрано из «Сейчас» и живёт как факт, а не как направление: первый живой brownfield
  (`real-product-qualification`, продолжение — в `qualification-closeout`), автономия сессий под
  потолком (`session-autonomy-under-ceiling`), второй brownfield — `second-real-brownfield`
  доведён владельцем до слитого verified PR (ии-среда #701, 2026-09-02) — и план как управление
  (`plan-as-control-plane`: открытый PR не сосуществует с `todo`, расхождение с git краснеет,
  `done` требует названного результата, закрытое уезжает в историю). Разбор — в
  `history/plan-history.yaml`.
  Сюда же переведены 2026-09-03 два направления, чьи исходы достигнуты полностью (roadmap↔план
  свёл к нулю по ним): `checks-that-run` (4/4 — каждая объявленная проверка исполняется: нет теста
  без утверждения, линтер гоняется на пакете, каждая точка входа работает, проглоченные исключения
  разобраны) и `green-means-checked` (2/2 — «зелёное» значит проверки исполнились: `lint` зелён в
  CI, охват назван по `registry/release-claims.yaml → evidence_scopes`, а не словом «полный»). Оба
  держались в «Сейчас»/«Следующем» уже как факт — теперь стоят фактом, а не направлением.
  Сюда же переведён 2026-09-09 `trustworthy-core` — оба проверяемых исхода верны по коду: Phase 0
  (контроль честен) — 11 лент влиты, findings аудита закрыты (`docs/audit-2026-08-25/`); Phase B
  (ядро — отдельный слой) — `mutual_pairs` доведён до 0 (`packages/layering.yaml`: кольцо capabilities
  стало DAG), фазы pipeline — отдельные тестируемые функции (`_pipeline_*` в `execution_pipeline.py`)
  с характеристическими тестами. Стена «code_review-ревьюер молчит в сессии Клода» (#570/#580) снята
  и подтверждена ЖИВЫМ прогоном 09.09 (#668): durationkit/slugkit доведены до READY_FOR_PR ИЗ сессии
  Клода без внешнего ключа, ревьюер вернул заземлённый вердикт, 0 ложных green. Разбор — план
  (`goal: trustworthy-core → achieved`) и `history/plan-history.yaml`.

- Настоящий параллельный прогон: отдельные клоны на пакет. Сейчас пакеты идут по одному
  (`execution_concurrency: serial`), и это объявлено честно, а не выдаётся за параллелизм.
- Доставка следует 301-redirect перенесённого репозитория. Сейчас — объявленный разрыв
  (`future_opportunities.delivery_follows_repo_transfer_redirects: known_gap`): исход становится
  `outcome_unknown`, PR дозавершается прямым API.
- Строгий DAG пакетов. Замер ядра — 12 взаимных пар и 210 циклов длиннее двух
  (`packages/layering.yaml → baseline`); это потолок с ратчетом только вниз, и разбирается он по
  одной паре, а не выключателем.

- Claude-native онбординг и zero-touch подключение. Сейчас установка начинается с ручного
  `python3 installer/ai_ops.py init` — сценарий «открыл незнакомый репозиторий и сказал одной
  фразой „Установи AI Ops“» подхватывается скиллом рантайма, но first-class путём с чистой машины
  не является. Целевой путь: подключение само → «я изучил проект, вот что понял, нужны ответы на
  четыре вопроса» → обычный диалог → «вот модель и план, которые предлагаю создать» →
  подтверждение → bootstrap → «следующим рекомендую X, потому что Y». Машиночитаемая правда
  (`onboarding-answers.yaml`, `planning/plan.yaml`, provenance) остаётся, но владелец больше не
  обязан править YAML руками. Это продуктовый слой поверх готового ядра, а не новый механизм.

- Интеграция с Figma и извлечение design context, синхронизация с реальной дизайн-системой,
  публикация Storybook preview в PR, инфраструктура visual regression. Порядок объявлен решением
  владельца: **после** рабочего Storybook-цикла, а не до него.
- Портфельная приоритизация, continuous discovery и автоматическая связь
  insight → opportunity → experiment → roadmap; Knowledge Graph полного продуктового цикла.
- Ротационные глубокие ночные обзоры (документация / тесты / архитектура / продукт и UX / security),
  недельные тренды, hotspot detection, повторяющиеся находки превращаются в валидаторы, и
  `portfolio-intelligence`: обезличенные сигналы по всем подключённым продуктам поднимаются в
  материнский кит методами и метриками, БЕЗ смешивания кода и evidence разных продуктов.

## Later

Осознанно НЕ берём сейчас. Записанное решение «не сейчас» отличает roadmap от списка желаний:
через месяц никто не помнит, было ли это забыто или решено.

- Автономный автосудья strict security — не берём: held-out N=80 и каскад v3.8.4 показали
  «не квалифицирован» (`future_opportunities.autonomous_security_judging`). Человек на strict
  security объявлен ЦЕЛЕВОЙ границей портфеля (`release-claims.yaml → standing_boundaries`), а не
  недоделкой. Вернуться: появилась заметно сильнее модель или принципиально другая архитектура судьи.
- Автономный green сложного full-stack дешёвым writer'ом — не берём: за границей портфеля.
  Доказанный live путь — сильный writer (`claude -p`) + дешёвый независимый ревьюер (deepseek) +
  человек. Вернуться: дешёвый writer станет сильнее kimi.
- Устранение родового top-level имени `tools` в колесе — не берём: сначала решается, должен ли
  `tools` быть Python-пакетом или остаться набором точек входа для скриптов и документации child.
  Имя объявлено границей осознанно, ратчет на состав top-level имён есть
  (`tests/unit/test_distribution_surface.py`).
- Единый форматтер кода (`ruff format`) — не берём: замер при реальной конвенции (line-length 110,
  p99 длин строк) — 528 файлов из 901 и ~45 000 изменённых строк. Один такой коммит топит
  `git blame` по всему репозиторию, а по-файлово это годы. Хук в pre-commit снят как неприменимый
  (2026-08-12). Вернуться: если решим заплатить историей за единый стиль — тогда одним коммитом,
  объявленным заранее, и с обновлением `.git-blame-ignore-revs`.
- Ещё десятки агентов, отдельный документ под каждую продуктовую мысль, сложный Knowledge Graph,
  глубокая Figma-интеграция до рабочего Storybook-цикла, blocking-режим новых дизайн-гейтов и рост
  числа workflow без живого вертикального сценария — не берём (решение владельца 14.08.2026).
  Причина одна: красивый Cockpit поверх неудобного пользовательского пути и «дизайн готов, потому
  что Storybook собирается» — это те же ложные green, только на продуктовом слое. Вернуться:
  после того как ОДНА настоящая функция пройдена целиком — от пользовательской проблемы до
  измеренного результата.
- Автоматическая уборка остаточного `.pth`-пояса в site-packages — не берём: pip не может (файла
  нет в RECORD), installer не должен (молча удалять файлы за пределами своего окружения — тот же
  дефект, что молча их писать). Лечение явное: `doctor` находит и блокирует,
  `doctor --remove-path-belt` удаляет по просьбе.

## Правила движения по roadmap

- Каждая фаза проходит полный набор валидаторов; новые механизмы приносят свои валидаторы и
  тесты вместе с собой.
- Новые агенты не принимаются без eval-кейсов (`validate_agent_evals.py`).
- Гейты вводятся как non-blocking и становятся blocking только после обкатки на child-репозиториях.
- Capability-декларации честные: заявляем только реализованное, планы — со статусом `unsupported`
  + note «planned».
- Цель под «Сейчас» без работы в плане и работа под целью, которой здесь нет, — оба расхождения
  кит показывает сам (`ai-ops next`).
