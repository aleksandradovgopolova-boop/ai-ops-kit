# Novelty-Watch — журнал еженедельной разведки

Формат: один блок на прогон. Только проверяемые факты с URL. «Пусто» — валидный результат.
Кандидаты уходят в `.research/sources/inbox-novelty.yaml` (unvetted).

---

## 2026-07-30 (первый прогон; окно — неделя 2026-07-23…30)

**Топ-находка недели.** Финальная ревизия спеки MCP `2026-07-28` вышла — с ломающими изменениями: протокол становится stateless (убраны `initialize`/`notifications/initialized` и `Mcp-Session-Id`), добавлен обязательный `server/discover`, удалены `ping`/`logging/setLevel`, убрана resumability SSE-потока, введён обязательный `resultType`; features Roots/Sampling/Logging и OAuth DCR переведены в Deprecated, принята политика жизненного цикла с окном депрекации не менее 12 месяцев ([changelog](https://modelcontextprotocol.io/specification/2026-07-28/changelog), контекст — [AAIF blog](https://aaif.io/blog)). Затрагивает MCP-декларации в `registry/tools.yaml` (hf-mcp-server и др.) и любые наши предположения о сессионности MCP.

**Харнесс под китом (Claude Code 2.1.217→2.1.220, эта неделя).** Изменения, задевающие инварианты и sandbox-политику: вложенные субагенты теперь по умолчанию до глубины 3 (было 1, флаг `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`); появилась настройка `sandbox.network.strictAllowlist` (запрет неразрешённых хостов без промпта); новый хук `DirectoryAdded`; хуки из frontmatter агента теперь требуют workspace-trust папки самого файла агента; скиллы с `context: fork` по умолчанию уходят в фон ([CHANGELOG](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)). Первые два пункта — потенциальный дрейф относительно EV-910 (sandbox как конкретная изоляция) и writer≠judge/оркестрации.

**Пополнение охвата (не новинки недели, но пропуски прошлого скана).** Найдены два жанровых соседа, отсутствовавших в EV-660/EV-663: [controlkeel](https://github.com/aryaminus/controlkeel) — Apache-2.0, Elixir, push 2026-07-29, самоописание почти дословно повторяет тезис кита («enforce policy gates, track findings, proofs, and evals»), но масштаб крошечный (11 звёзд); [ai-devkit](https://github.com/codeaholicguy/ai-devkit) — «The control plane for AI coding agents», 1568 звёзд, push 2026-07-26, **лицензия не указана** (переиспользование закрыто). Плюс governance-рамка [CAGE-1](https://arxiv.org/abs/2607.03510) (2026-07-03, один автор, кода нет), чьи оси оценки почти покрывают тезис кита.

**Проверка прошлых evidence — расхождений не найдено, три подтверждены заново:**
- EV-662 (OpenHands: gates/транзакционная доставка не документированы) — **в силе**: релизы 1.6.1…1.7.2 (28-30 июля) — LLM-селектор, память, WebSocket-auth, багфиксы; ни policy gates, ни delivery-транзакции ([releases](https://github.com/OpenHands/OpenHands/releases)). Побочно: `OpenHands/agent-canvas` **заархивирован**, а функция ушла под SaaS-аутентификацию (cloud-1.47.0).
- EV-663 (Rel(AI)Build — публичного репозитория нет) — **в силе**: поиск по GitHub даёт `total_count: 0`; Galileo Agent Control без новостей с марта (Apache-2.0).
- EV-804 (Microsoft Agent Governance Toolkit: 7/10 Full + 3/10 Partial) — **в силе**: описание репозитория заявляет «Covers 10/10 OWASP Agentic Top 10», их же compliance-док по-прежнему говорит дословно «Official ASI coverage: 7/10 Full, 3/10 Partial, 0 Gaps» ([док](https://github.com/microsoft/agent-governance-toolkit/blob/main/docs/compliance/owasp-agentic-top10-architecture.md)).

**Спокойные направления.** AGENTS.md — ломающих изменений нет: канонический репозиторий [agentsmd/agents.md](https://github.com/agentsmd/agents.md) последний push 2026-03-12, страница проекта на aaif.io пуста; конвенция кита в безопасности. ECC — жив (MIT в корне, 236k звёзд, push 2026-07-29), граница MIT/Pro не двигалась с марта 2026 ($19/seat: приватные репозитории, pooled usage, AgentShield-проверки глубже); осторожность: счётчики каталога расходятся между сайтом (261 скилл/64 агента) и README (281/67) — числа EV-702 использовать нельзя. ruflo — прежний churn: 5+ релизов за неделю до v3.32.39 (2026-07-30), лицензия MIT без изменений, из содержательного — фильтрация MCP-инструментов и совместимость PreToolUse-вердиктов с Codex. Quality gates/evals — нового инструмента за неделю не вышло; только заявки на стандарты (индивидуальный IETF-драфт по security-бенчмарку агентов, 55 метрик, рабочей группой не принят).

**Постскриптум (тот же день).** RR по MCP заведён и прогнан: RR-012 → EV-1001…EV-1011 → DP-112 (status draft, независимого judge не было). Вывод прогона: протокольная ломка на кит не падает (кит MCP не реализует), срочности нет до 2027-07-28, но реестр Deprecated (https://modelcontextprotocol.io/specification/2026-07-28/deprecated) ставится на квартальный watch — у транспорта HTTP+SSE срок наступает раньше всех.

**Рекомендация.** Один узкий RR стоит: «влияние MCP 2026-07-28 на MCP-контур кита» — датированное, ломающее, спеко-уровневое изменение с 12-месячным окном миграции. controlkeel и ai-devkit — не отдельный RR, а довесок к следующему конкурентному скану (EV-660/EV-663 пора обновлять: они пропустили двух соседей). Остальное — наблюдать.

---

## 2026-08-05 (окно — 2026-07-30…08-05)

**Топ-находка недели: жанр перестал быть пустым.** За семь дней вышло три содержательных проекта, повторяющих части тезиса кита, и все под permissive-лицензией. [QiQi14/multi-agents-control-plane](https://github.com/QiQi14/multi-agents-control-plane) (Apache-2.0, создан 2026-07-30, 5★) описывает себя дословно как «a task state machine with contracts, receipts, risk gates, and evidence, so that planning, execution, and review are separate roles with separate authority» — это одновременно gates, delivery-receipt, evidence-артефакты и writer≠judge в одном продукте; схемы объявлены нестабильными, v0.2.2. [disler/super-simple-software-factory](https://github.com/disler/super-simple-software-factory) (MIT, создан 2026-07-31, **371★**) — «moving the control plane out of the prompt and into Python», «Gates become the definition of done», типизированные envelope на стыках фаз, трейс в SQLite; в отличие от прочих соседей имеет реальный канал распространения (один Claude Code skill + YouTube-разбор). [0xwilliamortiz/ratchet](https://github.com/0xwilliamortiz/ratchet) (MIT, создан 2026-07-31, **430★**) — не конкурент, а **механизм, которого у кита нет**: хук PostToolUse измеряет каждую правку и возвращает вердикт в живую сессию; их формулировка проблемы («every minimalism ruleset for coding agents is an open loop») — прямой аргумент против гейтов, срабатывающих только на ревью.

**Харнесс под китом (Claude Code 2.1.221→2.1.222).** Две починки классов **обхода разрешений**, а не косметика: (1) zsh мог исполнить скрытые команды внутри regex-условия `[[ ]]`, минуя проверку Bash-инструмента; (2) PreToolUse-хуки с auto-allow обходили ограничения инструментов в фоновых агентных задачах (summaries, compaction, renames). Плюс: worktree-изолированные сессии и их субагенты могли запускать деструктивные git-команды по основному чекауту — изоляция теперь распространяется на правки файлов и Bash во всех типах сессий; `/fork` создаёт собственный worktree; появился `mode: "mask"` для sandbox-credential-файлов (Linux/WSL); удалена фича ultraplan ([CHANGELOG](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)). **Вывод для кита:** если гейты кита опираются на хуки и permission-слой харнесса, то на версиях до 2.1.221/222 у них были документированные обходы — это довод в пользу минимальной поддерживаемой версии харнесса в capability-декларациях, а не только «хуки настроены».

**Пополнение охвата (не новинки окна, пропуски прошлых сканов).** [fluidbox](https://github.com/hrishikeshdkakkad/fluidbox) (MIT, создан 2026-07-10, 11★) — Rust control plane с policy-gated tools, human approvals и append-only audit ledger. Плюс маркер зрелости жанра: появился [awesome-agent-control-plane](https://github.com/Ar9av/awesome-agent-control-plane) (CC0, 2026-08-02) — у категории завёлся awesome-список, то есть она получила имя.

**Проверка прошлых evidence — расхождений не найдено, три подтверждены заново:**
- EV-662 (OpenHands: gates/транзакционная доставка не документированы) — **в силе**: релизы v1.8.0 (07-30) и v1.9.0 (08-03) — extension-manifest host, живая активность агента в чате, статусы CANCELLED/SKIPPED у automations, обновление AGENTS.md-гайдлайнов; ни policy gates, ни delivery-транзакции ([releases](https://github.com/OpenHands/OpenHands/releases)).
- EV-663 (Rel(AI)Build — публичного репозитория нет) — **в силе**: поиск по GitHub по-прежнему `total_count: 0`; Galileo Agent Control в открытых GitHub-организациях не находится.
- EV-804 (Microsoft AGT: 7/10 Full + 3/10 Partial) — **в силе**: [док](https://github.com/microsoft/agent-governance-toolkit/blob/main/docs/compliance/owasp-agentic-top10-architecture.md) версии 1.1, last reviewed 2026-07-11, дословно «Official ASI coverage: 7/10 Full, 3/10 Partial, 0 Gaps».

**Спокойные направления.** AGENTS.md — ломающих изменений нет: [agentsmd/agents.md](https://github.com/agentsmd/agents.md) последний push всё ещё 2026-03-12; конвенция кита в безопасности. AAIF — спековых новостей нет, из нового только пост «The EU AI Act and the New Rules for Building AI Agents» (Angie Jones, 2026-08-02, [индекс блога](https://aaif.io/blog)) — регуляторный контекст, не инструмент. ECC — граница MIT/Pro **не двигалась**: в README дословно «This repo is MIT-licensed forever. ECC Pro is the hosted GitHub App for private repos», «Private repos from $19/seat/mo»; за окно ~15 коммитов, из релевантного — свои же починки обхода хуков (`fix(hooks): catch the bypass short flag anywhere in a cluster` #2668, `never format installed plugin and marketplace clones` #2667). ruflo — прежний churn, v3.32.37…v3.34.0 за окно, MIT без изменений; содержательного два пункта: слой защиты выборки AgentDB от memory poisoning (ADR-377, со ссылкой на arXiv:2606.12703) и интеграция с AGNTCY/Outshift (ADR-378/380: `ruflo transport use slim`, `agent publish`, `swarm join`) — сосед начал подключаться к внешнему стандарту интероперабельности. Стандарты evals/gates — нового не принято: IETF draft-han-bmwg-agent-security-benchmark по-прежнему **версия 00 от 2026-07-05, рабочей группой не адоптирован**, «no formal standing in the IETF standards process» ([datatracker](https://datatracker.ietf.org/doc/draft-han-bmwg-agent-security-benchmark/)); у OWASP «State of Agentic AI Security and Governance» августовского обновления не подтвердилось (страница даёт 2.01 от 2026-06-01). Открытые модели — принятых решений не ломает: вышел официальный [DeepSeek-V4-Flash-0731](https://huggingface.co/deepseek-ai/DeepSeek-V4-Flash-0731) вместо preview, лицензия по-прежнему MIT, заявлен рост именно агентных способностей (важно для DP-101, если он ссылался на preview); у Qwen и t-tech за окно новых базовых релизов нет (T-lite/T-pro — Apache-2.0 без изменений, DP-106 в силе).

**Рекомендация.** **Один RR стоит завести — узкий, конкурентный:** «обновление охвата EV-660/EV-663 по трём новым соседям (multi-agents-control-plane, super-simple-software-factory, fluidbox) + ratchet как механизм in-session-проверки правил». Причина не в угрозе, а в том, что EV-660 — негативное утверждение «связной цепочки не обнаружено», и за неделю против него появилось три записи, одна из которых воспроизводит формулировку почти дословно. Второй, отдельный и более срочный по существу пункт: **проверить, не опирались ли гейты кита на обойдённые до 2.1.221/222 механизмы харнесса** — это не RR, а инженерная задача с минимальной поддерживаемой версией. Остальное — наблюдать.

**Постскриптум (тот же день) — обе рекомендации выполнены.**

1. **RR заведён:** RR-013 «Соседи по жанру, август 2026: жив ли ещё вывод EV-660 о ненайденной связной цепочке» (status draft, deadline 2026-08-21, required_confidence medium). Скоуп — читать код и артефакты трёх новых соседей, а не README; отдельный вопрос про ratchet как заимствуемый механизм и отдельный про то, как честно переформулировать EV-660.

2. **Инженерная проверка «опирались ли гейты кита на обойдённые механизмы харнесса» — выполнена, ответ отрицательный, с одной оговоркой.** Гейты кита харнесс-хуками не реализованы вообще: `find -iname "*hook*"` по репозиторию (кроме `.research/`) не даёт ни файла, гейты в `quality/gates.yaml` — это runnable-валидаторы (`validation/*.py`, `tools/*.py`), вызываемые воркфлоу; ограничение инструментов — свой `tools/tool_broker.py`, а не permission-слой харнесса; изоляция — свои контейнеры с одноразовым клоном (`containers/run-sandboxed.sh`, EV-910) и свои git worktree (`tools/worktree.py`). Поэтому починка PreToolUse-обхода в фоновых задачах (2.1.222) кита не задевает, а worktree-фикс относится к собственным изолированным сессиям харнесса, не к нашим git-worktree.
   Класс «скрытая команда внутри условного выражения» (zsh, починен в 2.1.221) на брокер **не транслируется** — проверено вызовом `Policy.decide` на векторах: `[[ -n *(e:cmd:) ]]` → deny (`['[[']` не в allowlist), `if cmd; then` → deny, `c=touch; $c …` → deny, `op=git` со вторым сегментом → deny. Плюс брокер исполняет через `subprocess(shell=True)`, то есть `/bin/sh`, где zsh-квалификаторов glob нет.
   **Оговорка (реальный, но небольшой дефект).** В `SANDBOX_SHELL_ALLOWLIST` три бинаря отнесены к группе с комментарием «безопасное чтение/навигация (модель исследует репо)» — `awk`, `sed`, `find` — но у всех трёх есть штатное исполнение команд: `awk 'BEGIN{system("…")}'` → ALLOW, `sed '1e …'` → ALLOW, `find . -exec … \;` → ALLOW (проверено на дефолтном `sandbox_policy()`, то есть при `allow_network=True`). Честная декларация кита про «инструменты, исполняющие код репо по своей природе» покрывает `python3/node/make/npm/pytest`, но эти три лежат не в той группе и описаны неверно. Гарантия при этом не пробита: настоящая граница — контейнер (read-only rootfs, смонтирован только одноразовый клон), и кит это прямо заявляет. То есть дефект — в **классификации и комментарии**, а не в границе; правка на один комментарий и перенос трёх строк в соседнюю группу.
   Побочно: первая проба дала ложное «всё deny», потому что `NETWORK_RE` ловит слово `curl` в любом месте строки — вердикты приходили за payload, а не за разбор конструкта. Урок для будущих проб брокера: adversarial-вектор не должен содержать сетевого слова, иначе проверяешь денайлист вместо allowlist.

## 2026-08-12 (окно — 2026-08-05…08-12)

**Топ-находка: жанровый сосед, родившийся внутри окна.** [thiagocorreanet/mestre-yoda](https://github.com/thiagocorreanet/mestre-yoda) (MIT, TypeScript, создан 2026-08-05, 7★) описывает себя как «a deterministic, observable Spec-Driven Development harness for AI coding agents. Reliable workflows, **enforceable gates, auditable evidence, and human-controlled delivery**» — четыре элемента тезиса кита в одной строке. Рядом — [AgentPanelX](https://github.com/aowo-1345/AgentPanelX) (MIT, 08-10, 7★, «local-first control plane… plan, govern, and **recover** long-running delivery») и [ACP](https://github.com/yacine-kellib/agent-control-plane) (Apache-2.0, 08-10, 3★) — последний интересен не конкуренцией, а методом: спека + **доказательства на Dafny** + «every claim replays locally». Фон: за неделю создано ~6.4k репозиториев с упоминанием claude-code, из них десятки — однозвёздочные «governance kit / evidence gates»; жанр коммодитизируется, порог входа в риторику кита упал до README.

**Расхождение — не с EV-663, а с брифом прошлой недели.** *(Уточнение, внесено в тот же день после сверки с файлом evidence: EV-663 ошибочного утверждения не содержит — он называет Galileo Agent Control жанровым соседом и «open source control plane», что подтвердилось; неверной была проверочная строка брифа 2026-08-05 «в открытых GitHub-организациях не находится», потому что поиск шёл по `org:rungalileo`. EV-663 остаётся active.)* Публичный код Galileo Agent Control **существует**: [agentcontrol/agent-control](https://github.com/agentcontrol/agent-control), Apache-2.0, создан 2026-01-30, 289★/44 форка, релиз v8.4.0 (2026-07-27), push 2026-08-11. Прошлый скан искал по `org:rungalileo` и потому не нашёл. Часть EV-663 про Rel(AI)Build **в силе**: поиск по GitHub по-прежнему `total_count: 0`.

**EV-672 остался без источника: ratchet исчез.** Репозиторий `0xwilliamortiz/ratchet` (431★ на 2026-08-05) и **весь аккаунт владельца** отдают 404 (проверено GitHub API и прямым HTTP; поиск по дословной формулировке описания — 0 результатов, форк-наследника нет). Механизм in-session-гейта, записанный в EV-672 и в скоуп RR-013, больше нельзя прочитать в первоисточнике — evidence надо пометить, а вопрос RR-013 про ratchet либо снять, либо переформулировать на живые аналоги.

**Подтверждено заново.** EV-662 (OpenHands: гейтов и транзакционной доставки нет) — **в силе**: релизы v1.10.0…v1.12.0 (08-05…08-07) это стоимость LLM за прогон в Activity Log, экспорт лога, витрина automations, дефолтная модель Canvas; политик и доставки нет. EV-804 (Microsoft AGT) — **в силе по документу**: тот же дословный «Official ASI coverage: 7/10 Full, 3/10 Partial, 0 Gaps», версия 1.1, last reviewed 2026-07-11; **но осторожность**: описание самого репозитория теперь маркетирует «Covers 10/10 OWASP Agentic Top 10» — ссылаться нужно на док, не на шапку. ECC — граница MIT/Pro **не двигалась** (дословно «This repo is MIT-licensed forever… Private repos from $19/seat/mo»), 38 коммитов за окно, содержательного для кита нет. ruflo — прежний churn: v3.35.0…v3.38.0 за двое суток, MIT.

**Стандарты.** AGENTS.md — **тихо**: 0 коммитов за окно, предложение v1.1 (issue #135) открыто и не двигалось с 2026-06-11, ломающих изменений нет. У AAIF за окно новых постов нет (последний — EU AI Act, 08-02). Новое для watch-списка: формат **Agent Skills** оформлен как открытый стандарт — [agentskills/agentskills](https://github.com/agentskills/agentskills) (Apache-2.0, 24.2k★, «Specification and documentation for Agent Skills», изначально Anthropic), витрина клиентов от Claude Code и Codex до OpenHands и goose. Это второй внешний стандарт после AGENTS.md, от которого зависит форма артефактов кита; за окно менялась только витрина, спека — нет. Evals — нового стандарта не принято; в охват добавлен пропущенный [supabase/evals](https://github.com/supabase/evals) (Apache-2.0, анонс 08-01): вендор публикует свой бенчмарк, гоняющий кодовые агенты на реальных задачах платформы.

**Харнесс под китом: 2.1.222 → 2.1.228, снова про обходы разрешений.** Существенное: обход проверки Bash/PowerShell через **табуляцию и Unicode-трюки**; workflow-скрипты уходили из песочницы через динамический `import()`; `bypassPermissions` игнорировал org-политику запрета (все — 2.1.223); **завершающий слэш обходил filesystem deny-записи** на Linux/macOS (2.1.224); скиллы, синхронизированные из claude.ai, могли затенять локальные команды и исполнять `!`-команды (2.1.224/228) ([CHANGELOG](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)). Класс «padding внутри команды» прошлой неделей у нас **не проверялся** — проверяли только zsh-условия.

**Открытые модели — принятых решений не задевает.** За окно новых базовых релизов нет: DeepSeek — последний `DeepSeek-V4-Flash-0731` (MIT, 07-31, уже учтён), Qwen — новых базовых нет (Apache-2.0), t-tech — последнее `T-Search` (Apache-2.0, июль), DP-106 в силе. Оговорка на будущее: Kimi K3 на HF идёт под `license:other` («Kimi K3 License» с условиями для крупных коммерческих сервисов) — в DP-101 он фигурирует только как ценовой ориентир, но как поставщик весов он **не permissive**.

**Рекомендация (исходная).** Три пункта прицепить к RR-013: mestre-yoda в скоуп, поправка по Galileo, снятие вопроса про ratchet. Отдельно, вне RR, инженерная проверка: прогнать `tools/tool_broker.py` на векторах с табуляцией/Unicode-паддингом и с завершающим слэшем в путях — ровно те классы, которые харнесс чинил в 2.1.223/224.

**Постскриптум (тот же день) — выполнено, с одной поправкой к самой рекомендации.** RR-013 оказался **не draft, а `answered`**: он закрыт четырьмя evidence (EV-670…673), из которых EV-673 суперсидит EV-660. Дописывать скоуп в закрытый запрос нельзя, поэтому вместо этого:
1. **Заведён RR-014** (status draft, deadline 2026-08-28, required_confidence medium, workflow_hint quick) — узкое продолжение: читать *кодом* Galileo Agent Control и mestre-yoda и решить, меняет ли расширенная выборка формулировку EV-673. Вопрос про in-session-гейт в него **не перенесён** — источник исчез.
2. **EV-1101** — публичный Apache-2.0 код Galileo (agentcontrol/agent-control, 289★, v8.4.0), с зафиксированным `conflicts_with` на ошибочную строку брифа 2026-08-05. Пункт EV-663 про отсутствие публичного репозитория у Rel(AI)Build перепроверен — в силе.
3. **EV-1102** — исчезновение источника ratchet, проверенное четырьмя способами; **EV-672 переведён в `status: stale`** со ссылкой на EV-1102: механизм не опровергнут, он стал непроверяемым.
4. **Проверка брокера выполнена — и в отличие от прошлой недели нашла настоящий дефект.** Класс «padding внутри команды» там, где ходит живая модель (`sandbox_policy`, режим allowlist), **не переносится**: таб-разделители, подоболочка, `exec`-обёртка, backslash- и zero-width-обфускация сетевого слова — всё deny посегментной проверкой бинарников. Дефект нашёлся во **втором** классе, аналоге trailing-slash обхода: `protected_paths` сверяются строковым префиксом без `normpath`, поэтому `./migrations/destructive/x`, `migrations//destructive/x`, `migrations/./destructive/x` (и `Migrations/…` на macOS) правило не накрывают. Проверено не вердиктом, а сквозь `execute()` на временном репозитории: канонический путь — deny и файл цел, четыре переписанных — **ALLOW и файл перезаписан**. Записано в реестр кита как **R-37 (P0, открыт)**; побочно **R-38 (P3)** — денайлисты `NETWORK_RE`/`GIT_PUSH_RE` обходятся backslash-escape и переносом строки, но только при `shell_mode="unrestricted"`, которого у живой модели нет. Вывод для watch-а: смотреть на чужие починки стоит не ради заимствования кода, а как на список классов, которые надо прогонять по своей границе — второй раз подряд именно это и сработало.
