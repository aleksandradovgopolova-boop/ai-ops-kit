# Freshness sweep log

Еженедельный freshness-свип research-модуля. Append-only.

## 2026-07-30

- Горизонт: 14 дней. `freshness_sweep.py --days 14` → exit 0.
- expired: 0 | expiring (≤14д): 0 | superseded: 0 | продлено: 0
- Затронутые DP: нет.
- Валидатор `validate_research_artifacts.py`: OK (errors=0, warnings=7 — предсуществующие quote-grounding WARN по EV-415/416/419/421/422/535/708, не связаны с этим прогоном).
- Итог: всё свежо, правок нет.

## 2026-08-14

- Горизонт: 14 дней. `freshness_sweep.py --days 14` → exit 0 (`today: 2026-08-14`).
- expired: 0 | expiring (≤14д): 0 | superseded: 0 | продлено: 0
- Покрытие: 178 EV, у всех задан `freshness.expires_at`, все в статусе `active`; ближайшее
  истечение — 2026-09-22 (≈39 дней), за пределами окна. Следующая волна попадёт в окно
  свипа примерно 2026-09-08.
- Затронутые DP: нет.
- Валидатор: OK (errors=0). Warnings 7 → 1 после ревизии quote-grounding (ниже).
- Грабли прогона (важно для будущих Watch): системный `python3` — homebrew 3.14 без `pyyaml`,
  на нём и `freshness_sweep.py`, и `verify_quotes.py` падают на `import yaml`. Запускать
  строго через venv кита: `/Users/sasad/.venvs/ai-ops-kit/bin/python3`. Путь валидатора —
  `ai_ops_kit/validation/validate_research_artifacts.py` (не `validation/…`), и ему нужен
  `PYTHONPATH=/Users/sasad/ai-ops-kit`. Проверять статус без пайпа: `cmd | tail` возвращает
  статус `tail`, из-за чего несуществующая команда выглядит как `EXIT=0`.
- Ревизия quote-grounding (по запросу владельца, вне штатных шагов свипа): в EV-415, EV-416,
  EV-419, EV-421, EV-422, EV-535 добавлены verbatim `citation.quote`, снятые живым re-fetch
  источников; уточнены `locator`. `verify_quotes.py` по этим шести: `quote_match`, score=1.00.
  - EV-535: источник перевязан с блога Fabric (403 + бот-защита Cloudflare, цитированию
    недоступен) на Microsoft Learn `fabric/fundamentals/copilot-enable-fabric`, где порог F2
    цитируется дословно. Дата 30.04.2026 и прежний порог F64/~$5,250 остаются НЕподтверждёнными.
  - EV-422: источник не подтверждает часть statement — флагман GigaChat3.1-702B-A36B и
    MMLU RU 0.8267 на странице отсутствуют (там MMLU RU 5-shot у 20B-A3B = 0,598), «обучен
    с нуля» не заявлено. Зафиксировано в `reliability.rationale`; statement не правился.
  - EV-708 — единственный оставшийся WARN, осознанно: statement — агрегат grep-подсчёта по
    ~279 файлам SKILL.md, дословной цитатой такие числа не заземляются. Требует либо
    воспроизведения подсчёта на пиннутом коммите, либо явного исключения в конвенции.

## 2026-08-03 (запись отозвана)

Запись за эту дату была ошибочной и удалена. Она сообщала «валидатор OK» и результат свипа,
но обе команды в реальности не исполнялись: `freshness_sweep.py` падал на `import yaml`,
а путь `validation/validate_research_artifacts.py` не существует; дата в записи (2026-08-03)
не совпадала с датой прогона. Фактическое состояние на 2026-08-14 — в записи выше.

## 2026-09-10

- Горизонт: 14 дней. `freshness_sweep.py --days 14` → exit 2 (`today: 2026-09-10`).
- expired: 0 | expiring (≤14д): 3 — EV-104, EV-106 (обе 2026-09-22), EV-1102 (2026-09-12)
  | помечено stale: 0 (expired не было, `--mark-stale` не запускался)
  | superseded: 2 | продлено: 1
- **EV-1102 продлён до 2026-10-11** (исходный горизонт 31 день). Отрицательное утверждение
  «репозиторий `0xwilliamortiz/ratchet` и аккаунт владельца исчезли» переподтверждено всеми
  четырьмя исходными способами: `/repos/…` 404, `/users/…` 404, прямой HTTP с редиректами 404
  (`url_effective` без перенаправления), поиск `in:description` по дословному описанию
  `total_count 0`. Важно для будущих Watch: у такого EV `verify_quotes.py` даёт `fetch_fail`,
  и это НЕ провал проверки — 404 здесь и есть подтверждаемый факт; статус проверять глазами,
  а не по счётчику.
- **EV-104 → superseded_by EV-107** (цена изменилась). Ре-фетч листинга OpenRouter показал у
  ОФИЦИАЛЬНОГО провайдера Moonshot AI `$0.95 / $4.00 / $0.16` вместо `$0.66 / $3.41`. Пара
  `$0.65 / $3.41` на странице есть, но принадлежит стороннему CoreWeave — то есть либо цена
  выросла, либо EV-104 изначально приписал Moonshot чужую строку таблицы; задним числом по
  внешним данным это не различить, ограничение записано в `reliability.rationale` EV-107.
  Побочный результат: одно из двух `unresolved` противоречий EV-104 закрыто — обзор с
  `$0.95 / $4.00` оказался прав. Контекст 262,144 подтверждён и сохранён. `statement` EV-104
  не правился.
- **EV-106 → superseded_by EV-108** (пересчёт по той же формуле `0.1M*output + 0.2M*input`).
  Изменилось только слагаемое k2.6: `$0.47 → $0.59` (+25%); deepseek-v4-flash `$0.06`,
  v4-pro `$0.17`, k3 `$2.10` не изменились (EV-101/EV-103 годны до 2026-10-22 и живым
  re-fetch в этом прогоне НЕ перепроверялись — так и записано). Разброс `~35x → ~37x`.
  В EV-108 добавлен второй ориентир: самый дешёвый маршрут OpenRouter (Chutes `$0.58/$3.40`)
  даёт `≈$0.46/книга`, то есть официальный API к K2.6 — не самый дешёвый доступ к модели.
- Затронутые DP: **DP-101 опирается на устаревшие EV-104 и EV-106** (оба в `evidence_ids`,
  цитируются в 5 местах: обоснование, `appropriate_when` у двух альтернатив и в рисках):
  рекомендация может требовать пересмотра. Сам DP не правился — решение за человеком.
  Для человека: направление рекомендации (deepseek-v4-flash) новые цены не разворачивают —
  K2.6 стал ещё дороже и потому ещё сильнее «доминируемым», — но числа `~35x` и `≈$0.47`
  внутри DP-101 теперь неверны, и строка рисков «при истечении пересчитать EV-106» этим
  прогоном как раз отработана. DP-101 в статусе `draft`.
- Валидатор `validate_research_artifacts.py`: OK (errors=0, warnings=9 — предсуществующие
  quote-grounding WARN по EV-708/1121/1124/1126/1128/1162/1166/1167/1168; ни один не связан
  с правками прогона). `verify_quotes.py --only EV-107`: `quote_match`, score=1.00.
- Контрольный повтор свипа после правок: `expired: []`, `expiring: []`, **exit 0**.
- Ничего не коммичено (ограничение задачи): правки лежат в рабочем дереве.

### 2026-09-10, дополнение: DP-101 обновлён по решению владельца

Строка выше «Сам DP не правился — решение за человеком» относится к штатному шагу свипа.
Сразу после прогона владелец решил обновить пакет; правки внесены в тот же день:

- `evidence_ids`: EV-104 → EV-107, EV-106 → EV-108 (EV-101/102/103/105 без изменений).
- Числа: `≈$0.06/книга` без изменений, разброс `~35x → ~37x`, ссылки в обоснованиях и в двух
  `appropriate_when` перевязаны на EV-108.
- Альтернатива kimi-k2.6 переписана по существу, а не только по ссылкам: официальная цена
  даёт `≈$0.59/книга`, что дороже v4-pro в 3,4 раза и **выше собственного потолка допущений
  `~$0.5/книга`**. То есть K2.6 через официальный API теперь не проходит по юнит-экономике
  этого DP, а не просто «доминируется»; вход в него остаётся только через сторонний маршрут
  OpenRouter (`≈$0.46/книга`) — с зависимостью от стороннего провайдера.
- Риски: строка о пересчёте закрыта как отработанная и указана следующая точка (2026-10-22)
  с честной оговоркой, что цифры deepseek и K3 держатся на сроке годности EV-101/EV-103, а не
  на свежем замере; добавлен риск необъяснённого расхождения цены K2.6 с июльским замером.
- Unknown про официальную цену K2.6 сохранён и уточнён: $0.95/$4.00 — строка официального
  провайдера у агрегатора, публикации вендора по-прежнему нет.
- Рекомендация (deepseek-v4-flash), `confidence: medium` и `status: draft` НЕ менялись:
  новые цены направление решения не разворачивают.

Грабли прогона (для будущих Watch): валидатор проверяет не только `evidence_ids`, но и
упоминания `EV-\d+` В ТЕКСТЕ пакета — исторические ссылки на вытесненные EV в `risks` дали
`ERROR: EV-104 упомянут в тексте, но отсутствует в evidence_ids`. Правильный приём —
описывать вытесненные данные словами («истекавшие ценовые данные по K2.6»), а не их id:
иначе DP приходится формально опирать на superseded-эвиденс, что следующий свип снова
поднимет как «DP опирается на устаревший EV». Валидатор после правок: errors=0, warnings=9
(те же предсуществующие quote-grounding WARN).
