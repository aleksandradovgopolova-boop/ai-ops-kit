# AI Ops Kit — development dependencies (v3.28.0 R17)
# Install: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest>=7.0
pytest-cov>=4.0
pytest-xdist>=3.0     # параллельный прогон (-n auto): полный набор 11мин -> ~5мин, набор параллель-безопасен
hypothesis>=6.0

# Linting
ruff>=0.8.0

# Type checking
mypy>=1.0

# Pre-commit
pre-commit>=3.0
