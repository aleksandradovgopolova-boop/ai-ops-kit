# AI Ops Kit

**AI-операционная система для продуктовой команды, которая не умеет притворяться, что «готово».**

Кит живёт в репозитории продукта и ведёт его по полному циклу — от намерения до измеренного
результата: discovery → delivery → release → measurement → следующий цикл. Он организует работу
людей и AI-исполнителей, проверяет качество по риску и — главное — не выдаёт утверждение без
доказательства.

---

## Зачем это

AI-агент собирает маленький greenfield за вечер. Но по мере роста продукта решения откладываются
на худший момент и принимаются тем, у кого меньше всего контекста, а «сделано» перестаёт означать
«работает». Кит переносит нагрузку туда, где она дешёвая: человек принимает продуктовые решения с
полным контекстом, Кит детерминированно исполняет уже решённое и доказывает каждый шаг.

## Принципы

- **No evidence → no claim.** Каждый существенный вывод несёт источник и статус
  (увидел / вывел / спросил / не знаю). Догадка не публикуется как факт.
- **Fail-closed.** Сбой блокирует и не даёт ложный green.
- **Writer ≠ Judge.** Тот, кто сделал, не закрывает собственную проверку.
- **«Исправлено в ките» = «работает у владельца».** Проверки доезжают до установленной копии,
  а не до чтения кода.
- **Роль, а не исполнитель.** Смена Claude Code на Codex не переписывает план.
  Провайдеры: Anthropic, OpenAI, DeepSeek, Claude CLI, локальные модели.

## Что Кит умеет сегодня

Единственный достоверный источник — машинная карта, **выведенная из кода и реестров**, а не
написанная руками: **[docs/capability-map.md](docs/capability-map.md)**. Там у каждой возможности
состояние built / built≠wired / partial / planned и способ проверки.

Коротко: 32 команды владельца (`status`, `inbox`, `next`, `explain`, `do`, `run`, `review`,
`contract`, `model` …), 35 <!-- claim:gates-total --> quality-gate (21 блокирующих, 14 advisory),
42 <!-- claim:agents-total --> роли в реестре, детерминированный security-scan.

## Быстрый старт (60 секунд)

```bash
python3 installer/ai_ops.py init /path/to/project
cd /path/to/project
./ai-ops model .          # что за продукт, что известно, чего Кит не знает
./ai-ops next             # что взять следующим и почему
./ai-ops run "описание задачи" --execute
```

Полный первый путь — **[docs/QUICKSTART.md](docs/QUICKSTART.md)**.

## Честно о границах

- **Установка пока ручная.** Сценарий «сказал „Установи AI Ops“ в незнакомом репозитории» ещё
  не first-class — это следующий шаг ([ROADMAP.md](ROADMAP.md)).
- **Канал — `qualification`, не `stable`.** Текущая версия — **v4.1.0 qualification**. `stable`
  требует полевых доказательств на двух живых дочках и проверяется машиной; пока обкатки нет —
  слово не выдаётся авансом.
- **Часть intelligence-слоя построена, но не проведена в контур** (organizational learning,
  outcome-аналитика, ночной watch). См. раздел built≠wired в карте возможностей.

## Для кого

Продуктовая команда от одного человека до десятков: PM, аналитик, дизайнер, разработчик,
основатель. Знать внутреннюю механику (workflow, агент, модель, гейт) не нужно — Кит выбирает
способ сам и объясняет результат человеческим языком.

## Где что в репозитории

В корне много каталогов, но искать по ним просто — почти всё это **данные** (реестры, стандарты,
записи), а собственно кода — два каталога. Ориентир по тому, что вы ищете:

| Ищете | Смотрите |
|---|---|
| Код: движок и установщик | `ai_ops_kit/` · `installer/` |
| Источник истины — данные, из которых всё выводится | `registry/` · `schemas/` · `manifest/` · `presets/` · `config/` |
| Проверки качества, гейты и ратчеты | `quality/` · `packages/` · `tests/` · `evaluations/` |
| План, решения, история работ | `planning/` · `decisions/` · `history/` · [ROADMAP.md](ROADMAP.md) |
| Что кит требует: стандарты и правила | `standards/` · `rules/` · `governance/` · `security/` |
| Прозаический слой: инструкции агентам, команды, шаблоны | `agents/` · `commands/` · `workflows/` · `templates/` · `context/` |
| Документация для человека | [`docs/`](docs/index.md) · `platform-guides/` |
| Кит как плагин Claude Code и скиллы | `plugin/` · `skills/` |
| Обкатка, находки, исследования | `qualification/` · `findings/` · `research/` · `product-learning/` · `examples/` |
| Записи для CHANGELOG (по одной на изменение) | `newsfragments/` |

Полная карта для тех, кто работает над самим китом, — в [AGENTS.md](AGENTS.md) (раздел «Карта
репозитория»); исчерпывающий пофайловый список — в [FILE_INDEX.md](FILE_INDEX.md).

## Дальше

- **Вся документация — [docs/index.md](docs/index.md).** Одна дверь: четыре уровня (START / USE /
  EXTEND / ARCHITECT) — найдите строку по тому, что делаете сейчас, и идите по ссылкам.
- Полевые находки и история разработки — **[DEVELOPMENT-HISTORY.md](DEVELOPMENT-HISTORY.md)**
- Куда идёт продукт — **[ROADMAP.md](ROADMAP.md)**
- Организующий тезис — **[VISION.md](VISION.md)**

MIT.
