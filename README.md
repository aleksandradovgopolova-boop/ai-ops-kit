# AI Ops Kit

Открытая **AI Product Operating System** для продуктово-технологических команд.

AI сопровождает продукт на всём жизненном цикле: Discovery → Delivery → Release → Measurement → Insights.

## Quick Start

```bash
# 1. Установка в репозиторий (пока — вручную, см. ниже про zero-touch)
python3 installer/ai_ops.py init /path/to/project
cd /path/to/project

# 2. Дальше всё через точку входа, которую положил установщик
./ai-ops model .          # что за продукт: класс репозитория, контуры, чего кит не знает
./ai-ops bootstrap        # первое направление и план из фактов аудита (--apply — записать)
./ai-ops next             # что взять следующим и почему

# 3. Работа
./ai-ops run "описание задачи" --execute
./ai-ops status           # что идёт прямо сейчас
```

Обёртка `./ai-ops` сама находит пригодный интерпретатор (признак — `import yaml`); если ни одного
нет, она скажет об этом и назовёт команду, а не упадёт трейсбеком. Явно задать —
`AI_OPS_PYTHON=/путь/к/python3`.

**Честно про установку:** шаг 1 — ручной. Сценарий «открыл незнакомый репозиторий и сказал
„Установи AI Ops“» пока НЕ является first-class путём с чистой машины: фраза подхватывается
скиллом рантайма, а сам кит подключается командой. Это объявленный следующий продуктовый шаг —
см. горизонт «Дальше» в [ROADMAP.md](ROADMAP.md), а не недокументированный пробел.

## Что внутри

| Слой | Содержимое |
|------|-----------|
| **Execution Engine** | `ai-ops run` — единый движок «задача → draft PR» |
| **Quality Gates** | 34 гейта объявлено; на конкретной задаче оценивается её набор (QUICK — 3) |
| **Agents** | 51 роль объявлена в реестре; движок вызывает по имени 10, остальные — контракты владения и ревью |
| **Registry** | Модели, провайдеры, workflow — machine-readable SoT |
| **Security** | 12 доменов, детерминированный scan, human-in-the-loop |
| **Observability** | Метрики стоимости, задач, доставки, моделей |

## Ключевые свойства

- **Spec-First** — модель не запускается, пока спека не достаточна
- **Fail-Closed** — сбой блокирует, не даёт ложный green
- **Writer ≠ Judge** — ревьюер не может закрыть свой гейт
- **Usage Truth** — unavailable ≠ 0, честный учёт стоимости
- **Provider-Agnostic** — Anthropic, OpenAI, DeepSeek, Claude CLI, локальные модели

## Версия

**v3.36.8 stable** — Revision Truth

Ревизия репозитория: проверки, которые были ОБЪЯВЛЕНЫ, но не исполнялись. Полный реестр находок
с доказательствами — [docs/audit-report.md](docs/audit-report.md).

- линтер исполняется: 40 653 строки движка не линтовались ни в CI, ни в pre-commit — ни разу;
  набор из 30 правил класса «поломка», по нему ноль
- тест обязан УТВЕРЖДАТЬ: шесть проверок проходили независимо от результата (`return` вместо
  `assert`), то есть четыре валидатора можно было сломать при зелёном контуре
- пять точек входа из 103 падали с `NameError` при любом запуске — функция удалена в v3.30,
  вызовы остались
- две ЗАЯВЛЕННЫЕ проверки существовали как код и не исполнялись: кросс-сверка data-residency с
  реестром провайдеров и `--selftest` в 11 модулях
- обёртка `./ai-ops` выбирает интерпретатор по способности работать, а не по имени: прежде на
  машине с несколькими python владелец получал трейсбек на ПЕРВОЙ команде при зелёном `doctor`
- «не смог прочитать» больше не выдаётся за «нет»: битая DeliveryReceipt, нечитаемый workitem,
  непрочитанный реестр гейтов — каждый называет себя, а не маскируется под отсутствие
- утрата служебной записи (usage, lifecycle-журнал) стала ВИДНОЙ в отчёте: fail-open сохранён,
  но занижённая стоимость больше не подаётся как факт
- покрытие стало гейтом с порогом-ратчетом (пол 70% при замере 71%), а не метрикой без последствий
- репозиторий выполняет свои же контракты: `ROADMAP.md` — четыре горизонта, `planning/plan.yaml`
  существует, и `ai-ops next` отвечает на самом ките

Предыдущий канал — v3.36.7 Qualification Readiness · Clean Update:

- онбординг заканчивается РАБОТОЙ, а не документацией: `ai-ops bootstrap` собирает направление и
  план из фактов аудита, и `ai-ops next` сразу советует, за что взяться и почему
- то, чего кит знать не может, помечено «нужно ваше слово», а не заполнено правдоподобным текстом
- репозиторий описывает СЕБЯ: где лежит план, какие пути считать сигналом — и какой сигнал кита
  снять, если он не про ваш проект (`entities/` во Frontend-Sliced Design — не модель данных)
- с человеком говорят ВСЕ команды, а не четыре: наружу выходит смысл, внутренние имена остаются
  в технических деталях — и «проверено» не путается с «проверять было некому»
- репозиторий знает, КУДА идёт: `ROADMAP.md` (четыре горизонта) стал проверяемым артефактом
- и что брать СЛЕДУЮЩИМ: `ai-ops next` отвечает на четыре вопроса — где мы, что идёт, что
  блокирует и чем, какую работу взять и ПОЧЕМУ, что можно вести одновременно и какими ролями
- «Установи AI Ops» стало сценарием: `ai-ops model` изучает репозиторий, определяет его класс,
  восстанавливает доказуемое и спрашивает недостающее ОДНИМ пакетом — только то, что не видно в коде
- у каждого утверждения есть статус и основание: увидел / вывел / спросил / не знаю — догадка не
  публикуется как факт
- нельзя закрыть работу, обновив компонент, если изменилась модель данных: гейт `contour_consistency`
  сверяет затронутые контуры с заявленным (advisory до обкатки)
- «не умею видеть» больше не выглядит как «не менялось»: `unknown` не сворачивается в `not_changed`
- кит говорит с владельцем продукта, а не логом: сначала смысл, техника — по запросу; при этом
  недоказанное называется недоказанным
- план называет РОЛЬ, а не исполнителя: смена Claude Code на Codex не переписывает план продукта

## Документация

- [Architecture](docs/architecture/overview.md)
- [Execution Engine](docs/architecture/execution-engine.md)
- [Quality Gates](docs/architecture/quality-gates.md)
- [Installation Guide](docs/guides/installation.md)
- [Child Repository](docs/guides/child-repo.md)
- [CI Configuration](docs/guides/ci.md)
- [API Reference](docs/api/contracts.md)

## Для разработчиков

```bash
# Установка для разработки
pip install -e ".[dev]"
pre-commit install

# Полный контур перед коммитом (линтер + весь pytest); охват — full-current-python
./scripts/check-full.sh

# Тесты
python3 -m pytest tests/ -q

# Линтер (часть обязательного контура; набор — pyproject.toml [tool.ruff.lint])
ruff check .

# Coverage — ТОТ ЖЕ набор целей, что у гейта в CI (джоба `coverage`, пол 70%)
python3 -m pytest tests/ --cov=ai_ops_kit --cov=installer --cov-report=term-missing

# Benchmarks
python3 tools/bench_performance.py --update-baseline

# Observability
python3 tools/kit_observability.py .
```

## Лицензия

MIT
