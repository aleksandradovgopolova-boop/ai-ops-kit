# AI Ops Kit

Открытая **AI Product Operating System**: ИИ ведёт продукт по всему циклу — Discovery → Delivery →
Release → Measurement → Insights, — а не пишет разовый код. Она для продуктово-технологических
команд, которым нужно, чтобы «сделано» значило «проверено», а не «правдоподобно выглядит». За первые
10 минут вы ставите кит в свой репозиторий, он сам изучает продукт (класс репозитория, контуры, чего
он про вас пока не знает) и предлагает первую работу с планом. Каждое изменение проходит через
quality-гейты с разделением **writer ≠ judge** и приезжает черновым PR — без ложного «green».
Внутренняя сложность кита остаётся внутри: наружу выходит смысл и результат, а не лог.

## За 60 секунд

```bash
python3 installer/ai_ops.py init /path/to/project   # поставить кит в репозиторий
cd /path/to/project
./ai-ops model .                                     # что за продукт: класс, контуры, чего кит не знает
./ai-ops run "коротко опишите задачу" --execute      # задача → черновой PR через гейты
```

Полный первый путь (`init → doctor → onboard → specify → plan → run --execute`) —
в [docs/QUICKSTART.md](docs/QUICKSTART.md).

## Чего пока нет (честно)

Кит доказан на своих дочерних репозиториях, но НЕ на произвольном стороннем; установка (шаг 1 выше)
пока ручная, а не сценарий «открыл незнакомый репозиторий и сказал „поставь AI Ops“»; и главный клин —
организационное обучение (кит копит уроки на исходах и переносит их дальше) — доказывается вживую, а
не объявлен готовым.

## Что кит умеет сегодня

36 <!-- claim:gates-total --> quality-гейтов, 32 команды владельца и 51 роль в реестре. Единая
витрина с состоянием каждой возможности (**built** / **built≠wired** / **partial** / **planned**) и
способом её проверить — [docs/capability-map.md](docs/capability-map.md). Она **выведена из реестров
и кода**, а не написана руками, поэтому не расходится с реальностью молча.

Ключевые свойства: **Spec-First** (модель не стартует без достаточной спеки) · **Fail-Closed** (сбой
блокирует, не даёт ложный green) · **Writer ≠ Judge** (ревьюер не закрывает свой гейт) ·
**Provider-Agnostic** (Anthropic, OpenAI, DeepSeek, Claude CLI, локальные модели).

## Версия

**v4.0.0 qualification** — канал `qualification`, а не `stable`: `stable` требует полевых
доказательств на двух живых дочках именно для этой версии и проверяется машиной. Подробная
инженерная история релизов с деталями — [DEVELOPMENT-HISTORY.md](DEVELOPMENT-HISTORY.md) и
[CHANGELOG.md](CHANGELOG.md).

## Дальше

- Куда идёт продукт (Now/Next/Later) — [ROADMAP.md](ROADMAP.md)
- Для разработчиков самого кита — [AGENTS.md](AGENTS.md)
- Архитектура и установка — [docs/architecture/overview.md](docs/architecture/overview.md),
  [docs/guides/installation.md](docs/guides/installation.md)

## Лицензия

MIT
