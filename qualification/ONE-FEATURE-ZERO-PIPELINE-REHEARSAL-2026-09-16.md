# Репетиция P0 №3: чистая установка + продуктовые поверхности — 2026-09-16

Репетиция (не полный прогон) canonical-сценария
[`ONE-FEATURE-ZERO-PIPELINE-SCENARIO.md`](ONE-FEATURE-ZERO-PIPELINE-SCENARIO.md) на **чисто
установленном** ките (не через `PYTHONPATH` из main). Цель — снять каveat «мерили новым кодом кита
напрямую» на входных границах и выловить баги установки ДО живого внешнего прогона (P0 №7). Вёл кит
автономно; это НЕ засчёт исхода — по решению `ep-2026-09-16` срез засчитывается только живым внешним
прогоном человеком со стороны.

## Метод
Одноразовый репозиторий-подопытный: мини «доска работ» (index.html + app.js + статусы todo/in_progress/
done) — под фичу сценария «быстрый фильтр по статусу». Кит поставлен из текущего main (v4.3.2, 640
файлов) командой `python3 installer/ai_ops.py setup <repo>` (init→doctor→onboard→bootstrap→model,
авто-коммит). Все команды ниже — через установленный `./ai-ops` в репозитории-дочке, БЕЗ `PYTHONPATH`.

## Что прошло чисто (дословно)

| Граница сценария | Поверхность | Что увидел человек | Итог |
|---|---|---|---|
| установка | `setup` | «AI Ops установлен одной командой» + честный раздел «Сделано автоматически» / «Осталось от тебя (секреты и решения — за человеком)»; 640 файлов, авто-коммит | ✅ чисто |
| 1 intent | `specify "…"` | «Поняла: «добавь на доску работ быстрый фильтр по статусу задачи».» + продуктовые вопросы словами + «насколько крупная и рискованная… можно словами» + «потом скажи, что переходим к плану» — без feature id / YAML / CLI | ✅ |
| — | `propose` | «Что нужно продукту… Рекомендую… Дальше… настройка кита — по запросу» — **без «фундамента»** (P0 №1) | ✅ |
| — | `status` | «Сейчас ничего не идёт… Вижу только ЭТУ машину» — без веток/worktree/заявок | ✅ |
| — | `next` | «Что взять следующим… Потому что ценность высокая» — продуктовым языком с обоснованием | ✅ |

Вывод: продуктовые улучшения P0 (№1/№2/№5/№6/№4) рендерятся корректно на РЕАЛЬНОЙ установке, а не
только под `PYTHONPATH`. Входные границы сценария (1, product-surfaces) — без утечек трубопровода.

## Что НЕ проверено репетицией (честная граница)
- Границы 6–14 (run→PR→**Verified-readout**→merge→deploy→analytics→**Outcome-readout**→recommendation)
  требуют реального писательского прогона (`run --execute`, вложенный `claude -p`) и пост-релизных
  данных (deploy/аналитика). У подопытного репо реальной аналитики нет → продуктовый Outcome честно
  будет «ещё не накоплен». Полное закрытие этих границ — это P0 №3 (полный) и P0 №7 (внешний человек).
- Замер вёл кит, не сторонний человек. Сильнейшее доказательство (P0 №7) остаётся за живым внешним
  прогоном человеком со стороны — эту каveat репетиция НЕ снимает, только подготавливает почву.

## Находки (для follow-up, не блокеры)
1. **[ИСПРАВЛЕНО, #993]** ~~Плейсхолдер-id из bootstrap-черновика течёт в текст `propose`/`next`.~~
   На свежей установке bootstrap рисует цели с именами-заглушками `goal-id-1`/`goal-id-2`, и `propose`
   говорил «начать двигать направление «goal-id-1»» — дословный внутренний id в лицо человека. Это не
   регресс P0 (после `ai-ops model` человек задаёт имена), но читаемость страдала.
   **Исправлено (#993, 2026-09-16):** пока у цели нет человеческого имени, кит называет её «безымянное
   направление (задай имя через `ai-ops model`)», а сам id в данных не трогает; несколько безымянных
   направлений схлопываются в один совет. Проверено сквозняком на чистой установке (`setup` →
   `propose`): сырых `goal-id` в выводе ноль.

## Вердикт репетиции
Чистая установка работает end-to-end; продуктовые входные поверхности честны и без утечек на реальном
инсталле. Готовность к живому внешнему прогону (P0 №7) — по входу подтверждена; выход (Verified/Outcome
на реальном продукте) остаётся за внешним прогоном. Одна находка читаемости найдена и исправлена (#993).
