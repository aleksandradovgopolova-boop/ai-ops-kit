# Real-Product Qualification #2 — severnaya_traektoriya (2026-08-12)

**Прогон:** установка кита v3.36.8 с нуля в незнакомый живой репозиторий
`Desktop/projects/severnaya_traektoriya` (Vite + React + TypeScript, 72 коммита, vitest +
eslint + build, без CI и без БД) → `doctor` → `model`.

**Итог: прогон ОСТАНОВЛЕН на онбординге, до продуктовой задачи.** Не потому, что кит сломался, а
потому что он выдал владельцу **придуманный продуктовый факт со статусом «подтверждено»** — а это
нулевой критерий приёмки (`planning/plan.yaml → second-brownfield-run`). Доводить такой прогон до
PR значило бы получить зелёный результат, опирающийся на ложное утверждение о продукте.

## Выбор репозитория и отклонение от критерия

Критерий требовал репозиторий, максимально НЕ похожий на первый (niti: Next.js/Turborepo,
монорепо, TS). Совпало: другой продукт, другая структура, существующая история, свои
продуктовые артефакты, кита никогда не было (то есть установка настоящая).

**Отклонения, названные заранее, а не после:**

| Критерий | Факт | Следствие |
|---|---|---|
| другой стек | Vite/React **TS** (первый тоже TS) | стековая разница слабее заявленной |
| реальная БД | **нет** | сценарий B обкатки `contour_consistency` невозможен |
| свой CI | **нет** | «verified» опирается на lint/test/build локально, не на конвейер |
| frontend+backend | только frontend | интеграционная поверхность уже |
| живой продукт | последний коммит 6 месяцев назад | не активная разработка |

Отдельно: владелец в ходе прогона исключил первоначального кандидата
(`msh_news_bot_v2` — Python, PostgreSQL+pgvector, 9 workflows, 493 коммита), который критериям
соответствовал полностью. Установка из него **откачена побайтово** (дерево чистое, SHA
`88532450` не изменён, следов кита нет).

## Метрики приёмки

| Метрика | Цель | Факт |
|---|---|---|
| технических вмешательств владельца | 0 | **0** — чинить кит по дороге не потребовалось |
| необоснованных вопросов | 0 | **0** — из 12 вопросов ни один не выводится из кода |
| ложных green | 0 | **2** (F-018, F-019) |
| придуманных продуктовых фактов | 0 | **1** (F-019) |
| verified PR | 1 | **0** — прогон остановлен до задачи |

## Что отработало хорошо (фиксируем, чтобы не сломать)

- **Установка и `doctor` — без единого вмешательства.** `rc=0`, ни трейсбека, ни ручной правки
  артефактов.
- **Классификация верна:** «Это уже работающий продукт» — brownfield распознан.
- **Кит отказался выдумывать и сказал это словами:** «а то, что из кода честно узнать нельзя,
  спрошу у тебя — выдумывать это я не буду».
- **Догадка предъявлена как догадка:** `production_env` → `status: inferred`, `asks_human: true`,
  «предполагаю: … — подтвердить?».
- **Вопросы разделены по силе:** 5 блокирующих (⚠) и 7 мягких (·), а не 12 одинаковых.
- **Честное «не знаю» в местах, где нечего знать:** `окружения: not_detected`,
  `экономика: unavailable — не ноль`, `ui-evidence: absent … (не маскируем)`.
- **Реальный сигнал, который человек бы пропустил:** `main` отстаёт от upstream на 313 коммитов.

## F-018 · doctor рапортует «✓ артефакты на месте» про свои же черновики — ЗАКРЫТО

**Симптом.** Сразу после `init`, на репозитории, где направления и плана нет и быть не может:

```
планирование (направление и план): ✓ артефакты на месте
Всё в порядке. Кит на месте и работает как ожидается.
```

**Доказательство противоречия внутри кита.** На тех же файлах:

```
is_template(plan):  True   ← кит САМ считает это заготовкой
целей в плане:      ['goal-id-1']
roadmap.check:      ⚠ цель 'goal-id-2' объявлена, но работы под неё нет
```

**Причина.** `_planning_gaps()` в установщике проверял только `Path.exists()`. Предикат
«заполнено ли» (`delivery_plan.is_template`) в ките ЕСТЬ, но doctor его не спрашивал. Комментарий
над проверкой обещал ровно обратное: «контур планирования — пробел ВИДЕН, а не молчит».

**Исправление — три состояния вместо двух.** Заготовка это не пробел и не готовность, а
объявленный следующий шаг, и печатается она **без `✗`/`⚠`** — тем же идиомом, что
`ui-evidence: absent … (не маскируем)`. Так соблюдаются оба уже записанных правила разом:
«вердикт следует за худшей строкой» (иначе вердикту не верят) и «ставить кит и сразу иметь
замечание — плохой первый экран». `✗` остаётся там, где артефакта нет вовсе — это дрейф, а не
свежесть. Теперь владелец читает:

```
планирование (направление и план): заготовки, не заполнены (ROADMAP.md, planning/plan.yaml) —
ждут `./ai-ops model`; норма для свежей установки, но направления и плана у репозитория ПОКА НЕТ
```

Здесь же вскрылось, что **существующий тест закреплял старое поведение**
(`test_doctor_ok_on_fresh_install` требовал «Всё в порядке» и отсутствие замечаний). Тест прав по
замыслу — первый экран не должен пугать; неверен был способ этого добиться: ложное «✓».

## F-019 · продуктовый факт подтверждён СОБСТВЕННЫМ артефактом кита — ЗАКРЫТО

**Самая тяжёлая находка прогона.** Кит сообщил владельцу:

> Техническую картину я восстановил сам: languages, frameworks, **ci pipeline**, test coverage
> presence, architecture style, **production env**.

Машиночитаемо:

```json
"ci_pipeline":    {"value": ["build","lint","test"], "status": "verified",
                   "evidence": [".github/workflows"]}
"production_env": {"value": "окружение, куда деплоит существующий конвейер",
                   "status": "inferred", "evidence": [".github/workflows"]}
```

**А в репозитории CI нет вовсе.** Каталог `.github/` отсутствует в git-истории; три файла в
`.github/workflows/` — `ai-ops-record.yml`, `ai-ops-update.yml`, `ai-ops-validate.yml` — положил
сам установщик секундой раньше.

**Две ошибки в одном месте** (`repo_audit.py`):

1. Условие всего утверждения — «существует ли `.github/workflows`». Его выполняют файлы кита,
   поэтому в любом репозитории без CI кит сначала ставит свой CI, а потом объявляет продукту
   наличие конвейера. Самоподтверждающееся доказательство.
2. Значения (`build`, `lint`, `test`) читаются из `package.json → scripts`, а `evidence`
   указывает на workflows. Ссылка ложна даже там, где CI настоящий: шаги workflow не разбирались
   никогда.

Статус `verified` на таком основании — худший вид ошибки для этого продукта: владелец верит, что
кит прочитал ЕГО конвейер, и `production_env` строится на той же пустоте.

**Исправление.** `_product_ci()` признаёт CI продуктом только если в `.github/workflows` есть
хотя бы один файл, который положил не кит (`ai-ops-*.yml` узнаются по имени — так их называет
установщик и так же проверяет `validate_ci_templates`). Прочие системы (`.gitlab-ci.yml`,
`Jenkinsfile`, `.circleci`) распознаются как раньше. `evidence` для `ci_pipeline` теперь включает
манифесты, откуда взяты команды, и несёт примечание, что шаги workflow не разбирались.

Регресс-тесты: workflow кита в одиночку **не** считаются CI продукта; настоящий workflow
считается (иначе правка ослепила бы проверку); другие CI-системы по-прежнему видны.

## Что этот прогон НЕ доказал

- **Продуктовая задача, implementation, review, exact-SHA verification и draft PR** — не
  выполнялись. Прогон остановлен осознанно: F-019 отравляет вход для любой последующей работы.
- **`contour_consistency`, сценарий B** (меняется схема → устаревший DataMap обнаружен) —
  невозможен на этом репозитории: БД и схемы нет. Graduation гейта по-прежнему ждёт продукта
  с реальной моделью данных.
- **Стоимость и живой writer.** API-ключей провайдеров в окружении нет; писателем был бы
  `claude` CLI (v2.1.228) через адаптер, но до вызова writer'а дело не дошло.

## Следующий шаг

Повторить прогон **после** этих исправлений, на репозитории с реальной БД и своим CI — тогда же
закрывается сценарий B `contour_consistency`. Кандидат, соответствующий критериям полностью,
известен, но исключён владельцем; нужен другой такой же.

---

## Дополнение — проверка исправлений на ii-sreda (2026-08-12, read-only)

Владелец предложил `ii-sreda` как репозиторий с БД и CI. Прогон выполнен **без записи** в его
рабочее дерево: модули планирования текущего `main` кита запущены против дерева ii-sreda напрямую.
Причина — репозиторий под активной разработкой (последний коммит 7 часов назад, ветка
`base/evals-stack`, 11 незакоммиченных изменений), а установленный там кит на 3.36.4.

### F-019 подтверждена на НАСТОЯЩЕМ CI

`ii-sreda` имеет 9 workflow, из них 6 не китовых (`ci.yml`, `deploy.yml`, `e2e-app.yml`,
`bootstrap-admin.yml`, `status-gate.yml`, `sync-context.yml`). После исправления:

```
ci_pipeline     verified   ['build','lint','test','typecheck']
                evidence:  ['.github/workflows', 'package.json']   ← источник значений назван
production_env  inferred   evidence: ['Dockerfile','docker-compose.yml']  ← не workflow кита
```

**Аудит на придуманные факты: 0.** Ни одно утверждение со `status: verified` не ссылается на
артефакт кита (`languages` ← npm-lock/package.json, `ci_pipeline` ← workflows + манифест,
`test_coverage_presence` ← обход дерева).

Классификация: `EXISTING_PRODUCT`, `confidence: high`, основания названы фактами — «коммитов 745,
файлов кода 384», «признаки живой системы: CI, тесты».

### Заявка про FSD-ловушку подтверждена

`ii-sreda` использует Frontend-Sliced Design и содержит `src/entities/`. README v3.36.7 заявлял,
что `entities/` в FSD — не модель данных и такой сигнал снимается. Проверено: контур
`data_contracts` (9 сигналов) на изменениях последних коммитов даёт `not_changed`, ложного
срабатывания на `src/entities` нет.

### Сценарий B обкатки contour_consistency здесь ТОЖЕ невозможен

Вопреки ожиданию, БД в репозитории не объявлена: `supabase/` нет, `*.sql` нет, миграций нет,
`context/system/DataMap.md` нет. Ссылки на postgres живут в `vite.config.ts`, `.env.example` и
`ci.yml` — то есть база внешняя, а её схема в репозитории не хранится. Кит отвечает про это
честно: `persistence: unknown`, evidence — контейнеры, note «хранилище может быть объявлено в
compose — не разобрано».

Следствие: сценарий B требует, чтобы устаревший `DataMap` было ЧЕМ обнаружить. Пока схема продукта
вне репозитория, гейт нечего сверять — graduation ждёт продукта, где модель данных лежит в коде
(миграции, `schemas/`, `openapi`, `prisma`).

### Исправлена собственная ошибка замера

В обзоре я написал «py 1450 + ts 764 → fullstack». Неверно: из 2028 python-файлов **2027
принадлежат самому киту** (`.ai/managed` и копии в `.ai/worktrees`), у продукта — один, и он в
`node_modules`. `ii-sreda` — чистый Node/TS продукт, и `languages: ['node']` кита правильно:
свои артефакты он из детекции исключает. Ошибка была в моём `find`, не в ките.

### Чего не хватает для полного прогона здесь

Полный путь (задача → implementation → verified → draft PR) не запускался. Предпосылки, которые
нужны от владельца:

1. работа на `base/evals-stack` закоммичена или отложена — иначе прогон движка смешается с ней;
2. кит в ii-sreda обновлён (там 3.36.4; апдейт до 3.36.8 был применён 2026-08-12 и **утерян** —
   он лежал в рабочем дереве незакоммиченным, как того требует сам кит, и не сохранился при
   переключении ветки).
