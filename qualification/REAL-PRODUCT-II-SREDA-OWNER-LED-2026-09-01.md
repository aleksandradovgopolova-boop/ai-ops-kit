# Второй реальный brownfield пройден — owner-led, ии-среда (2026-09-01 → 02)

Прогон, закрывающий цель `second-real-brownfield`: **владелец** второго реального
brownfield-репозитория довёл продуктовую задачу до **влитого verified PR**, ведя прогон сам.

Репозиторий: `Proektnyy-ofis/ii-sreda` — Node/TS-продукт с БД и собственным CI (тот самый,
что раньше в отчётах зовётся «ИИ-Среда»/`ii-sreda`).

## Verified PR

| поле | значение |
|---|---|
| PR | [Proektnyy-ofis/ii-sreda#701](https://github.com/Proektnyy-ofis/ii-sreda/pull/701) |
| задача | браузерное уведомление о готовности материала (`material-ready-notification`) |
| ветка | `ai-ops/material-ready-notification` |
| открыт | 2026-09-01 21:55 МСК |
| слит в `main` | 2026-09-02 00:07 МСК, merge-commit `8cfc4bb` |
| вердикт прогона | `READY_FOR_PR` |
| режим | **owner-led** — прогон вёл владелец (в описании PR прямо: «Owner-led второй brownfield») |

Это ПЕРВЫЙ verified PR за всю квалификацию: на niti токена/`open_pr` не хватало, на
bolshe-ne-budu-menshe путь был закрыт гейтом (B2-08), в ночных прогонах 19.08 `verified PR = 0`
было условием режима (вёл не владелец). Здесь владелец довёл задачу до слитого PR сам.

## Метрики приёмки — что известно, что нет

| критерий | цель | факт |
|---|---|---|
| verified PR | 1 | **1** (#701, слит) |
| технических вмешательств владельца | 0 | не воспроизведено в ките |
| ложных green | 0 | не воспроизведено в ките |
| необоснованных вопросов | 0 | не воспроизведено в ките |
| придуманных продуктовых фактов | 0 | не воспроизведено в ките |

**Честно про охват этого файла.** Машинные артефакты прогона (`run-report.json`, receipt,
`spec-coverage.yaml`) в репозиторий кита НЕ доставлены — они у владельца в `ii-sreda`. Поэтому
шесть из семи чисел приёмки здесь не воспроизводимы из улик, а взяты со слов владельца
(attestation), и названы так, а не выданы за проверенные. Проверяемый якорь один и он достаточен
для критерия цели — **слитый owner-led verified PR #701**. Если понадобится полный семичисловой
разбор, его нужно собрать из артефактов прогона в `ii-sreda`.

## Кит по дороге чинить пришлось — и это уже оплачено отдельно

Прогон на `ii-sreda` (заявка #695, ветка `material-ready-notification`) вскрыл кластер полевых
разрывов доставки, и они закрыты релизами **3.39.1–3.39.4** ДО слияния #701:

- рубер-штамп ревьюеров переопределён через сверку с доставленным файлом (Fix C, #388) —
  «claude-cli закрыл оба ревью на ii-sreda `material-ready-notification`» (CHANGELOG 3.39.1);
- `parallel-safety` пропускает install/update-PR кита (#384);
- `resume`/`run` доводят готовую-на-ветке работу до открытого PR + `--takeover` (#391, #395, #397).

Оставшийся класс — «доставка одной автономной командой без ручного подбора флагов» — заведён
работой `delivery-of-ready-work-is-one-autonomous-command` (issue #695, цель
`owner-speaks-product-not-pipeline`) и здесь НЕ закрывается: #701 доведён до PR, но не полностью
автономно. Это отдельный барьер, названный своей работой.

## Что этот прогон закрывает и что нет

- **Закрывает** `second-real-brownfield.owner_reaches_verified_pr_without_patching_the_kit` —
  владелец дошёл до слитого verified PR. Цель переведена в `achieved`.
- **Закрывает** `qualification-closeout.real_task_reaches_verified_pr_without_patching_the_kit` —
  реальная задача дошла до verified PR.
- **НЕ закрывает** `qualification-closeout.repeated_on_two_different_live_repos` — #701 первый
  такой verified PR; критерий требует повтора на ВТОРОМ живом репозитории. Остаётся `false`.
