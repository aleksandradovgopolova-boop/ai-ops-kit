# registry/feature-registry/surface-extractors.yaml
#
# ЧЕСТНАЯ ДЕКЛАРАЦИЯ ЭКСТРАКТОРОВ ПОВЕРХНОСТЕЙ (W1 фичи feature-registry-coverage).
#
# Инвариант кита (AGENTS.md): capability-декларации честные — нельзя объявлять возможность, которой
# нет в коде. На этапе W1 код-экстракторов ещё НЕ существует (извлечение поверхностей — работа W2),
# поэтому список реализованных экстракторов ПУСТ. Это не заглушка «на будущее»: это честное «пока
# кит не умеет извлекать ни одной поверхности ни из одного стека».
#
# Как читать: `extractors` — то, что кит УМЕЕТ прямо сейчас (у каждого есть код и он проверяем).
# `planned` — намерения; они помечены status: unsupported + note planned и НЕ заявляют confidence,
# ровно как планы в registry/capability-index.yaml. Пока экстрактор в `planned`, кит не выдаёт по
# нему verified-поверхностей и не блокирует по ним (см. правило силы в стандарте FEAT).
#
# ЗАМЕТКА О ДОСТАВКЕ (honest): как и схема рядом, файл лежит в подкаталоге registry/ и в дочку пока
# не едет — child-side читателя нет до W2/W3/W5. Поедет плоским registry/-файлом вместе с первым
# реализованным экстрактором, и тогда подъём поставки будет записан в quality/delivery-budget.yaml.

schema_version: 1
registry_type: surface-extractors

# Словарь видов поверхностей, которые вообще бывают (совпадает с surface.kind в схеме реестра фич).
surface_kinds: [route, cli, screen, api]

# Уровни уверенности вывода. verified — доказано детерминированным разбором кода (может блокировать
# в W4); inferred — предположено эвристикой (только предупреждает).
confidence_levels: [verified, inferred]

# ── Реализованные экстракторы ───────────────────────────────────────────────────────────────────
# Здесь ровно то, что кит УМЕЕТ извлекать ПРЯМО СЕЙЧАС — у каждого есть код и он проверяем тестом.
# Заявлять стек, для которого кода нет, запрещено инвариантом честных capability-деклараций.
#
# W2 добавил ПЕРВЫЙ реальный экстрактор: HTTP-маршруты Flask/FastAPI по декораторам, точным разбором
# AST без исполнения кода → confidence: verified. Код — ai_ops_kit/checks/surface_extraction.py
# (функция extract_python_web_routes; ядро обхода — extract_surfaces).
extractors:
  - id: python-web-routes
    stack: {language: python, frameworks: [flask, fastapi]}
    surface_kinds: [route]
    confidence: verified
    method: >-
      статический разбор AST: декоратор @<router>.<method>("/path", ...) над (async-)функцией, где
      <method> ∈ {route, api_route, websocket, get, post, put, delete, patch, head, options, trace},
      а первый позиционный аргумент — строковый путь-литерал, начинающийся с "/". Код не исполняется.
      ref = "file:line|symbol" (symbol — имя функции-обработчика).
    honest_limits:
      sees: >-
        маршруты, объявленные декоратором в исходнике со строковым путём-литералом
        (Flask @app.route/@bp.get, FastAPI @app.get/@router.post и т.п.); даёт точные file:line|symbol.
      does_not_see: >-
        маршруты, собранные динамически (Flask add_url_rule, FastAPI include_router через переменную);
        пути НЕ-литералы (f-строки, константы, конкатенация); Django urlpatterns (список, не декоратор);
        классовые/method-view; любые не-Python стеки. Их появление НЕ доказывает и не блокирует.

  # CLI-команды продукта (вид поверхности cli). Три экстрактора под три стека объявления команд;
  # у каждого — свой честный предел. Общий принцип: доказано литералом → verified, динамика → пропуск.
  - id: python-cli-argparse
    stack: {language: python, frameworks: [argparse]}
    surface_kinds: [cli]
    confidence: verified
    method: >-
      статический разбор AST в файле, импортирующем argparse: `<subparsers>.add_parser("<name>", ...)`
      (имя подкоманды из строкового литерала) и `ArgumentParser(prog="<name>")` (имя программы из
      литерала, по возможности). Код не исполняется. ref = "file:line|<имя-команды>".
    honest_limits:
      sees: >-
        подкоманды argparse с ЛИТЕРАЛЬНЫМ именем (add_parser("name")) и prog-имя верхнего парсера,
        заданное строковым литералом, — в файле, который действительно импортирует argparse.
      does_not_see: >-
        имена, собранные динамически (переменная/f-строка/имя из данных или конфига); prog из
        переменной; add_parser без литерала; argparse-обёртки, спрятанные за своим API без импорта
        argparse в этом файле. Недоказанное verified НЕ помечается.
  - id: python-cli-click
    stack: {language: python, frameworks: [click]}
    surface_kinds: [cli]
    confidence: verified
    method: >-
      статический разбор AST в файле, импортирующем click: декоратор @<x>.command()/@<x>.group()
      (или @command()/@group() из `from click import ...`) над функцией. Имя — из литерала в
      декораторе (`"name"` или `name="..."`) либо из имени функции по правилу click (lower + `_`→`-`).
      ref = "file:line|<имя-команды>".
    honest_limits:
      sees: >-
        команды и группы click, объявленные декоратором command/group в файле с импортом click;
        имя берётся из литерала или детерминированно из имени функции.
      does_not_see: >-
        Typer (@app.command() на typer.Typer — иной стек, не импортирует click) и иные click-надстройки;
        имя из переменной/f-строки; команды, зарегистрированные в рантайме (add_command из данных);
        нестандартный cls с собственным правилом именования. Недоказанное verified НЕ помечается.
  - id: python-console-scripts
    stack: {language: python, frameworks: [setuptools, poetry]}
    surface_kinds: [cli]
    confidence: verified
    method: >-
      текстовый разбор объявленных точек входа (БЕЗ tomllib — пол 3.9): ключи-имена в pyproject.toml
      [project.scripts]/[project.gui-scripts]/[tool.poetry.scripts] и вложенные строки
      console_scripts/gui_scripts в setup.cfg [options.entry_points]. ref = "file:line|<имя-команды>".
    honest_limits:
      sees: >-
        имена команд, ОБЪЯВЛЕННЫЕ как точки входа дистрибутива в pyproject.toml или setup.cfg —
        доказуемо объявленный интерфейс запуска продукта.
      does_not_see: >-
        entry_points, заданные исполняемым setup.py (аргумент entry_points= вызова setup()); точки
        входа из плагинов/иных групп; динамически сформированные списки. Только эти два файла.
  # E2 добавил ещё три верифицированных экстрактора маршрутов для питон-веба (тот же приём: verified
  # только когда путь/префикс — строковый литерал в исходнике, точным разбором AST без исполнения;
  # каждый сужен к файлам с импортом своего фреймворка, чтобы одноимённый чужой вызов не дал ложный
  # verified). Код — те же ai_ops_kit/checks/surface_extraction.py (функции extract_django_urls,
  # extract_drf_router, extract_aiohttp_routes).
  - id: django-urls
    stack: {language: python, frameworks: [django]}
    surface_kinds: [route]
    confidence: verified
    method: >-
      статический разбор AST: вызов path("route/", view) или re_path(r"^...$", view) в файле,
      импортирующем django.urls, где первый позиционный аргумент — строковый путь-литерал.
      Код не исполняется. ref = "file:line|<вьюха-или-путь>" (имя вьюхи, если она записана именем).
    honest_limits:
      sees: >-
        объявления path()/re_path() со строковым путём-литералом в файле, импортирующем django.urls;
        symbol — имя вьюхи (views.orders) либо сам путь-литерал.
      does_not_see: >-
        include(...) — вложенный urlconf монтируется, конкретный эндпоинт им НЕ доказывается (пропуск);
        пути НЕ-литералы (переменная/f-строка/конкатенация); маршруты, собранные динамически или за
        плагинной регистрацией; class-based view с кастомным роутингом; urlpatterns без импорта
        django.urls в этом файле. Ничего из этого НЕ становится verified.
  - id: drf-router
    stack: {language: python, frameworks: [django-rest-framework]}
    surface_kinds: [route]
    confidence: verified
    method: >-
      статический разбор AST: вызов <router>.register("prefix", ViewSet) в файле, импортирующем
      rest_framework, где первый позиционный аргумент — строковый префикс-литерал. Код не
      исполняется. ref = "file:line|<ViewSet-или-префикс>".
    honest_limits:
      sees: >-
        router.register() со строковым префиксом-литералом в файле, импортирующем rest_framework;
        symbol — имя ViewSet либо сам префикс.
      does_not_see: >-
        префикс-НЕ-литерал (переменная/f-строка); конкретные суффиксы маршрутов, которые роутер
        разворачивает в рантайме из ViewSet (list/detail/@action) — виден только зарегистрированный
        префикс, а не порождённые из него URL; чужой .register() в файле без импорта rest_framework.
  - id: aiohttp-routes
    stack: {language: python, frameworks: [aiohttp]}
    surface_kinds: [route]
    confidence: verified
    method: >-
      статический разбор AST в файле, импортирующем aiohttp: app.router.add_route("GET","/p",h)
      (путь — 2-й арг), app.router.add_get("/p",h) и семейство add_<method> (путь — 1-й арг), а также
      app.add_routes([web.get("/p",h), web.route("GET","/p",h), ...]) — разбор списка-литерала. Путь —
      строковый литерал, начинающийся с "/". Код не исполняется. ref = "file:line|<обработчик-или-путь>".
    honest_limits:
      sees: >-
        add_route/add_<method>/add_routes([...]) со строковым путём-литералом (начинается с "/") в
        файле, импортирующем aiohttp; symbol — имя обработчика либо сам путь.
      does_not_see: >-
        путь-НЕ-литерал (переменная/f-строка); add_routes с аргументом-переменной вместо списка-литерала
        (список собран динамически); маршруты, добавленные через RouteTableDef-декораторы (@routes.get)
        или другой способ; чужой .add_route()/.add_get() в файле без импорта aiohttp.

  # E3 добавил ПЕРВЫЙ не-Python вид поверхности — экраны UI (screen) фронтенд-роутеров. ВАЖНО про
  # честность силы: фронтенд разбирается ТЕКСТОМ/регэкспом по исходнику (stdlib ast к JS неприменим),
  # без исполнения node и без сети. Это НЕ доказательный AST того же класса, что питон-экстракторы →
  # оба заявляют confidence: inferred (эвристика паттерна: видна аналитику, в W4 НЕ блокирует). Код —
  # те же ai_ops_kit/checks/surface_extraction.py (extract_react_router_screens/extract_vue_router_screens).
  - id: react-router
    stack: {language: [javascript, typescript], frameworks: [react-router]}
    surface_kinds: [screen]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не AST) разбор исходника .jsx/.tsx/.js/.ts без исполнения: JSX-объявление
      `<Route path="/x" ...>`/`<Route path='/x'/>` (путь — строковый литерал в кавычках) и объектные
      роуты `createBrowserRouter([{path:"/x", ...}])` / `useRoutes([{path:...}])` (ключ `path:` со
      строковым литералом — берётся только в файле с индикатором объектного роутера). Регэксп по
      паттерну — эвристика, не доказательство → confidence: inferred. ref = "file:line|<path>".
    honest_limits:
      sees: >-
        экраны-маршруты React Router, объявленные ЛИТЕРАЛЬНЫМ строковым путём — JSX-атрибутом path=
        у <Route> или ключом path: в объектных роутах (createBrowserRouter/useRoutes). Даёт file:line
        и сам путь как symbol, но помечает inferred (текстовый разбор, не блокирует).
      does_not_see: >-
        путь из ПЕРЕМЕННОЙ / шаблон-строки (backtick) / выражения `{...}` (в кавычки не попадает —
        пропуск); объектный `path:` в файле БЕЗ индикатора роутера (чтобы чужой объект с ключом path
        не дал ложный экран); <Route> с JSX-значением пропа (`element={<X/>}`), стоящим ПЕРЕД path в
        том же теге (символ `>` внутри прерывает скан атрибутов — редиома «path первым» видна, обратный
        порядок может быть пропущен); маршруты, собранные динамически из данных/конфига; серверные
        JS-маршруты (Express/Nest/Next) — это иной вид, не screen. Ничего из этого НЕ verified.
  - id: vue-router
    stack: {language: [javascript, typescript], frameworks: [vue-router]}
    surface_kinds: [screen]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не AST) разбор исходника .vue/.js/.ts без исполнения: ключи `path: '/x'` в
      `routes: [{ path: '/x', component: ... }]` (createRouter/VueRouter). Ключ берётся только в файле
      с индикатором vue-router (createRouter/VueRouter/импорт vue-router), путь — строковый литерал.
      Регэксп по паттерну — эвристика, не доказательство → confidence: inferred. ref = "file:line|<path>".
    honest_limits:
      sees: >-
        экраны-маршруты Vue Router, объявленные ЛИТЕРАЛЬНЫМ строковым path: в массиве routes файла с
        индикатором vue-router. Даёт file:line и путь как symbol, помечает inferred (не блокирует).
      does_not_see: >-
        путь из переменной/шаблон-строки/выражения (не литерал — пропуск); routes-массив, собранный
        динамически или импортированный из другого модуля; `path:` в файле без индикатора vue-router;
        вложенные роуты, у которых конкретный путь склеивается из родителя в рантайме (виден только
        объявленный литерал сегмента); серверный рендеринг вне known-роутера. Ничего из этого НЕ verified.

  # E5 добавил ТРЕТИЙ фронтенд-роутер к виду `screen` — Angular Router. Честность силы та же, что у
  # react-router/vue-router (E3): Angular-роутинг живёт в TypeScript, а stdlib ast к TS неприменим —
  # разбор ТЕКСТОМ/паттерном по исходнику, без исполнения → confidence: inferred (в W4 НЕ блокирует).
  # Код — тот же подмодуль ai_ops_kit/checks/surface_extractors/js_ui.py (extract_angular_router_screens,
  # когезивно рядом с react/vue — один вид поверхности), зарегистрирован в DEFAULT_EXTRACTORS фасада.
  - id: angular-router
    stack: {language: typescript, frameworks: [angular]}
    surface_kinds: [screen]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не AST) разбор исходника .ts без исполнения: ключи `path: 'x'` в литеральном массиве
      маршрутов — `RouterModule.forRoot([{ path: 'billing', component: … }])` / `.forChild([...])` и
      `const routes: Routes = [{ path: 'x', ... }]`. Ключ берётся ТОЛЬКО в файле с индикатором Angular
      (`@angular/router` / `RouterModule` / аннотация `: Routes`), путь — строковый литерал. Angular
      объявляет путь обычно БЕЗ ведущего "/" (`path: 'billing'`) — нормализуется к `/billing` ради
      единообразия ref с react/vue. Регэксп по паттерну — эвристика, не доказательство → inferred.
      ref = "file:line|/path".
    honest_limits:
      sees: >-
        экраны-маршруты Angular, объявленные ЛИТЕРАЛЬНЫМ строковым path: в массиве routes файла с
        индикатором Angular (RouterModule.forRoot/forChild или `const routes: Routes = [...]`).
        Литеральный динамический сегмент (`users/:id`) сохраняется как есть. Даёт file:line и
        нормализованный путь как symbol, помечает inferred (текстовый разбор, не блокирует).
      does_not_see: >-
        lazy-модули через `loadChildren` (конкретные дочерние пути грузятся лениво, из этого файла не
        видны); wildcard `path: '**'` (catch-all, конкретный экран им не задаётся — пропуск) и пустой
        `path: ''` (дефолт/редирект-корень — пропуск); путь из переменной/шаблон-строки/выражения (не
        литерал — пропуск); матричные/динамически собранные пути; standalone-роуты в `provideRouter`,
        когда массив маршрутов задан НЕлитералом (переменной/импортом); склейку вложенных `children`
        в полный путь в рантайме (виден только объявленный литерал сегмента). Ничего из этого НЕ verified.

  # E4 добавил СЕРВЕРНЫЕ JS/TS-маршруты (вид route) для JS-бэкенда — Express, NestJS, Next. Честность
  # силы та же, что у screen-экстракторов E3: JS/TS разбирается ТЕКСТОМ/паттерном (Express/Nest) или
  # выводится из РАСКЛАДКИ ФАЙЛОВ (Next), без исполнения node и без сети — это НЕ доказательный AST →
  # все три заявляют confidence: inferred (в W4 НЕ блокируют). Код — новый подмодуль
  # ai_ops_kit/checks/surface_extractors/js_server.py (extract_express_routes / extract_nest_routes /
  # extract_next_routes), зарегистрирован в DEFAULT_EXTRACTORS фасада surface_extraction.py.
  - id: express
    stack: {language: [javascript, typescript], frameworks: [express]}
    surface_kinds: [route]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не AST) разбор исходника .js/.ts/.mjs/.cjs без исполнения: вызовы
      `<obj>.<method>("/path", ...)`, где <method> ∈ {get, post, put, delete, patch, options, head,
      all, use}, а путь — строковый литерал в кавычках, начинающийся с "/". Берётся ТОЛЬКО в файле с
      индикатором express (import express / require('express') / from 'express'), чтобы одноимённый
      чужой `.get(...)` не дал ложный маршрут. Регэксп по паттерну — эвристика → confidence: inferred.
      ref = "file:line|<path>".
    honest_limits:
      sees: >-
        объявления маршрутов Express со СТРОКОВЫМ путём-литералом, начинающимся с "/" (app.get,
        router.post, app.use("/mount", …) и т.п.), в файле, действительно импортирующем express.
        Даёт file:line и сам путь как symbol, помечает inferred (текстовый разбор, не блокирует).
      does_not_see: >-
        путь из ПЕРЕМЕННОЙ / шаблон-строки (backtick) / выражения (в кавычки не попадает — пропуск);
        маршруты, собранные динамически из данных/конфига; цепочки middleware и вложенные кастомные
        роутеры, смонтированные через переменную; `.route("/x").get(...).post(...)`-цепочки (виден
        лишь литеральный вызов метода с путём); файлы без импорта express. Ничего из этого НЕ verified.
  - id: nest
    stack: {language: [typescript, javascript], frameworks: [nestjs]}
    surface_kinds: [route]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не AST) разбор исходника .ts/.tsx/.js/.mjs/.cjs без исполнения: method-декораторы
      `@Get("/x")`/`@Post(...)`/@Put/@Delete/@Patch/@Options/@Head/@All на методах + `@Controller("/prefix")`
      на классе; полный маршрут = префикс контроллера + путь метода (для метода берётся ближайший
      предшествующий @Controller). Путь/префикс — строковый литерал; `@Get()`/`@Controller()` без
      литерала дают пустой сегмент (маршрут сводится к префиксу/корню). Берётся ТОЛЬКО в файле с
      индикатором `@nestjs`. Регэксп по паттерну — эвристика → confidence: inferred. ref = "file:line|<path>".
    honest_limits:
      sees: >-
        маршруты Nest, объявленные method-декоратором со строковым путём-литералом, склеенные с
        префиксом @Controller того же файла; file:line и путь как symbol, помечает inferred.
      does_not_see: >-
        путь/префикс из переменной/шаблон-строки/константы (не литерал — сегмент пустой или пропуск);
        глобальные и версионные префиксы, заданные в рантайме (app.setGlobalPrefix, versioning);
        `@Controller`, импортированный/сгенерированный динамически; RouterModule.register с путями из
        данных; файлы без индикатора @nestjs. Ничего из этого НЕ verified.
  - id: next
    stack: {language: [typescript, javascript], frameworks: [nextjs]}
    surface_kinds: [route]
    confidence: inferred
    method: >-
      СТРУКТУРНЫЙ вывод из РАСКЛАДКИ ФАЙЛОВ (содержимое не читается, без исполнения): серверные
      эндпоинты Next — Pages API `pages/api/**` (`pages/api/orders.ts` → `/api/orders`; index → корень
      сегмента; допускается ведущий `src/`) и App Router Route Handler `app/**/route.ext`
      (`app/billing/route.ts` → `/billing`). Route-группы `(group)` и параллельные слоты `@slot` в URL
      опускаются, catch-all `[...slug]` честно пропускается (маршрут не выводим), обычный `[id]`
      сохраняется. Вывод из имени файла — эвристика раскладки → confidence: inferred. ref =
      "file:line|<path>" (line = 1: маршрут выведен из пути файла, а не из места в тексте).
    honest_limits:
      sees: >-
        серверные маршруты Next, выводимые из ПУТИ ФАЙЛА: Pages API под pages/api/** и App Router
        Route Handler app/**/route.ext (с опциональным ведущим src/). Путь как symbol, inferred.
      does_not_see: >-
        UI-страницы (pages/index.tsx, app/**/page.tsx — иной вид, не серверный route); catch-all и
        optional catch-all `[...slug]`/`[[...slug]]` (пропуск); rewrites/redirects/basePath из
        next.config; middleware.ts; route.ext, не лежащий под app/; перехватывающие роуты `(.)`/`(..)`
        и иные конвенции App Router сверх route-групп/слотов. Ничего из этого НЕ verified.

  # E6 добавил СЕРВЕРНЫЕ Go-маршруты (вид route) — первый не-Python/не-JS серверный стек. Честность
  # силы та же, что у js_server (E4): Go разбирается ТЕКСТОМ/паттерном по исходнику (stdlib ast к Go
  # неприменим), без вызова компилятора Go и без сети — это НЕ доказательный AST → confidence: inferred
  # (в W4 НЕ блокирует). Код — новый подмодуль ai_ops_kit/checks/surface_extractors/go_web.py
  # (extract_go_web_routes), зарегистрирован в DEFAULT_EXTRACTORS фасада surface_extraction.py.
  - id: go-web
    stack: {language: go, frameworks: [net/http, gin, chi, echo, gorilla/mux]}
    surface_kinds: [route]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не AST) разбор исходника .go без исполнения: вызовы `<ident>.<Verb>("/path", …)`, где
      <Verb> — HTTP-глагол или монтирование: net/http `HandleFunc`/`Handle`; gin/echo заглавные
      GET/POST/PUT/DELETE/PATCH/HEAD/OPTIONS и `Group("/prefix")`; chi CamelCase Get/Post/… и
      `Route("/prefix")`/`Mount("/prefix")`; gorilla/mux `HandleFunc("/path").Methods("GET")` (метод из
      цепочки НЕ читаем — берём путь). Путь — строковый литерал ("…" или raw `…`), начинается с "/", за
      ним `,` или `)` (конкатенация/fmt.Sprintf/переменная/шаблон в кавычки не попадают → пропуск).
      Берётся ТОЛЬКО в файле с индикатором Go-веба (импорт net/http / gin-gonic/gin / go-chi/chi /
      labstack/echo / gorilla/mux), чтобы одноимённый чужой `.Get(...)`/`.HandleFunc(...)` не дал
      ложный маршрут. Регэксп по паттерну — эвристика → confidence: inferred. ref = "file:line|<path>".
    honest_limits:
      sees: >-
        объявления маршрутов Go со СТРОКОВЫМ путём-литералом, начинающимся с "/" (http.HandleFunc,
        r.GET/r.Get, e.GET, gorilla r.HandleFunc(...).Methods(...)), а также литеральный путь групп
        Group/Route/Mount как отдельный route, в файле, действительно импортирующем один из Go-веб-стеков.
        Даёт file:line и сам путь как symbol, помечает inferred (текстовый разбор, не блокирует).
      does_not_see: >-
        middleware (r.Use и цепочки хендлеров); СКЛЕЙКУ префикса роут-группы (Group/Route/Mount) с
        дочерними путями в полный URL — виден только объявленный литерал каждого сегмента, не собранный
        рантаймом полный путь; роут-группы с ДИНАМИЧЕСКИМ (нелитеральным) префиксом; роутеры/пути через
        переменную, конкатенацию, fmt.Sprintf или константу (не литерал — пропуск); генерируемые/
        рефлексией-собранные маршруты; HTTP-метод gorilla из .Methods(...) (берётся путь, не метод);
        стеки сверх перечисленных. Ничего из этого НЕ verified.

  # E7 добавил СЕРВЕРНЫЕ Java-маршруты (вид route) Spring — первый JVM-стек. Честность силы та же, что
  # у go_web (E6) / js_server (E4): Java разбирается ТЕКСТОМ/паттерном по исходнику (stdlib ast к Java
  # неприменим), без вызова компилятора Java / JVM и без сети — это НЕ доказательный AST → confidence:
  # inferred (в W4 НЕ блокирует). Код — новый подмодуль ai_ops_kit/checks/surface_extractors/java_spring.py
  # (extract_spring_routes), зарегистрирован в DEFAULT_EXTRACTORS фасада surface_extraction.py.
  - id: spring-web
    stack: {language: java, frameworks: [spring, spring-mvc, spring-boot]}
    surface_kinds: [route]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не AST) разбор исходника .java без исполнения: method-аннотации `@GetMapping("/x")`,
      `@PostMapping`, `@PutMapping`, `@DeleteMapping`, `@PatchMapping` и `@RequestMapping(...)`
      (в т.ч. `@RequestMapping(value="/x", method=RequestMethod.GET)`); путь метода — строковый литерал,
      позиционный (`"/x"`) или именованный `value=`/`path=`. Class-уровневый `@RequestMapping("/prefix")`
      над `@RestController`/`@Controller`-классом задаёт ПРЕФИКС, который склеивается с путём метода
      (оба литералы); сам префикс маршрутом не считается. Метод без литерального пути (`@PostMapping`,
      `@GetMapping(produces=…)`) сводится к префиксу/корню. Берётся ТОЛЬКО в файле с индикатором Spring
      (импорт `org.springframework.web.bind.annotation` ИЛИ `@RestController`/`@Controller`), чтобы
      одноимённая чужая аннотация `@GetMapping` другого фреймворка не дала ложный маршрут. Регэксп по
      паттерну — эвристика → confidence: inferred. ref = "file:line|<path>".
    honest_limits:
      sees: >-
        маршруты Spring MVC/Boot, объявленные method-аннотацией @*Mapping со строковым путём-литералом
        (позиционным или value=/path=), склеенные с литеральным class-префиксом @RequestMapping того же
        контроллера; file:line и путь как symbol, помечает inferred (текстовый разбор, не блокирует).
      does_not_see: >-
        путь/префикс НЕ-литерал — переменная, КОНСТАНТА (`@GetMapping(ORDERS_PATH)`, `value=BASE`),
        склейка через `+`, шаблон — в кавычки не попадает → пропуск; class-уровневый @RequestMapping с
        МАССИВОМ путей (`{"/a","/b"}`) и метод-аннотацию с массивом путей (виден 0, не разворачиваем);
        кастомные мета-аннотации `@*Mapping` (составные @GetMapping-обёртки со своим именем);
        функциональные роуты WebFlux `RouterFunction`/`route(...)`.route(...); глобальные префиксы из
        конфигурации (server.servlet.context-path) и путь из `application.properties`/`.yml`
        (`@GetMapping("${api.base}")`); класс без индикатора Spring. Ничего из этого НЕ verified.

  # E8 добавил ПЕРВЫЙ вид поверхности `api` — операции продукта из GraphQL-схемы (SDL). Честность
  # силы та же, что у go_web (E6) / spring-web (E7) / js_server (E4): SDL — формальная грамматика, но
  # разбирается ТЕКСТОМ/паттерном по исходнику (не полноценным GraphQL-парсером, без исполнения,
  # интроспекции и сети) → это НЕ доказательный AST того же класса, что Python-AST → confidence:
  # inferred (в W4 НЕ блокирует). Код — новый подмодуль
  # ai_ops_kit/checks/surface_extractors/graphql_api.py (extract_graphql_operations),
  # зарегистрирован в DEFAULT_EXTRACTORS фасада surface_extraction.py.
  - id: graphql-schema
    stack: {language: graphql, frameworks: [graphql-sdl]}
    surface_kinds: [api]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не GraphQL-парсер) разбор файлов .graphql/.gql без исполнения: поля КОРНЕВЫХ типов
      `type Query { … }`, `type Mutation { … }`, `type Subscription { … }` и их расширений
      `extend type Query { … }` — каждое поле корневого типа есть операция API продукта (имя операции
      = имя поля). Обычный `type Order { … }` — модель данных, его поля НЕ операции. Комментарии `#…`
      и описания-строки (`\"\"\"docstring\"\"\"`, `"…"`, дефолтные строковые значения аргументов)
      маскируются пробелами (позиции/номера строк сохраняются) — ложных полей не дают. Тело блока
      берётся балансировкой `{…}`; незакрытый блок (обрыв файла) пропускается. Регэксп/скан по
      паттерну — не грамматический парсер → confidence: inferred. ref = "file:line|<operation>".
    honest_limits:
      sees: >-
        операции GraphQL, объявленные полями корневых типов Query/Mutation/Subscription (и их extend)
        прямо в SDL-файле .graphql/.gql, включая поля с аргументами (`order(id: ID!): Order`) и
        директивами; даёт file:line и имя операции как symbol, помечает inferred (не блокирует).
      does_not_see: >-
        схемы, заданные SDL-строками В КОДЕ (gql`…`/graphql-tag в .js/.ts) — иной носитель, не .graphql;
        code-first схемы (Nexus/TypeGraphQL/Strawberry — операции заданы декораторами/классами, не SDL);
        интроспекцию рантайма; переименование корневых типов через `schema { query: MyQuery }` (берём
        только конвенциональные имена Query/Mutation/Subscription); директивы схемы как таковые;
        ОБЪЕДИНЁННУЮ схему federation на уровне рантайма (видны только per-file `extend type`), и поля
        НЕ-корневых типов (модели данных). Ничего из этого НЕ verified.

  # E9 добавил серверные маршруты Ruby on Rails (вид route) — DSL файла config/routes.rb. Честность
  # силы та же, что у go_web (E6) / spring-web (E7) / js_server (E4): Ruby разбирается ТЕКСТОМ/паттерном
  # по исходнику (stdlib ast к Ruby неприменим), без вызова интерпретатора Ruby и без сети — это НЕ
  # доказательный AST → confidence: inferred (в W4 НЕ блокирует). Развёртка resources/resource в
  # RESTful — тоже inferred: это КОНВЕНЦИЯ Rails (роутер разворачивает пути в рантайме), а не доказанный
  # в исходнике литерал. Код — новый подмодуль ai_ops_kit/checks/surface_extractors/ruby_rails.py
  # (extract_rails_routes), зарегистрирован в DEFAULT_EXTRACTORS фасада surface_extraction.py.
  - id: rails-routes
    stack: {language: ruby, frameworks: [rails, ruby-on-rails]}
    surface_kinds: [route]
    confidence: inferred
    method: >-
      ТЕКСТОВЫЙ (не AST) разбор Ruby-DSL config/routes.rb без исполнения: глаголы
      `get`/`post`/`put`/`patch`/`delete` со строковым путём-литералом → route (метод из глагола DSL);
      `root "home#index"` → `GET /`; `resources :orders` → 7 стандартных RESTful-маршрутов Rails
      (index/create/new/show/edit/update/destroy: `/orders`, `/orders/:id`, `/orders/new`,
      `/orders/:id/edit`); `resource :profile` → 6 RESTful без index и без `:id`. Блоки
      `namespace :admin do …` дают префикс пути `/admin`, `scope "/api" do …` (и `scope path: "/api"`) —
      префикс `/api`, склеиваемый с путями внутри блока. Путь-НЕ-литерал (переменная, символ
      `get :dashboard`, интерполяция `"/u/#{id}"`, склейка через `+`) → пропуск. Берётся ТОЛЬКО в файле
      с индикатором Rails (`Rails.application.routes.draw` в тексте ИЛИ имя файла routes.rb), чтобы
      чужой `.get`/`resources` в стороннем Ruby не дал ложный маршрут. Регэксп/скан по паттерну — не
      грамматика Ruby → confidence: inferred. ref = "file:line|<МЕТОД> <path>".
    honest_limits:
      sees: >-
        маршруты Rails, объявленные литеральным глаголом (get/post/…), `root`, а также стандартную
        RESTful-развёртку `resources`/`resource` (inferred-конвенция), с учётом префиксов namespace и
        scope одного уровня; file:line и "<МЕТОД> <path>" как symbol, помечает inferred (не блокирует).
      does_not_see: >-
        `only:`/`except:`-фильтры (полный RESTful-набор выдаётся всегда, конкретные фильтры НЕ
        применяются); constraints (`constraints:`/`constraint`-блоки); member/collection-маршруты и
        ВЛОЖЕННЫЕ resources глубже 1 (нутро `resources … do` не разбирается); монтирование движков
        (`mount Engine => "/x"`); concerns (`concerns:`/`concern`); переименование пути через `as:`/`to:`
        (берётся путь, не имя); redirect/`match … via:`; динамические пути (переменная/символ/
        интерполяция/склейка `+`); переименование корня/параметров и любые пути, собранные в рантайме.
        Ничего из этого НЕ verified.

# ── Планы (намерения, НЕ возможности) ───────────────────────────────────────────────────────────
# status: unsupported + note planned — тот же честный приём, что для планов в capability-index.yaml.
# Здесь перечислены НАПРАВЛЕНИЯ, для которых экстрактор задуман, но кода ещё нет. Заявлять по ним
# поддержку (тем более verified) до появления кода — запрещено инвариантом честных деклараций.
planned:
  - intent: python-web-routes-remaining
    stack: {language: python, frameworks: [flask, fastapi, django, aiohttp]}
    surface_kinds: [route, api]
    status: unsupported
    note: >-
      planned: осталось за пределами реализованных экстракторов маршрутов (python-web-routes,
      django-urls, drf-router, aiohttp-routes) — маршруты, собранные ДИНАМИЧЕСКИ: Flask add_url_rule,
      FastAPI include_router через переменную, Django include(...)-вложенность и class-based view с
      кастомным роутингом, aiohttp RouteTableDef-декораторы (@routes.get) и add_routes с
      аргументом-переменной, любая плагинная регистрация в рантайме. Их доказать статическим разбором
      нельзя — они остаются НЕ verified (пропуск), а не заявляются как возможность.
    honest_limits:
      sees: "будет видеть (когда/если реализуем): часть перечисленного как inferred, не verified"
      does_not_see: "не увидит сейчас: динамически собранные маршруты, маршруты за плагинной регистрацией"
  - intent: cli-commands-remaining
    stack: {language: python, frameworks: [typer, setuptools]}
    surface_kinds: [cli]
    status: unsupported
    note: >-
      planned: осталось за пределами реализованных argparse/click/console_scripts — Typer
      (@app.command() на typer.Typer, не импортирует click), entry_points в исполняемом setup.py
      (аргумент вызова setup()), и любые команды, зарегистрированные в рантайме из данных/плагинов.
    honest_limits:
      sees: "будет видеть: объявления команд Typer и entry_points из setup.py статически"
      does_not_see: "не увидит: команды, зарегистрированные в рантайме из данных/плагинов"
  - intent: ui-screens-remaining
    stack: {language: [typescript, javascript], frameworks: [react-router, vue-router, angular]}
    surface_kinds: [screen]
    status: unsupported
    note: >-
      planned: осталось за пределами реализованных screen-экстракторов (react-router, vue-router,
      angular-router) — экраны, СОБРАННЫЕ ДИНАМИЧЕСКИ (path из переменной/конфига/импортированного
      модуля), <Route> с JSX-значением пропа перед path (обратный порядок атрибутов), вложенные роуты
      со склейкой пути в рантайме, Angular lazy `loadChildren` и standalone-роуты `provideRouter` при
      НЕлитеральном массиве, а также иные фронтенд-роутеры сверх перечисленных. Статически текстом их
      доказать нельзя — остаются НЕ verified (пропуск), а не заявляются как возможность.
    honest_limits:
      sees: "будет видеть (когда/если реализуем): часть перечисленного как inferred, не verified"
      does_not_see: "не увидит сейчас: динамически собранные экраны, серверный рендеринг вне known-роутера"
  # ПРИМЕЧАНИЕ О ГРАНИЦЕ: серверные JS-маршруты (Express/Nest/Next) — это вид route, а НЕ screen.
  # Их извлекают экстракторы express/nest/next выше (работа E4, подмодуль js_server.py); screen-
  # экстракторы E3 (react-router/vue-router) покрывают только UI-роуты фронтенда.
