---
name: Задача (единый стиль)
about: Продуктовая задача или дефект в едином стиле кита — заголовок-исход + фиксированные секции
title: "<slug>: <исход простыми словами>"
labels: []
---

<!--
ЗАГОЛОВОК: `<slug>: <исход>` — что станет верно для человека, а не «пофиксить X» и не место в коде.
ТЕЛО: отвечает на четыре вопроса ниже В ЭТОМ ПОРЯДКЕ. Так задачи читаются так же
предсказуемо, как история коммитов (conventional commits). Стиль — часть стандарта кита (tier3,
advisory): его несёт `registry/artifact-registry.yaml -> standard_catalog[issue_template]`.
-->

**Что.** Что меняется — одним-двумя предложениями по существу.

**Зачем.** Последствие для человека или продукта (а не место в коде): что станет лучше/безопаснее.

**Проверяемо.** Как поймём, что цель достигнута — наблюдаемый признак, тест или замер.

**Границы/Источник.** Что осознанно НЕ берём; ссылка на решение в реестре / ROADMAP, если есть.
